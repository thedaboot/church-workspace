# 이 레포에서 일할 때의 규칙

## 커밋

- **커밋 메시지에 어시스턴트를 공동 작성자로 넣지 마세요.** `Co-Authored-By: Claude …`,
  `🤖 Generated with [Claude Code]` 같은 줄을 붙이지 않습니다. 이 레포의 커밋 작성자는
  노준석 한 사람입니다(사용자 결정 2026-08-27 — 전체 이력에서 그 줄을 지우고
  강제 푸시했습니다. 다시 붙이면 그 작업이 헛일이 됩니다).
- 메시지는 한국어로, 무엇을 왜 바꿨는지 적습니다. 형식은 `git log`를 보고 따르세요.

## 검증

- **고친 것과 관련된 스위트만 돌리세요.** `npm run verify`(전체 33종, 약 7분)를 습관적으로
  돌리지 마세요(사용자 지적 2026-08-27 — "해당 건에 대해서만 돌리라니까"). 개별 실행은
  `node tests/<이름>.mjs <서버주소>`이고, 브라우저 스위트는 게스트 서버가 필요합니다
  (`npx vite --mode guest --port 4598 --strictPort`).
- 무엇이 무엇을 보는지: 순수 로직 `logcheck` · 엑셀 `sheet` · 워드/PPT `office` ·
  드라이브·첨부 `drivesync` · AI 프롬프트 `aictx` · 대시보드/연결 지도
  `dashfix` · 내비/연도/탭 `navsmoke` · 달력 `calfit` · 드래그 `drag`·`dragdesk` ·
  디자인 규격 `handoff` · 마크다운 왕복 `mdcheck` · 다크/라이트 `themefit`. 목록은 `tests/README.md`.
  **문서(HANDOFF·README·V2)가 코드와 맞는지 자동으로 보는 검사는 없습니다** — 사람이 맞춰야 합니다.
- **전체는 사용자가 부를 때만 돌립니다.** 예전에는 "푸시 직전 한 번"이라고 적혀 있었는데,
  2026-09-06에 그 문장을 근거로 푸시 직전에 전체를 돌리려다 중단당했습니다("전체 검증
  하지말고. 그 해당 건만 검증 후 푸시해줘" — 같은 지적의 네 번째). **푸시할 때도 관련
  스위트만** 돌리고 푸시하세요.

## 화면 문구

- **안내·힌트 문구 줄을 임의로 추가하지 마세요.** "노드를 끌어서 정리할 수 있어요" 같은
  사용법 안내를 붙였다가 지적받았습니다(2026-08-27). 필요해 보이면 코드에 넣기 전에
  먼저 물어보세요. 정보가 밖으로 나가는 고지(예: 오피스 뷰어로 파일 주소가
  마이크로소프트에 전달된다)는 예외로 유지합니다.

## 그 밖의 모든 것

`HANDOFF.md`가 기준입니다 — 2026-09-11에 **짧은 기준서**로 줄였습니다(사용자 결정: 매 세션 컨텍스트에
쌓인다). 거기에는 코드·git에서 읽을 수 없는 것만 둡니다 — 사용자 결정·관례·흐름·화면↔파일 지도·
비밀 자리·남은 일. 특히:

- **§7 "다시 제안하지 말 것"** — 사용자가 판단해서 뺀 것들입니다. 다시 꺼내지 마세요.
- **§3 흐름** — 새 기능을 붙이는 순서(저장 자리 → 마이그레이션 → 코드 → 검사 → 문서).
  줄일 때 절 번호가 하나 밀려서, 코드 주석의 `§2-N`은 지금 §3의 N단계입니다.
- **§4 화면↔파일 지도** → 그 파일 **머리말 주석** → 필요할 때만 함정 번호. 이 순서로 찾으세요.
- **함정은 `docs/PITFALLS.md`로 옮겼습니다**(옛 §4·§6). 코드 주석의 `§6-N`·`§4.N`은 그 문서의
  번호이고, **그 파일을 건드릴 때 해당 번호만** 읽으면 됩니다. 전체를 읽을 문서가 아닙니다.
  번호는 코드가 가리키므로 바꾸거나 중간에 끼우지 말고 `9-a`처럼 뒤에 갈라 쓰세요.
- 검사는 **관련된 것만** `npm run verify -- <이름>`. 새 로직에는 검사를 하나 남기고,
  **일부러 되돌려서 실제로 실패하는지 확인**하세요(§3-5).
