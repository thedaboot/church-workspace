# 이 레포에서 일할 때의 규칙

## 커밋

- **커밋 메시지에 어시스턴트를 공동 작성자로 넣지 마세요.** `Co-Authored-By: Claude …`,
  `🤖 Generated with [Claude Code]` 같은 줄을 붙이지 않습니다. 이 레포의 커밋 작성자는
  노준석 한 사람입니다(사용자 결정 2026-08-27 — 전체 이력에서 그 줄을 지우고
  강제 푸시했습니다. 다시 붙이면 그 작업이 헛일이 됩니다).
- 메시지는 한국어로, 무엇을 왜 바꿨는지 적습니다. 형식은 `git log`를 보고 따르세요.

## 검증

- **고친 것과 관련된 스위트만 돌리세요.** `npm run verify`(전체 26개, 약 7분)를 습관적으로
  돌리지 마세요(사용자 지적 2026-08-27 — "해당 건에 대해서만 돌리라니까"). 개별 실행은
  `node tests/<이름>.mjs <서버주소>`이고, 브라우저 스위트는 게스트 서버가 필요합니다
  (`npx vite --mode guest --port 4598 --strictPort`).
- 무엇이 무엇을 보는지: 순수 로직 `logcheck` · 엑셀 `sheet` · 워드/PPT `office` ·
  드라이브·첨부 `drivesync` · AI 프롬프트 `aictx` · 대시보드/연결 지도
  `dashfix` · 내비/연도/탭 `navsmoke` · 달력 `calfit` · 드래그 `drag`·`dragdesk` ·
  문서 정합성 `handoff`·`mdcheck` · 다크/라이트 `themefit`. 목록은 `tests/README.md`.
- 전체를 도는 것은 **푸시 직전 한 번**이면 충분합니다.

## 화면 문구

- **안내·힌트 문구 줄을 임의로 추가하지 마세요.** "노드를 끌어서 정리할 수 있어요" 같은
  사용법 안내를 붙였다가 지적받았습니다(2026-08-27). 필요해 보이면 코드에 넣기 전에
  먼저 물어보세요. 정보가 밖으로 나가는 고지(예: 오피스 뷰어로 파일 주소가
  마이크로소프트에 전달된다)는 예외로 유지합니다.

## 그 밖의 모든 것

`HANDOFF.md`가 기준입니다. 특히:

- **§7 "다시 제안하지 말 것"** — 사용자가 판단해서 뺀 것들입니다. 다시 꺼내지 마세요.
- **§2 흐름** — 새 기능을 붙이는 순서(저장 자리 → 마이그레이션 → 코드 → 검사 → 문서).
- **§6 함정** — 코드 주석의 색인입니다. 그 파일을 건드릴 때 해당 항목만 읽으면 됩니다.
- 검사는 **관련된 것만** `npm run verify -- <이름>`. 새 로직에는 검사를 하나 남기고,
  **일부러 되돌려서 실제로 실패하는지 확인**하세요(§2-5).
