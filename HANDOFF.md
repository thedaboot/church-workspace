# 인수인계 — 더다붓 워크스페이스

교회 청년부 워크스페이스입니다. 이 문서는 **다음 사람이 코드를 처음 열었을 때 어디를 보면
되는지**, **이미 한 번 밟은 함정을 다시 밟지 않도록**, 그리고 **무엇이 남았는지**를 담습니다.
기능 소개는 [`README.md`](README.md), 검증 스위트 목록은 [`tests/README.md`](tests/README.md)에
있습니다.

- 레포 `github.com/thedaboot/church-workspace` · 브랜치 `main` · 배포 Vercel(푸시 시 자동)
- 최근 작업은 `git log --oneline -15`로 봅니다. **여기에 커밋 해시를 적지 않습니다** —
  커밋마다 손으로 고쳐야 해서 금방 낡습니다(실제로 한 번 어긋나 있었습니다)
- 검증 `npm run verify` → **23개 스위트 443 pass**, 약 7분

## 처음이라면 이 순서로

1. **§1 지금 무엇이 남았나** — 할 일이 있다면 여기부터
2. **§2 이 레포의 흐름** — 새 기능을 붙이는 순서(6단계). 이대로 하면 됩니다
3. **§7 다시 제안하지 말 것** — 사용자가 판단해서 뺀 것들. 이걸 다시 꺼내지 않는 것이
   이 문서의 목적 중 하나입니다
4. **§6 함정** — 코드에 달린 주석의 색인입니다. 그 파일을 건드릴 때 해당 항목만 읽으면 됩니다

## 이 문서 밖에 있는 것 (레포만 받아서는 알 수 없는 것)

1. **시각 규격의 원본**은 외부에서 받은 디자인 핸드오프 번들이고 레포에 없습니다.
   `C:\Users\노준석\Downloads\대시보드 완전 리디자인\design_handoff_workspace_redesign\README.md`
   규격 논쟁이 생기면 **그 문서가 기준**이고 임의 판단으로 바꾸지 않습니다. 한 번 어겼다가
   되돌렸습니다(모바일 KPI를 2×2 대신 3열로 바꿨는데 핸드오프대로 원복).
2. **비밀 값**은 로컬 `.env`와 Vercel 환경변수에만 있습니다(§5). 마이그레이션을 돌리려면
   `.env`의 `SUPABASE_DB_URL`(대시보드 Connect의 Session pooler URI)이 필요합니다.
3. **검증 스위트는 게스트 모드만 돌립니다.** 로그인·실시간·알림·푸시·첨부·담당자 저장·요약
   고정처럼 클라우드에서만 도는 경로는 테스트가 보지 못합니다. 이 사각지대가 실제로 버그를
   여럿 가리고 있었습니다 — 멘션 알림이 한 번도 생성되지 않던 것(§6-25), 담당자 추가 시
   duplicate key(§6-27), 업무 저장 시 `activity_pkey`(§6-28), 알림의 본인 제외가 통째로
   죽어 있던 것(§6-29). **그쪽을 건드리면 배포 후 직접 확인해 달라고 부탁하세요.**
4. **라이브 DB 확인은 psql로 합니다.** `.env`의 `SUPABASE_DB_URL`로 붙고, 한글이 깨지면
   `PGCLIENTENCODING=UTF8`을 붙이세요(psql 메타 명령에 한글을 넣으면 인코딩이 깨져서 SQL은
   파일로 넘기는 쪽이 안전합니다). RLS가 걸린 경로를 재현할 때는 트랜잭션 안에서
   `set local role authenticated` + `set_config('request.jwt.claims', …)`로 같은 역할·같은
   클레임을 만들고 **ROLLBACK**하세요. 참고 링크가 저장되지 않던 버그(§6-0)에서 "DB는
   정상이고 클라이언트가 문제"임을 이 방법으로 30초 만에 갈랐습니다.

---

## 1. 지금 무엇이 남았나

### 1.1 사용자만 할 수 있는 것 (클라우드 경로 — 검증 스위트가 못 봅니다)

아래는 전부 **코드·배포가 끝난 것**이고, 게스트 모드로 도는 테스트가 볼 수 없는 자리라
사람이 한 번 눌러 봐야 합니다. 순서는 값이 큰 것부터입니다.

- **프로필 사진**(§4.7). 로그인하면 가입 때 들어온 구글·카카오 사진이 카드·댓글·헤더에
  바로 보여야 합니다. **카카오로 가입한 세 분(조준환·조해리·박지호)** 이 특히 중요합니다 —
  그쪽 주소가 `http`라서 예전에는 브라우저가 막던 자리입니다. 그리고 '내 정보 → 사진 변경'으로
  하나 올려 보고 새로고침해서 남는지, '기본으로'가 글자 원으로 돌아가는지.
- **사람 칸·달력 생일**(§4.8). 대시보드에 '현재까지 가입한 사람'이 뜨는지, `N명`을 누르면
  최근 방문순 목록이 열리는지(접속 중이면 초록 원), 달력의 **8월 25일(조해리)·10월 23일
  (조준환)** 에 얼굴이 붙는지. **'오늘 다녀간 사람'은 각자 한 번씩 앱을 열어야 채워집니다**
  (0019 이후 첫 접속부터 기록됩니다).
- **최근 활동·선후관계**(§4.9). 대시보드 피드가 카드별로 묶여 뜨는지, 업무 창에서 '선행 업무'를
  하나 정하고 프로젝트 '그래프' 보기에서 선이 이어지는지(선행이 완료면 초록).
- **새 가입자가 열려 있는 화면에 바로 뜨는지**(0018). 한 기기에서 프로젝트를 열어 두고, 다른
  계정으로 새로 가입해 그 카드의 담당자를 자기로 바꿔 보세요. 열어 둔 화면이 **새로고침 없이**
  그 이름을 보여야 합니다(예전에는 '알 수 없음'이었고 담당자까지 어긋났습니다 — §6-21-a).
- **실기기 푸시**(§4.3). 종 팝오버에서 '이 기기로 알림 받기'를 켜고, 다른 계정으로 자신을
  담당자로 지정해 잠금화면에 오는지, 눌러서 그 업무가 열리는지. **아이폰은 홈 화면에 추가한
  뒤에만** 됩니다.
- **요약 고정·고치기**(§4.4). 관리자로 '이 요약 고정' → '고치기' → 저장 → 새로고침해서 DB에
  남았는지, 다른 계정에서 '3줄 요약'을 눌렀을 때 그 글이 나오는지.
- **참고 링크.** 하나 달고 새로고침해서 남는지(게스트 스위트는 localStorage까지만 봅니다).
- **2026-08-24 회차분** (전부 클라우드 경로거나 실데이터가 필요합니다):
  - **댓글 즉시 반영** — 두 기기로 같은 업무를 열고 한쪽에서 댓글을 쓰면 **양쪽 모두**
    바로 보이는지(예전에는 낡은 스냅샷이 목록을 덮어 "나갔다 와야 보였다"). 댓글에
    @멘션을 넣으면 그 사람 종·푸시에 바로 오는지.
  - **새 업무 만들면서 첨부** — 생성 폼에서 파일을 골라 두고 저장 → 보기 화면 첨부
    목록에 올라와 있는지, 활동 기록에 남는지.
  - **엑셀 펼쳐보기** — xlsx 첨부 행의 '펼쳐보기'로 업무 창 안에 표가 뜨는지(MS 뷰어).
  - **탭 드래그 순서** — 데스크톱에서 프로젝트 탭을 끌어 순서를 바꾸고 새로고침·다른
    계정에서도 같은 순서인지(0021 적용 후).
  - **프로젝트 삭제 개방** — 관리자가 아닌 계정으로 삭제가 실제로 되는지(0021 적용 후).
  - **멘션 목록** — 댓글·본문에서 `@`를 치면 **가입자 전원**이 나오는지(예전에는 6명에서
    잘려 뒷순번 사람이 아예 없는 것처럼 보였습니다). 목록은 스크롤됩니다.
  - **만들면서 첨부(다시)** — 저장하자마자 첨부 자리에 '올리는 중…' 줄이 뜨고, 끝나면
    실제 파일로 바뀌는지. 예전에는 업로드가 끝날 때까지 첨부 영역이 통째로 비어 있어
    "다시 들어가야 보인다"였습니다.
  - **로딩 자리** — 업무 창을 처음 열 때 댓글·활동 자리에 스켈레톤이 먼저 오는지
    ("첫 댓글을 남겨보세요!"가 번쩍이지 않아야 합니다), 엑셀 '펼쳐보기'가 뜰 때까지
    우리 스켈레톤이 MS 뷰어의 로딩 화면을 가리는지. 둘 다 클라우드 경로라 게스트
    스위트가 못 봅니다.
- **2026-08-25~26 회차분** (전부 클라우드 경로입니다):
  - **가입 승인**(0022) — 다른 계정으로 새로 가입하면 '승인을 기다려주세요' 화면만
    보이고 프로젝트·업무가 하나도 안 보이는지, 관리자 종에 승인 알림이 오는지,
    '멤버' 화면에서 수락하면 바로 들어와지는지. **기존 15명은 전원 승인 백필**했으므로
    아무도 잠기지 않아야 합니다(배포 직후 이것부터 확인하세요).
  - **관리자 지정·해제** — '멤버' 화면에서 이메일로 추가·해제. **자기 자신은 못
    내립니다**(DB도 막습니다). `VITE_ADMIN_EMAILS`는 없앴으니 재배포가 필요 없습니다.
  - **카드 순서**(0024) — 같은 컬럼 안에서 위아래로 끌어 순서가 바뀌고, 컬럼의 빈
    자리에 놓으면 맨 밑으로 가는지. 새로고침·다른 계정에서도 같은 순서인지.
  - **연도 고르기**(0025) — 탭 줄 앞 `2026 ▾`. 2027로 바꾸면 2027 프로젝트만 뜨는지.
    프로젝트 창의 연도 칸으로 옮길 수 있는지.
  - **엑셀 비밀번호**(0023) — 자물쇠로 걸고 다시 열 때 묻는지. **화면을 가리는
    잠금이지 파일을 잠그는 것이 아닙니다**(0023 주석).
  - **드라이브 첨부** — 새 파일을 올리면 `프로젝트 / 업무` 폴더에 들어가는지,
    앱에 썸네일이 뜨는지. **프로젝트·업무 이름을 바꾸면 드라이브 폴더 이름도
    따라가는지**(0026).
  - **저장해도 댓글·활동이 안 비는지**(§6-28-a·§6-28-b) — 원인이 둘이었고 첫 수정
    뒤에도 증상이 그대로였습니다. 기록이 새로 안 생기는 저장(팀만 바꾸거나 그냥
    저장)도 같이 봐야 합니다. 그쪽이 활동이 통째로 0이 되던 경우입니다.

### 1.2 다음에 만들 것 (사용자가 고른 것)

대시보드 참여 아이디어 30개 중 사용자가 고른 것들입니다. 1·2·3·4·5·6·28·29는 끝났고
(§4.7~4.9) 남은 둘은 이렇습니다.

- **「이거 제가 할게요」** — 담당자 없는 업무를 대시보드 목록 줄에서 바로 자기 배정.
  참여 문턱을 가장 크게 낮추는 항목이고 **새 데이터가 필요 없습니다**. 대시보드가 이미 길다는
  지적을 받았으니 **카드를 새로 만들지 말고 줄 안 인라인 버튼**으로 붙이세요.
- **AI 업무 쪼개기** — 큰 업무에서 하위 업무 초안을 뽑아 체크박스로 채택. `cards.subtasks`와
  `services/ai.js`가 이미 있어서 새 컬럼이 필요 없습니다.

**다시 꺼내지 않기로 한 것**은 §7에 있습니다. 랭킹·점수·배지·연속 참여(스트릭)도 같은
취급입니다 — §8의 "누가 누구와 견주는 구조를 만들지 마세요"와 정면으로 부딪힙니다.

### 1.3 짚어둔 것 (급하지 않음)

- **`DatePicker`/`AssigneePicker` 드롭다운만 portal이 아니라 `absolute`** 입니다. 업무 창
  본문이 `overflow-y-auto`라 잘릴 수 있습니다(지금 깨지지는 않습니다). §6-1대로 body 포털이
  기본이어야 합니다.
- **캘린더 띠 줄 수 상한 `CAL_LANES = 2`** — 사용자가 2로 유지하기로 정했습니다.
- **검색은 아직 `String.includes()`** 입니다. "버스 견적"이 "전세버스 견적서"를 못 찾고,
  댓글·첨부 내용은 검색되지 않습니다. RAG(pgvector)를 도입할 자리로 정리해 두었는데,
  카드 본문은 이미 AI 컨텍스트에 다 들어가므로 **첨부 파일 내용·댓글·지난 프로젝트
  아카이브**에만 값이 있습니다. **카드에 벡터를 붙이는 것부터 시작하지 마세요.**
- **보드 조작 줄 합치기.** 모바일에서 카드에 닿기까지 여섯 겹입니다(상단바 · 프로젝트 탭 ·
  값 줄 · 링크 줄 · 보드/캘린더/그래프+팀 필터 · 상태 칩). 겹을 진짜로 줄이려면 마지막 두
  줄을 합치는 쪽이 값이 큰데, 보드 자체를 건드리는 일입니다.
- **성능은 측정하지 않았습니다.** 이 PC에서는 실행 간 편차가 3배까지 나서, 예전에 낸 "블러
  제거로 3배 빨라졌다"는 결론은 근거가 없어 철회했습니다. **성능 주장은 같은 실행 안에서
  조건을 교대(A/B)해 비교하세요.** 이 함정에 두 번 빠졌습니다.
- **메인 번들 667KB(gzip 193KB).** TipTap·pdf.js는 이미 분리돼 있고 남은 큰 덩이는
  supabase-js·dnd-kit·React입니다.
- **구글 드라이브 첨부 이관이 끝났습니다**(2026-08-26). `files` 233건 전부 `source='drive'`
  이고 Storage에 남은 것은 0건입니다. 구조는 `더다붓 워크스페이스 / 프로젝트 / 업무 / 파일`
  (프로젝트 폴더 5 · 업무 폴더 12). 자세한 건 `docs/DRIVE.md`.
  **다만 Storage 객체 337MB는 아직 지우지 않았습니다** — `files.storage_path`가 그대로라
  되돌릴 수 있습니다. 실제로 폴더 구조를 바꾸느라 이미 옮긴 153건을 되돌렸고(실패 0),
  그때 이 원본이 있어서 아무것도 잃지 않았습니다. **눈으로 확인하기 전에는 지우지 마세요.**
  지울 때는 `files.storage_path`를 null로 만들고 Storage 객체를 지웁니다.
- **`tests/`는 헤드리스 Chrome이 있는 로컬에서만 돕니다.** CI에 붙이려면 Chrome 설치와
  `CHROME` 환경변수가 필요합니다.
- **Supabase Egress.** 사용자가 규모 대비 많다고 지적해 두 군데를 고쳤다(2026-08-24) —
  ① 첨부 서명 URL을 50분 재사용(매번 새 토큰이라 브라우저 캐시가 통째로 빗나가 같은
  썸네일을 열 때마다 다시 내려받았다), ② 본문 이미지를 올리기 전에 축소(긴 변 1600px
  jpeg — 아바타 squareThumb과 같은 판단). 남은 후보: 첨부 이미지 썸네일이 첫 열람에는
  여전히 원본을 받는 것(업로드 시 썸네일 파일을 따로 만들면 해결되지만 코드·경로가
  늘어난다), 초기 로드의 `listAllCards`가 본문 전체를 읽는 것(검색·AI가 써서 빼기
  어렵다). **어디서 나가는지는 Supabase 대시보드의 Egress 분류(Storage/DB/Realtime)를
  먼저 보고 정하세요** — 짐작으로 고치면 §1.3의 성능 함정과 같은 길입니다.
- **Vercel이 푸시를 놓칠 수 있습니다.** 실제로 한 번, 푸시가 GitHub에는 갔는데 배포가
  만들어지지 않아 "고쳤는데 안 보인다"가 됐다. 푸시 뒤 `gh api repos/…/deployments`로
  새 배포가 생겼는지 확인하고, 없으면 빈 커밋으로 재트리거하세요
  (`git commit --allow-empty` 후 푸시).
- **스크린샷을 레포 루트에 흘리지 마세요.** 검증 스크립트를 임시로 돌릴 때 출력 경로를 `.`로
  넘기면 루트에 png가 쌓입니다(실제로 8장 1.9MB가 커밋됐다가 지웠습니다). `.gitignore`가
  이제 루트 이미지를 막습니다 — 임시 스크립트는 스크래치 폴더로 출력하세요.

---

## 2. 이 레포의 흐름 (새 기능을 붙일 때)

기능을 여럿 붙이면서 매번 같은 순서를 밟았습니다. 그대로 하면 됩니다.

1. **저장 자리를 먼저 고릅니다 — 컬럼이냐 조인 테이블이냐.**
   카드와 언제나 같이 읽고 쓰고 항목이 몇 개뿐이면 **컬럼**(jsonb라도)이 맞습니다. 조인
   테이블은 따로 조회·집계할 이유가 있을 때만. 이유는 §6-27 — 조인은 왕복이 두 번이라 저장이
   겹치면 깨지고, 컬럼은 마지막 것이 남을 뿐입니다.
2. **앱 안의 모양은 그대로 두는 쪽을 먼저 봅니다.** 0013이 담당자를 조인으로 옮겼는데도
   `task.assignees`는 여전히 이름 배열입니다. 그래서 셀렉터·뷰·활동 기록·AI 컨텍스트를 한 줄도
   안 고쳤습니다. 저장 계층만 바꾸는 길이 있는지 먼저 찾으세요.
3. **마이그레이션은 dry-run 먼저.**
   `npx supabase db push --db-url "$SUPABASE_DB_URL" --dry-run` → 새 파일만 대상인지 보고
   `--yes`로 적용 → **psql로 결과를 눈으로 확인**(컬럼·제약·정책·백필 건수).
   되돌리는 SQL을 마이그레이션 파일 맨 아래 주석으로 같이 적어두세요.
4. **코드가 새 컬럼을 읽기 시작하면 마이그레이션이 먼저 나가야 합니다.** 조회에 없는 관계를
   넣으면 `loadCloudState`가 던지고 **모두에게 오류 화면**이 뜹니다. 순서는 마이그레이션 → 푸시.
5. **검사 하나를 남깁니다.** 화면이면 브라우저 스위트에, 순수 로직이면 `logcheck`에.
   **그리고 일부러 되돌려서 실제로 실패하는지 확인하세요.** 이걸로 여러 번 걸렀습니다 —
   담당자 조인 문장 모양, 활동 기록(첫판은 옛 방식도 통과해서 다시 썼습니다), 헤더 레이아웃
   (첫판은 옛 레이아웃에서도 통과해서 무엇이 진짜 고친 것인지 다시 찾았습니다).
   순수 로직은 `.js`(JSX 아님)에 두면 노드에서 바로 import해 검사할 수 있습니다
   (`utils.myScope`가 그래서 `utils.js`에 있습니다).
6. **클라우드 경로는 직접 확인해 달라고 부탁합니다.** 검증 스위트는 게스트 모드만 돕니다.
   지금까지 잡힌 버그 상당수가 사용자가 실제로 써 보고 알려준 것입니다.
7. **HANDOFF·README·`tests/README.md`를 같이 고칩니다.** 스위트 수·단정 개수·마이그레이션
   표가 낡으면 다음 사람이 그걸 믿습니다.

---

## 3. 화면 ↔ 파일 지도

```
src/App.jsx                 조립 + 라우팅 상태. 내비 마운트 분기, main 패딩, 뷰 래퍼
src/index.css               토큰(--app-*, --p-*) · 모션(dc-*) · 그리드·스크롤 유틸
                            폰트는 assets/fonts/suit.css를 import (§4.2)
src/config.js               팀·상태 상수, teamColor/teamBgColor/teamBar/teamPaint
src/utils.js                순수 헬퍼. generateId · formatRelative · subtaskProgress ·
                            httpsImage · seenToday · birthdaysWithin · visitOrder · agoLabel ·
                            birthdayMap · depLayers (전부 logcheck가 검사) ·
                            myScope(인사말이 세는 범위) — 브라우저 없이 검사되는 자리
src/components/layout.jsx   TopNav(데스크톱 2줄) / MobileTopBar / MobileTabBar / ProfileMenu
                            / SearchBox / NotificationBell(+PushRow)
src/components/boards.jsx   칸반 보드(dnd-kit) — 카드 · 상태 칩 · 컬럼 · DragOverlay
src/components/calendar.jsx 캘린더(주 단위 행) — 띠 배치(layoutWeek) · 모바일 달력 · 날짜 목록
                            · 날짜 옆 생일 얼굴(§4.8)
src/components/depgraph.jsx  프로젝트 '그래프' 보기 — 업무 선후관계 열 배치(utils.depLayers, §4.9)
src/components/linkIcons.jsx 참고 링크 앞의 서비스 표시(노션·유튜브 등). 경로는 커밋된 데이터
src/components/Avatar.jsx   사람 동그라미 — 사진 있으면 사진, 없거나 깨지면 이름 첫 글자(§4.7)
src/components/ConfirmPopover.jsx  삭제 확인 팝오버 + useAnchoredPos(팝오버 위치 공용 훅)
src/views/views.jsx         DashboardView / ProjectView / MyTasksView / TeamView / ScheduleView
                            / TeamFilterBar
src/views/dashboardParts.jsx  여러 화면이 공유하는 부품: 마감 구간 계산(byDue·groupByDue·
                            bucketOf·isStaleNoDue), KpiCell, Bar, StatusSegments, DueGroupList,
                            TeamLeftGrid, PersonLoadGrid·personLoad(청년별), SectionHead, Card,
                            PeopleStrip·MembersModal(§4.8), ActivityFeed·NetworkMap(§4.9)
src/modals/modals.jsx       업무 창 — 껍데기(TaskModalShell) · 보기(TaskViewer) · 수정 폼
                            (TaskEditor) · 하위 업무(SubtaskList) · 담당자 선택
                            · 선행 업무(DependsRow·DependsViewRow, §4.9)
src/modals/attachments.jsx  업무 창의 첨부 영역(업로드·미리보기 열기·삭제)
src/modals/comments.jsx     업무 창의 댓글 · 활동 기록 패널
src/modals/settings.jsx     내 정보(사진·이름·팀·연결된 계정) / 프로젝트 만들기·이름 수정
src/services/               cloud.js(Supabase) · cloudSync.js(모양 변환 + 실시간 라우팅)
                            · ai.js(Gemini 프롬프트+컨텍스트+요약 캐시) · markdown.js
                            · domain.js(TaskService·ActivityService)
                            · push.js(브라우저 구독 켜기/끄기)
                            · notifyText.js(알림 문구 — 앱과 api/push.js가 같이 본다)
                            · presence.js(지금 접속한 사람 — 워크스페이스 스토어 밖 미니 스토어)
src/store/                  useSyncExternalStore 기반 커스텀 스토어 + 셀렉터
public/sw.js                서비스 워커 — 푸시 표시 + 클릭 시 딥링크. 캐싱은 하지 않는다
api/                        ai.js(Gemini 프록시) · share.js(OG 메타)
                            · push.js(POST=앱이 만든 알림을 푸시로, GET=마감 임박 배치)
scripts/subset_suit.py      폰트 조각 생성(한 번 돌리고 결과물을 커밋 — §4.2)
tests/                      검증 스위트 + 러너
```

업무 창 네 파일은 서로를 이렇게 부릅니다: `modals.jsx`가 `attachments.jsx`와 `comments.jsx`를
가져다 쓰고, `settings.jsx`는 App이 직접 가져옵니다.

전역 화면 이름은 `App.jsx`의 `GLOBAL_MENUS`(`dashboard`·`myTasks`·`schedule`)에 모여 있습니다.
**새 전역 화면을 만들면 이 목록에도 넣으세요** — 없으면 그 이름이 프로젝트 id로 오해돼 '없는
프로젝트'로 판정되고 대시보드로 튕깁니다. 그리고 안에서 스크롤하는 화면(보드·달력)은
`needsFullHeight`에도 넣어야 높이가 확정됩니다(§6-2).

### 리디자인에서 손대지 않은 영역
`src/store/`, `src/hooks/`, `supabase/`, 첨부 관련(`FilePreviewModal.jsx`, `PdfView.jsx`,
`media.jsx`) — 디자인만 바꾸고 데이터 경로는 건드리지 않는다는 전제였습니다. 첨부·DB 동기화·
공유는 리디자인 후 실제로 다시 검증했습니다(`tests/share.mjs`, `tests/handoff.mjs`).

---

## 4. 무엇이 어떻게 되어 있나

### 4.1 검증

```bash
npm run verify                  # 전부. 게스트 dev 서버를 띄우고 끝나면 내린다
npm run verify -- calfit drag    # 골라서
npm run verify -- --jobs 3       # 동시 실행
SHOTS=1 npm run verify           # 스크린샷 저장(tests/*.png, gitignore)
CHROME=/path/to/chrome npm run verify
```

**중요 — `CRASH`를 그냥 넘기지 마세요.** 이 테스트들은 헤드리스 Chrome을 CDP로 붙잡고 실제
좌표를 재는 방식이라, 앱 마크업이 바뀌면 셀렉터가 못 찾고 **스크립트가 예외로 죽습니다.**
그때 `FAIL` 줄이 없어서 "안 돌고 있는 상태"가 통과처럼 보입니다. 러너가 이 경우를 `CRASH`로
따로 찍습니다. 실제로 여섯 개 스위트가 이 상태로 방치돼 있었고, 다시 맞추는 과정에서
드래그·캘린더 버그 두 개가 드러났습니다. 스위트를 쓸 때 **셀렉터를 못 찾으면 던지지 말고
FAIL로 남기세요**(그래야 어느 단정에서 어긋났는지 보입니다).

### 4.2 디자인 토큰 · 모션

`src/index.css` 한 곳에 모여 있습니다. `@theme inline`으로 Tailwind 유틸과 이중화되어 있어
`bg-surface` 같은 클래스와 `var(--app-surface)` 인라인 스타일이 같은 값을 봅니다.

- **진행 바 파스텔** `--p-track/--p-blue/--p-red/--p-yellow/--p-green/--p-gray/--p-brown/--p-purple/--p-pink`
  (라이트 스펙 값 `#93b4e4` / `#e5a29b` / `#e6e3e8`, 다크는 별도 정의)
- **이징 하나** `--ease-out-quint: cubic-bezier(.16, 1, .3, 1)`. 다른 이징을 새로 만들지 않습니다.
- **애니메이션 클래스** `.dc-screen`(260ms) `.dc-row` `.dc-card` `.dc-kpi` `.dc-pop`
  `.dc-bar-fill`(scaleX 전환) `.dc-draw`/`-2`/`-3`(선 그리기) `.dc-draw-ring`
- **`transform`/`opacity`만** 애니메이션합니다. 진행 바도 `width`가 아니라 `scaleX`.
- `prefers-reduced-motion`에서 전부 해제됩니다(`tests/handoff.mjs`가 검사).
- **가로 스크롤 줄** `x-scroll-lock`(= `touch-action: pan-x` + `overscroll-behavior-x: contain`).
  프로젝트 탭 · 프로젝트 헤더의 링크 줄 · 모바일 상태 칩 세 곳이 씁니다.
- 폰트 **SUIT Variable**. 패키지(`@sun-typeface/suit`)의 통짜 woff2(610KB)를 그대로 물리지 않고
  `scripts/subset_suit.py`로 **상용 한글 2,350자(503KB) + 나머지 음절(95KB)** 두 조각으로 나눠
  `src/assets/fonts/`에 커밋해 두었습니다. `index.css`는 거기 생성된 `suit.css`를 import하고
  `unicode-range`가 조각을 갈라 줍니다. **UI에 새 기호를 쓰면** 스크립트의 `LATIN` 범위에 넣고
  다시 돌려야 합니다 — 범위 밖 문자는 시스템 폰트로 떨어져서 한 문장 안에서 자획이 달라집니다
  (실제로 `⌘`를 빠뜨렸습니다). 더 잘게 나누는 길은 재보고 접었습니다(빈도별 92조각: 합계
  610→920KB로 늘고 첫 화면이 28조각 485KB. 이유는 스크립트 docstring에).
  Pretendard는 폰트 스택의 폴백 이름으로만 남아 있고 패키지는 없습니다.
- 아이콘 **lucide**, `svg.lucide { stroke-width: 1.4px }`로 굵기 통일. 이모지 아이콘 금지.
  lucide에 없는 브랜드 마크(노션·유튜브 등)는 `simple-icons`의 공식 path만 뽑아
  `src/components/linkIcons.jsx`에 커밋해 두었습니다 — **패키지를 의존성으로 두지 않습니다**
  (폰트 서브셋과 같은 방식). 색은 쓰지 않고 `currentColor`로 흐릅니다.
- 반응형 그리드 유틸 `.dash-grid`(1열 → lg에서 `1fr 360px`), `.side-grid`(lg에서 300px),
  `.kpi-grid`(모바일 2열 → lg 4열)
- **`docs/DESIGN.md`는 현재 기준이 아닙니다**(문서 머리에 적어 두었습니다). 초기 토큰 구조의
  참고 자료이고 값은 전부 갈렸습니다(accent `#0075de` → `#3f6fc4`, Pretendard → SUIT).

### 4.3 알림 · 웹 푸시

| 종류 | 만드는 자리 |
|---|---|
| `mention` `reply` | `cloudSync.notifyMentions` · `notifyComment` |
| `assign` | `controllers.js`의 `handleSaveTask` — `newAssigneesOnly`로 **이전에 없던 담당자만** |
| `due_soon` | `api/push.js`의 GET — Vercel Cron이 하루 한 번(22:00 UTC = 07:00 KST) |

- 문구는 `src/services/notifyText.js` **한 곳**입니다. 앱 안 목록과 잠금화면 푸시가 같은
  함수를 봅니다 — 갈라 두면 같은 알림이 두 군데서 다르게 읽힙니다.
- 푸시는 `cloud.insertNotifications` 안에서 같이 보냅니다(`requestPush`). 그 함수가 모든
  알림의 관문이라 **종류가 늘어도 푸시가 따라옵니다.** 기다리지 않고 실패도 삼킵니다.
- 권한은 **알림 종 팝오버의 '이 기기로 알림 받기'** 에서만 묻습니다(`PushRow`). 앱을 처음
  열 때 묻지 않는 이유: 무슨 알림인지 모르는 상태에서 거부하기 쉽고, 한 번 거부되면 브라우저
  설정에서 손으로 되돌려야 합니다.
- 알림은 **한 건씩 지울 수 있습니다**(목록의 X). 마감 임박 알림은 만들어진 시점의 문구를
  그대로 들고 있어서('내일 마감') 며칠 지나면 맞지 않는 줄이 남습니다. 업무를 끝내도 그 줄은
  사라지지 않습니다 — 배치가 완료된 카드에 **새** 알림을 만들지 않을 뿐입니다.
- 죽은 구독(410/404)은 발송할 때 서버가 지웁니다. 보관한 프로젝트는 마감 임박에서 뺍니다.
  같은 날 두 번 알리지 않게 넣기 전에 최근 20시간의 `due_soon`을 읽어서 거릅니다(유니크
  제약으로 못 막는 이유: `(created_at at time zone 'Asia/Seoul')::date`는 immutable이 아닙니다).
- **환경변수는 등록·배포까지 끝났습니다**(Production). `VITE_` 값은 빌드 시점에 박히므로
  **환경변수를 고치면 재배포가 필요합니다**(`npx vercel redeploy <배포 URL>`).
- 키가 없는 환경(로컬·프리뷰)에서도 앱은 돕니다. POST가 501을 주고 '알림 받기' 줄이 숨어서
  **푸시만 빠집니다.** 마감 임박 배치(GET)는 VAPID 키를 보지 않고 앱 안 알림을 만들고 발송만
  건너뜁니다 — 키가 없다고 라우트 전체를 막으면 종에도 아무것도 안 떠서 "마감 임박이 아예
  없는 것"과 구분이 안 됩니다. GET에 필요한 것은 `CRON_SECRET` 하나입니다.
- 손으로 부르기: `curl -H "Authorization: Bearer $CRON_SECRET" https://church-workspace.vercel.app/api/push`
  → `{cards, notified, sent, skipped}`
- 전달 방식을 웹 푸시로 정한 이유는 §7(카카오 알림톡)에 있습니다.

### 4.4 3줄 요약

- 저장 자리는 `cards.ai_summary`/`_at`/`_by`(0015). 쓰기는 `cloud.cardSummaryCloud`가
  **그 세 칸만** 건드립니다 — 카드 폼에 실어 보내면 요약을 고정하는 사람이 남의 편집을 같이
  덮습니다. 관리자의 '고치기'도 같은 경로입니다.
- **고정해도 업무 창을 열자마자 펼치지 않습니다.** '3줄 요약'을 눌렀을 때 나오고, 저장된
  것을 보여주므로 **AI를 다시 부르지 않습니다**(그 카드는 몇 명이 몇 번 열어도 API 비용 0).
  대신 화면은 새로 만드는 것과 똑같이 '분석하는 중'을 **2초** 지나서 나옵니다(`REVEAL_MS`).
  즉시 뜨면 같은 버튼이 사람마다 다르게 동작하고, 읽는 사람이 "누가 골라둔 글"임을 알아차립니다.
  **성능 문제로 오해하고 지우지 마세요 — 사용자가 원해서 넣은 지연입니다.**
- '고정' 배지는 **관리자에게만** 보이고 고정한 사람 이름은 화면에 붙이지 않습니다
  (`ai_summary_by`는 DB에만 남습니다). 읽는 사람에게는 저장된 글인지가 같은 값이고, 이름이
  붙으면 요약 내용보다 누가 손댔는지를 먼저 보게 됩니다.
- 관리자는 '고치기'로 저장된 요약을 손볼 수 있습니다. 마음에 든 요약의 한 줄만 바꾸고 싶을 때
  다시 돌리면 딴 글이 나옵니다.
- **관리자 제한은 화면에서 버튼을 감추는 것뿐입니다**(`canPin = cloudMode && isAdmin && id`).
  `cards`의 UPDATE 정책은 `auth.role() = 'authenticated'`라서 DB는 로그인 사용자 전체에
  열려 있고, `isAdmin`도 클라이언트가 이메일을 비교해 판정합니다. 요약은 다시 만들면 되는
  값이라 이대로 두기로 했습니다 — 진짜로 사라지는 일(프로젝트 삭제)은 `is_admin()`으로 DB에서
  막고 있습니다. 막으려면 요약 세 칸만 쓰는 security definer 함수를 두고 그 안에서
  `is_admin()`을 검사하는 길입니다(컬럼별 RLS 정책은 없습니다).

### 4.5 권한이 어디서 막히나

| 일 | 화면 | DB(RLS) |
|---|---|---|
| 앱에 들어오기 | 승인 전에는 '승인을 기다려주세요' 화면만 | 모든 내용 표의 정책에 `is_approved()`(0022) — 승인 전에는 DB에서도 0행 |
| 가입 수락·내보내기 | 전역 '멤버' 화면(관리자만) | `profiles_update_admin USING (is_admin())` |
| 관리자 지정·해제 | 같은 화면 | `admins_*` 정책이 `is_admin()`. **자기 자신은 삭제 불가**(마지막 관리자 보호) |
| 프로젝트 삭제 | 전원에게 버튼(확인 팝오버) | `projects_delete USING (authenticated)` — 0021에서 열었다(사용자 결정) |
| 업무 삭제 | 작성자·관리자 | `cards_delete USING (created_by = auth.uid() OR is_admin())` |
| 요약 고정·고치기 | `isAdmin`일 때만 버튼 | 막지 않음(`cards_update`는 authenticated 전체) |
| 팀 쓰기 | — | 관리자만(0009) |
| 알림 읽기·지우기 | — | 본인 수신 행만(0005) |
| 푸시 구독 | — | 본인 행만(0017). 발송은 서버가 service key로 |

관리자 원본은 **`admins` 표 하나**입니다(0022). 화면도 `is_admin()` RPC로 물어보므로 어긋날
자리가 없습니다. 예전에는 `VITE_ADMIN_EMAILS`(화면)와 표(DB)가 이중이라 **둘 중 하나만
넣으면 어긋났고**, 빌드 시점에 박히는 값이라 관리자를 한 명 늘릴 때마다 재배포가 필요했습니다.
그 환경변수는 코드·문서·`.env.example`에서 전부 지웠습니다 — **다시 넣지 마세요.**

승인 여부도 같은 방식입니다(`is_approved()`). 관리자는 자기 승인 여부와 무관하게 통과합니다 —
관리자가 잠기면 아무도 아무것도 승인할 수 없습니다. `AuthGate`는 승인 전에 워크스페이스를
**마운트하지 않습니다**(DB가 막혀 있어 들여보내도 빈 화면에 오류만 뜹니다). 권한을 아직 모르는
동안(`null`)은 승인된 것으로 봅니다 — 로그인 직후 한 프레임 '기다려주세요'가 번쩍이면 이미
쓰고 있던 사람에게 사고처럼 보입니다.

### 4.6 프로젝트 헤더 (모바일)

**개수가 변하는 것과 고정된 것을 같은 가로 스크롤 칸에 두지 않습니다.** 참고 링크는 미는 칸
안, 공유·삭제는 그 칸 **밖**에 둡니다(왼쪽 실선으로 갈라 "여기가 끝"을 보여줍니다).
링크가 몇 개든 삭제는 제자리입니다.

모바일은 두 줄입니다 — 위는 `값 + 진척 바 + 담당자 얼굴 + 새 업무`, 아래는 `링크 + 공유·삭제`.
값만 있던 줄이 비어 보여서 진척 바로 채웠습니다(대시보드가 쓰는 `Bar` 그대로).
데스크톱은 `md:contents`로 감싸개를 지워 예전처럼 한 줄로 흐릅니다.

**모바일 두 줄은 flex가 아니라 그리드입니다**(`grid-cols-[minmax(0,1fr)_auto]` → `md:flex`).
flex로 두면 왼쪽 칸이 `새 업무` 버튼 **왼쪽에서** 끝나고 아래 줄도 그 칸 안이라, 공유·삭제가
화면 오른쪽에 못 붙고 버튼 아래 어딘가에 떠 보였습니다. `새 업무`는
`row-start-1 col-start-2`로 자리를 못 박습니다 — 자동 배치에 맡기면 아래 줄 다음(3번째 줄)으로
떨어집니다.

**칸 네 개가 2×2로 각자 서 있습니다** — 감싸개를 `contents`로 지워서 값 줄·링크 줄·액션이 각각
그리드 칸이 됩니다. 액션(공유·삭제)이 `새 업무`와 **같은 칸**이라는 것이 핵심입니다.

- **실선·공유·삭제는 한 덩이**로 그 칸의 **가운데**에 섭니다(`justify-center`). 아이콘 묶음(약
  59px)이 버튼(80px)보다 좁아서 **한쪽 선에 붙이면 반대쪽에 구멍이 생깁니다.** 실선을 칸의
  테두리(`border-l`)로 두면 실선만 버튼 왼쪽 선에 남고 아이콘은 멀찍이 떨어져 보입니다 —
  그래서 실선도 안쪽 요소(`span[aria-hidden]`, 높이 16px = 아이콘 자획 높이)로 넣어 아이콘과
  같이 움직입니다.
- **`-mr-2`** 는 가운데를 잡을 때 마지막 아이콘의 오른쪽 여백 8px(`p-1.5` + lucide가 16px 박스
  안에서 비우는 2px)이 자획이 아니라서 상쇄하는 값입니다. 없으면 눈에 보이는 묶음이 버튼
  가운데보다 4px 왼쪽에 섭니다(왼쪽은 실선이 여백 없이 시작하니까요).
- **`+ 참고 링크`는 미는 칸 밖**입니다. 안에 두면 링크가 세 개쯤에서 **점선 버튼이 반쯤
  잘립니다** — 잘린 글자는 "오른쪽에 더 있다"로 읽히지만 잘린 점선 상자는 깨진 것처럼 보이고,
  밀어야 나오는 버튼은 §8을 어깁니다. 링크 칸은 `flex-1`이 아니라 내용만큼만 잡습니다
  (`flex-1`이면 링크가 없을 때도 칸이 늘어나 `+ 참고 링크`가 오른쪽으로 날아갑니다).
- **세로는 `items-center`** 입니다. 아래 줄에서 링크 글자(17px)와 아이콘 버튼(24px)은 높이가
  달라서, 칸을 위로 붙이면(`items-start`) 가운데선이 3~4px 어긋납니다.

기준선은 **헤더 아래 실선**입니다. 링크 0·1·3개 × 375·414px에서 실측한 값 — 메타 왼쪽 =
링크 왼쪽 = 실선 왼쪽, 묶음 가운데 = `새 업무` 가운데, 링크 줄 가운데선 = 아이콘 가운데선,
실선 밖으로 나가는 요소 없음(전부 0px). `tests/three.mjs`가 지킵니다.

**담당자 얼굴**은 진척 바 **뒤**입니다(`완료 N건`과 바는 같은 사실이라 그 사이를 가르지
않습니다). 세는 함수는 대시보드 '청년별 남은 업무'와 같은 `personLoad`이고 **남은 업무를 맡은
사람만** 셉니다 — '붙어 있다'는 지금 맡고 있다는 뜻이라, 프로젝트를 다 끝내면 얼굴이 사라지는
것이 맞습니다(사용자 판단). 4명까지 그리고 나머지는 `+N`이며 이름 글자는 쓰지 않습니다(같은
11px 글자를 늘어놓으면 메타 값과 구분이 안 되고, 원형은 한눈에 '사람'으로 읽힙니다).
이름을 다 보여줄 자리가 필요해지면 **눌러서 열리는 목록**으로 올리세요 — hover로만 나오게 두면
§8을 어깁니다.

얼굴을 4명에서 끊는 것은 폭 때문입니다. 이 줄에서 **양보할 수 있는 것은 메타 글자뿐**이라
(`truncate min-w-0`) 얼굴·바·버튼은 폭이 고정입니다 — 320px에서 실측한 결과 글자가 양보하지
않으면 얼굴이 `새 업무` 버튼 밑으로 파고듭니다(§6-9-c). 320~1440px에서 겹침 없음을 확인했고
`tests/three.mjs`가 지킵니다.

이 자리에서 검토하고 **접은 것**:
- **공유·삭제를 상단바(연필 옆)로 올리기** — 아이콘이 일곱 개가 되어 번잡합니다.
- **한 줄로 유지하고 메타 줄만 옆으로 밀기** — 삭제가 화면 밖으로 밀려서, '기능을 숨기지
  않는다'(§8)를 가로 스크롤로 다시 어기는 모양입니다.
- **`2/3`을 크게 두는 2단 배치** — 링크가 없어도 항상 두 줄이고, 바로 아래 상태 칩이 이미
  건수를 세고 있어 같은 값이 두 군데 나옵니다.

### 4.7 프로필 사진

**사진은 이미 전원 다 있었습니다.** 가입할 때 `ensureMyProfile`이 구글·카카오 메타데이터의
`avatar_url`을 `profiles.avatar_url`에 넣고 있었는데 **앱이 한 번도 읽지 않아서**, 어디서나
이름 첫 글자 원만 그리고 있었습니다. 그래서 대부분은 '내 정보'에서 아무것도 안 해도 얼굴이 보입니다.

- **동그라미는 `components/Avatar.jsx` 한 곳**입니다(카드·댓글·활동·알림·헤더·팀·대시보드 9곳).
  사진이 없거나 깨지면 **이름 첫 글자 원으로 돌아갑니다** — 깨진 이미지 아이콘을 그대로 두면
  그 사람만 화면이 고장 난 것처럼 보입니다. 사진 주소는 부르는 쪽이 넘기지 않아도 됩니다
  (앱 안에서 사람은 표시명으로 다니므로 `cloudSync.getAvatar(name)`가 이름으로 찾습니다).
- **`display`는 이 부품이 정하지 않습니다** — 호출부가 `flex`/`inline-flex`/`hidden sm:flex`를
  줍니다. 기본 클래스에 `inline-flex`를 박아 두면 §6-4대로 호출부의 `hidden`이 지고 모바일에서
  아바타가 두 개 보입니다.
- **카카오 사진 주소는 `http`** 로 옵니다. https 페이지에서 http 이미지는 브라우저가 혼합
  콘텐츠로 막아서 **카카오로 가입한 사람만 사진이 안 보였습니다.** `utils.httpsImage`로 올려
  줍니다(순수 함수라 `tests/logcheck.mjs`가 검사합니다).
- **직접 바꾸기**는 '내 정보'의 `사진 변경` / `기본으로`입니다(클라우드에서만 — 게스트에는 올릴
  곳이 없습니다). 저장소는 **새로 만들지 않았습니다** — 본문 이미지가 쓰는 `content-images`
  공개 버킷(0004)이 이미 "본인 폴더에만 업로드 · 공개 읽기"라 그대로 맞습니다. 업로드 함수도
  `cloud.uploadContentImage` 그대로입니다. **마이그레이션이 필요하지 않았습니다.**
- 올릴 때 **가운데를 정사각으로 잘라 256px jpeg로 줄입니다**(`squareThumb`). 폰 사진 5MB를
  18px 동그라미에 그대로 쓰면 목록마다 수십 개가 뜹니다. 자르기 UI는 두지 않았습니다.
- 사진은 고르는 즉시 올려서 미리 보여주고 **적용은 '저장'에서** 합니다. `기본으로`는 빈 문자열을
  보내 컬럼을 null로 지웁니다(→ 다시 글자 원). **안 건드렸으면 payload에서 키를 빼야 합니다** —
  `UPDATE_USER`가 payload를 그대로 펼치므로 `avatarUrl: undefined`를 넘기면 지금 사진이 사라집니다.

담당자 선택기에는 아직 얼굴이 없습니다(이름만) — 필요하면 같은 부품을 끼우면 됩니다.

### 4.8 대시보드 '사람' 칸 · 달력 생일 (0019)

대시보드가 숫자만 있고 사람이 없다는 이야기에서 나왔습니다. 참여의 시작은 "여기 사람이
있다"이고, 그걸 말하려면 얼굴이 필요합니다.

- **어디에 있나.** 데스크톱은 오른쪽 칸 맨 위, 모바일은 **KPI 아래·업무 목록 위**입니다.
  모바일에서 1열로 쌓이면 오른쪽 칸이 목록 **전부 아래**로 내려가므로, 감싸개를 `contents`로
  지우고 이 칸만 `order-first`로 올립니다(§6-3처럼 컴포넌트를 두 벌 두지 않는 방법).
- **세 줄뿐이고 없는 줄은 안 그립니다** — 오늘 다녀간 사람(얼굴만) · 생일(7일 전부터) ·
  새로 온 사람(3일). 기간은 사용자가 정했습니다. 환영이 오래 걸려 있으면 인사가 낡습니다.
- **줄 수에 언제나 상한이 있습니다**(`PEOPLE_ROWS = 2`). 수련회 시즌에 열 명이 한꺼번에
  가입하면 이 카드가 업무 목록을 화면 밖으로 밀어냅니다. 넘치면 `얼굴 묶음 + N명 더 …`입니다.
- **머리줄은 `현재까지 가입한 사람` + `N명`이고 그 숫자를 누르면 전체 목록이 열립니다**
  (`MembersModal` — 먼저 온 순서, 며칠 전에 왔는지). 가입일이 없는 사람도 목록에서 빼지
  않습니다 — 빼면 세어 봤을 때 머리줄 숫자와 달라집니다.
- **그 모달은 `createPortal`로 body에 띄웁니다**(§6-1). 그냥 `fixed`로 두었더니 대시보드
  뿌리의 `.dc-screen` transform이 조상 containing block이 되어 창이 화면 아래쪽에 박히고
  하단 탭바에 잘렸습니다.
- **나는 언제나 '오늘 다녀간 사람'에 듭니다.** `last_seen_at`은 로드가 끝난 뒤에 찍으므로 방금
  읽은 목록에는 없습니다. 기다렸다 로드하면 첫 화면이 그만큼 늦어집니다.
- **`activity`로 세지 않은 이유**: 거기에는 행동만 남습니다(제목 바꿈·댓글). 들어와서 보기만 한
  사람은 아무 데도 안 남아 얼굴 줄이 실제보다 훨씬 적게 나옵니다. 카드별 조회를 추적하면
  "누가 무엇을 봤는지"가 남는데, 같이 사역하는 사람들이 쓰는 화면에서 그건 감시처럼 읽힙니다
  (사용자 판단). 접속 시각 한 칸이 정직한 중간입니다.
- **순위·점수·판정어를 두지 않습니다.** 다녀간 사람은 많이 온 순으로 세우지 않습니다 —
  그러면 그 줄이 곧 "덜 온 사람" 목록이 됩니다(§8).
- **달력의 생일**은 날짜 숫자 **옆의 작은 얼굴**이고, 고른 날 목록의 첫 줄로도 나옵니다.
  띠 레인에 넣지 않는 이유: 레인이 두 줄뿐이라(`CAL_LANES`) 생일이 업무 띠를 밀어냅니다.
  업무 건수(`N건`)에는 세지 않습니다. 대신 생일만 있는 날은 `해당 날짜에는 업무가 없어요`가
  아니라 생일 줄이 뜹니다.
- 세는 로직은 전부 `utils.js`의 순수 함수이고 `tests/logcheck.mjs`가 지킵니다 —
  `seenToday` · `birthdaysWithin` · `joinedWithin` · `joinedOrder` · `birthdayMap`/`birthdaysOn`.
  **`localDate()`가 "오늘"과 "이 값 파싱"을 겸하다가 두 번 데었습니다**(값이 없는 사람이
  전부 오늘로 셈해졌습니다) — 지금은 `todayLocal()`로 갈라 두었습니다.

### 4.9 최근 활동 · 프로젝트 연결 지도 · 업무 선후관계 (0020)

- **최근 활동 피드**는 대시보드 사이드에 있고 **카드별로 묶습니다**(`groupFeed` — 카드마다
  최근 한 줄 + '외 N건', 다섯 카드까지). 안 묶었더니 카드 하나를 다듬을 때 기록이 줄줄이
  생겨 같은 제목이 여덟 줄 반복됐고, 그게 대시보드를 길게 만들었습니다(사용자 지적).
  '내 업무만 보기'는 접었습니다 — 이 칸의 값은 남들이 움직이는 게 보이는 것입니다.
  게스트는 tasks의 activityLog에서 파생하고(selectActivityFeed 폴백), 클라우드는 서버
  피드입니다. **실시간 라우팅**: activity 이벤트는 전체 재조회가 아니라 피드만 다시
  읽습니다(App에서 500ms 디바운스 — 저장 한 번에 기록이 여러 건 생깁니다).
  피드 줄에 `.dc-row`를 붙이지 마세요 — 검사들이 그 클래스로 마감 목록 행을 셉니다.
- **프로젝트 연결 지도**(사람—팀—프로젝트)는 본문 아래 전폭입니다. **모바일 좌표는 화면 폭에서 계산합니다**(nmMobile — 글자 열은 딱 필요한 폭만 갖고 남는
  것은 전부 선 구간에 줍니다. 고정 296px로 두었더니 양옆이 비고 곡선이 26px뿐이라 답답했습니다).
  카드 패딩을 -mx-2로 물어 그만큼도 선에 보탭니다. 옆으로 밀지 않아도 전체가 보입니다
  (안전망으로 overflow는 남겼습니다). 열 머리글은 노드와 같은 정렬입니다(사람
  오른쪽·팀 가운데·프로젝트 왼쪽) — 전부 왼쪽 정렬하면 머리글이 노드 무리에서 떨어져 보입니다. 세 열이 640px을 써서
  사이드 칸(360px)에 넣으면 프로젝트 열이 잘립니다(실제로 그랬습니다). 좌표는 측정 없이
  렌더와 같은 상수(NM)로 계산하고, **선 양끝에 점을 찍고 사람 이름을 오른쪽 정렬**로 선
  시작점에 붙입니다 — 선이 허공에서 시작하면 끊긴 그림이 됩니다(사용자 지적).
  좁으면 가로로 밉니다(그래프는 한 덩이 캔버스라 §8의 '숨긴 것'이 아닙니다).
- **업무 선후관계**는 `cards.depends_on`(uuid[] 컬럼 — 조인이 아닌 이유는 §2-1·§6-27)이고,
  업무 수정 폼의 '선행 업무' 줄(네이티브 select + 칩)에서 정합니다. 프로젝트 화면의 세 번째
  보기 '그래프'가 `utils.depLayers`로 열을 배치합니다 — 순환은 끊고(빈 열은 걷어냄, 희소
  배열 구멍이 sort에서 던졌습니다), 지워진 카드를 가리키는 id는 무시합니다. 선행이 끝났으면
  초록 선, 아직이면 회색 — "어디가 막혀 있나"가 색으로 보입니다.
- **가입한 사람 모달**은 **최근 방문순**입니다(접속 중 → 최근 방문 → 기록 없음). 접속 중은
  Realtime presence(`services/presence.js` 미니 스토어 — LOAD_STATE가 상태를 통째로 갈아치우는
  워크스페이스 스토어에 섞으면 재조회마다 사라집니다)로 판정하고 아바타 귀퉁이 초록 원 +
  '접속 중'으로 보입니다. 시간은 `utils.agoLabel`(초→분→시간→일→주→개월→년), 방문 기록이
  없으면 **가입 시각으로 대신**합니다(`lastVisitOf` — 가입하던 순간에도 앱에 있었습니다).
  팀은 대표만이 아니라 **전부** 보입니다(겸직이 안 보였습니다).
- **수정 모드에도 업무 창 사이드바(댓글·활동)를 그대로 둡니다** — 없애면 본문이 전체 폭으로
  늘어나 오른쪽이 통째로 비어 보입니다(사용자 지적). 편집 폼은 로컬 state라 안전합니다.
- `useStore` 셀렉터에서 **매번 새 배열을 만들면 무한 리렌더**입니다(useSyncExternalStore가
  ===로 비교) — DependsRow에서 실제로 터졌고, 안정 참조(s.tasks)만 구독하고 파생은 useMemo로
  합니다. selectMembers의 NO_MEMBERS 상수도 같은 이유입니다.

### 4.10 대시보드 인사말

인사말은 **나에게 말을 거는 문장**이라 `utils.myScope`로 **내 것만** 셉니다. 담당자가 없는
업무는 '공통'으로 보고 함께 셉니다(아무의 것도 아닌 일은 아무도 챙기지 않습니다).
KPI·목록은 그대로 상단 세그먼트를 따라갑니다 — 그건 필터의 일입니다.

예전에는 인사말도 세그먼트를 따라가서, 기본값이 '전체'라 **미디어팀 다른 사람의 지연 한 건이
"OOO님, 밀린 업무부터 정리해봐요"로** 떴습니다. 남의 지연을 내 이름으로 나무라는 문장이었습니다.
내 지연은 없는데 KPI의 '지연'에는 숫자가 있는 경우(남의 것)를 위해 **`잘하고 있어요!`** 를 따로
뒀습니다. `지연된 업무가 없네요 :)`라고 하면 바로 아래 칸과 어긋나 보이고, `내가 맡은 업무에는
없네요`라고 하면 **남과 견주는 문장**이 됩니다 — 같이 사역하는 사람들이 쓰는 화면이고 누가
밀렸는지 가리키는 자리가 아닙니다(사용자 지적). 내 상태만 말하고 전체 숫자에는 아무 주장을
하지 않는 문장으로 둡니다.

---

## 5. 데이터 · 스키마 · 비밀

스키마는 `supabase/migrations/0001~0018`이고 **전부 라이브 DB에 적용**되어 있습니다
(0001~0005는 대시보드에서 수동, 이후는 `npx supabase db push --db-url "$SUPABASE_DB_URL"`).

| 파일 | 한 일 |
|---|---|
| `0001_init` | 테이블 10개 + RLS 32정책 · `card_teams` 다대다 · `comments.parent_id` · `is_admin()` |
| `0002_profile_selfheal` | 누락 프로필 백필 |
| `0003_attachments` | 첨부 `files` + Storage 버킷/정책 + teams 시드 |
| `0004_content_images` | 본문 이미지용 공개 버킷 `content-images` |
| `0005_notifications` | 멘션 알림 + RLS + Realtime |
| `0006_hold_status_and_drive` | 상태 `hold` 허용 + `projects.drive_folder_id` |
| `0007_reply_notifications` | `kind`에 `reply` 추가 |
| `0008_profile_teams` | 한 사람이 여러 팀에 속하는 조인 테이블 + RLS |
| `0009_cleanup` | 안 쓰는 컬럼 정리 · activity 고아 행 + cascade · teams 쓰기는 관리자만 |
| `0010_card_updated_by` | `cards.updated_by`(트리거가 채움 → 업무 창의 '수정: 이름') |
| `0011_linter_warnings` | 어드바이저 경고 — search_path 고정 · 공개 버킷 목록 조회 차단 |
| `0012_retention` | 보존 기간(pg_cron) — 읽은 알림 30일 / 활동 기록 6개월 |
| `0013_card_assignees` | 담당자를 표시명이 아니라 프로필로(조인 + 백필) |
| `0014_project_archive` | `projects.archived` 복구 — 프로젝트 보관 |
| `0015_subtasks_and_summary` | `cards.subtasks`(체크리스트) + `cards.ai_summary` |
| `0016_card_counts` | `cards.comment_count`·`file_count` + 재계산 트리거 |
| `0017_push_notifications` | `kind`에 `assign`·`due_soon` + `push_subscriptions` |
| `0018_profiles_realtime` | `profiles`를 Realtime 발행에 추가 — 새로 가입한 사람이 열려 있는 화면에도 바로 |
| `0019_profile_presence_birthday` | `profiles.last_seen_at`(다녀간 시각) + `birthday`('MM-DD') — 대시보드 사람 칸·달력 생일 |
| `0020_deps_and_activity_feed` | `activity`를 Realtime 발행에 + `cards.depends_on`(선행 업무, uuid[] 컬럼 — §2-1 판단) |
| `0021_project_delete_and_order` | 프로젝트 삭제를 authenticated 전체로(사용자 결정) + `projects.position`(탭 드래그 순서, 전원 공유) |
| `0022_approval_and_admins` | 가입 승인(`profiles.approved` + `is_approved()`) · SELECT/쓰기 정책을 승인된 사람으로 · `admins`를 화면에서 다루게 개방 · 가입 시 관리자 알림(`kind='approval'`) |
| `0023_file_password` | `files.view_pw`/`_salt`/`_by` — 엑셀 첨부 **화면 가림** 비밀번호(파일 자체를 잠그지 않는다) |
| `0024_card_position` | `cards.position` 백필 — 같은 상태 안에서 카드 순서를 손으로 정한다 |
| `0025_project_year` | `projects.year` — 연도를 손으로 정한다(만든 날짜에서 뽑으면 미리 만드는 내년 프로젝트가 엉뚱한 해에 들어간다) |
| `0026_card_drive_folder` | `cards.drive_folder_id` — 드라이브 업무 폴더를 id로 잡는다(제목을 바꿔도 파일이 갈라지지 않게) |

알아둘 것:

- **`projects.description`은 없습니다**(0009에서 삭제) — `createProject`에 다시 넣으면 실패합니다.
  값이 있던 `teams.color`는 남았지만 앱은 이 컬럼을 보지 않습니다(팀 색은 `config.js`).
- 0012의 pg_cron 확인은 `select * from cron.job`, 해제는 `cron.unschedule`. UTC 19:00 = KST 04:00.
- 0014: 보관은 삭제가 아니라 탭·대시보드에서 빼는 것이고, 보관함은 `created_at`으로 연도를
  묶습니다 — 연도 컬럼을 따로 두지 않습니다. **보관하면 그 프로젝트에서 나갑니다**(대시보드로) —
  머물러 있으면 "보관된 것을 열어 두면 탭에 끌어올린다"는 규칙 때문에 방금 보관한 것이 탭에
  그대로 남아, 보관함으로 들어갔다는 표시가 화면에 아무 데도 없습니다. 그리고 **지금 열어 둔
  보관 프로젝트는 더보기의 보관함 목록에서 뺍니다** — 안 빼면 같은 이름이 탭과 더보기에 동시에
  보입니다(둘 다 사용자가 잡아냈습니다). 보관 해제는 그 자리에 그대로 둡니다.
- 0016: 목록에서 카드를 열지 않고 대화·파일 유무를 보여주려고 **개수만** 들고 있습니다.
  앱이 세지 않고 트리거가 유지하므로 어느 경로로 들어와도 맞습니다(증감이 아니라 실제 행 수
  재계산이라 어긋나도 자기 회복). 전부 다시 세려면 0016 맨 아래 update문.
- 0017: 체크 제약과 INSERT 정책을 **같이** 넓혔습니다(0007이 가르쳐 준 것). `due_soon`은
  INSERT 정책에서 **일부러 뺐습니다** — 사람이 만들 알림이 아니고 서버(service key)는 RLS를
  우회하므로, 넣어 두면 로그인 사용자가 남에게 가짜 마감 알림을 보낼 수 있습니다.
- `cloud.js`의 `listProfileTeams()` / `setMyTeams()`는 **에러를 삼킵니다** — 마이그레이션 전
  코드와 섞여도 죽지 않게 한 의도이니, 조용히 실패할 수 있음을 염두에 두세요.
- **비밀 값은 이 문서에 없고, 앞으로도 넣지 마세요.** 서버 전용(`VITE_` 접두사 금지):
  `GEMINI_API_KEY`, `SUPABASE_SECRET_KEY`, `VAPID_PRIVATE_KEY`, `CRON_SECRET`,
  `DRIVE_WEBAPP_URL`, `DRIVE_WEBAPP_TOKEN`(Apps Script 웹앱 — Production·Development
  등록 완료, **Preview는 아직**입니다. 프리뷰 배포에서만 쓰이므로 급하지 않습니다). 값은 Vercel 환경변수와 로컬 `.env`에만(`.env`·`.env.guest` 모두 gitignore).
  변수 목록은 README에 있습니다.
- 오피스 미리보기는 **서명 URL이 마이크로소프트로 전달**됩니다. UI에 그 사실을 표기하고 있고
  `OFFICE_VIEWER = false`로 끌 수 있습니다.
- 어드바이저 경고 중 둘은 **의도해서 남긴 것**입니다: `is_admin()`을 로그인 사용자가 실행할 수
  있는 것(RLS 정책이 평가할 때 필요합니다. 불러도 "나는 관리자인가"만 알 수 있습니다), 그리고
  Leaked Password Protection(구글·카카오 OAuth만 쓰므로 저장하는 비밀번호가 없습니다).
  `rls_auto_enable()`은 이 레포가 만든 함수가 아니라 PUBLIC 권한만 회수했습니다.

---

## 6. 이미 밟은 함정 (다시 밟지 마세요)

코드에는 각 지점에 왜 그렇게 했는지 주석이 달려 있습니다. 여기 있는 것은 그 주석의 색인입니다.

### 레이아웃 · CSS

0. **포털로 띄운 팝오버는 "바깥 클릭" 판정에 자기 자신도 넣어야 한다.** 앵커 ref만
   `contains(e.target)`로 보면, 본체가 `createPortal`로 body에 나가 있어 앵커의 자손이 아니므로
   **팝오버 안을 누르는 것이 '바깥'으로 잡힌다.** `mousedown`에서 언마운트되니 그 뒤의 `click`은
   사라진 버튼에 닿지 않는다. 참고 링크가 이것 때문에 **한 건도 저장되지 않았다**(URL 칸을
   누르는 순간 닫혔고 DB `resource_links`가 0행이었다). 앵커 ref + 본체 ref 둘 다 보세요 —
   `ConfirmPopover`·`ProfileMenu`·`더보기`는 그렇게 하고 있었고 링크 팝오버만 빠져 있었다.
   **`el.click()`으로는 재현되지 않습니다**(mousedown이 안 나갑니다).
1. **`position: fixed`가 뷰포트 기준이 아니게 된다** — `.dc-screen`·`.dc-card`에 걸린 transform
   애니메이션이 조상 containing block이 되어, dnd-kit의 `DragOverlay`가 카드보다 **약 100px
   아래**에서 떴다. 해결: 오버레이를 `createPortal(..., document.body)`로. 앞으로 fixed로 띄우는
   것은 전부 body 포털을 기본으로 두세요.
2. **스크롤 컨테이너의 `padding-bottom`은 넘친 내용에 적용되지 않는다** — 뷰 래퍼의 `h-full`
   때문에 내용이 박스를 넘쳐 흘러서 `main`의 아래 여백이 무효가 되고 마지막 줄이 하단 탭바에
   가렸다. `h-full`은 내부 스크롤이 필요한 **프로젝트 화면에만**(`App.jsx`의 `isProjectScreen`).
   회귀는 `tests/bottomgap.mjs`가 막는다.
3. **CSS로 감추기 ≠ 언마운트** — 데스크톱/모바일 내비를 `hidden`/`md:hidden`으로만 가리면 둘 다
   마운트되어 알림 종이 두 개가 된다. 그러면 같은 Supabase 실시간 채널에 두 번 붙어
   `cannot add postgres_changes callbacks ... after subscribe()`로 앱이 죽는다. 해결:
   `useIsMobile()`로 **한쪽만 마운트** + `cloud.js`에서 같은 topic의 기존 채널 제거.
   **반응형으로 구조가 달라질 때 컴포넌트를 두 벌 두지 마세요** — 하나의 DOM에 반응형 클래스로
   가는 쪽이 맞습니다(프로젝트 헤더는 `md:contents`로 그렇게 했습니다).
4. **Tailwind display 유틸 충돌** — 컴포넌트 기본 클래스에 `inline-flex`가 있는데 호출부에서
   `hidden md:inline-flex`를 주면 같은 레이어라 `hidden`이 진다. display는 호출부가 소유하게.
5. **`<button>`은 내용을 수직 중앙 정렬한다** — 형제가 `div`인 grid에서 라벨이 8px 어긋난다
   (`block` 추가로 해결).
6. **iOS Safari의 `transform-box`가 Chrome과 다르다** — CSS로 회전한 SVG `<text>`가 부모 밖으로
   튀어나갔다(도넛 % 라벨). SVG 안에서 회전 텍스트를 쓰지 말고 HTML을 겹치세요.
7. **`touch-action: pan-x`는 자손 전체에 걸린다** — 가로 스크롤 줄에 걸면 그 안 카드 목록의 세로
   스크롤까지 죽는다. 보드 스크롤러에는 `overscroll-behavior-x`만 걸었다.
8. **`overscroll-behavior-x: contain` vs `none`** — `contain`은 부모로 넘기는 것만 막고 자기
   고무줄은 남긴다. 그래서 '완료' 오른쪽으로 더 갈 것처럼 밀렸다 → `none`.
9. **`button` 안에 `button`은 유효하지 않다.** 줄 전체가 클릭 대상인데 그 안에 지우기 버튼을
   넣어야 하면 줄을 `div`로 바꾸고 여는 영역만 `button`으로 남기세요(알림 목록).

   (번호는 문서 전체에서 `§6-N`으로 참조하므로 새 항목은 뒤에 붙이지 말고 여기처럼 `9-a`로
   갈라 씁니다 — 중간에 끼워 번호를 밀면 모든 참조가 어긋납니다.)

9-a. **두 줄을 세로로 맞추려면 두 줄을 같은 칸에 넣으세요 — 마진으로 밀지 마세요.**
   프로젝트 헤더에서 링크·공유·삭제 줄이 왼쪽 칸 안이라 `새 업무` 버튼 **왼쪽에서** 끝났고,
   공유·삭제가 화면 오른쪽이 아니라 버튼 아래 어딘가에 떠 보였다. 그리드로 바꾸고 아래 줄을
   `col-span-2`로 걸치니 오른쪽 끝에는 붙었지만, 이번엔 **공유 왼쪽 세로 실선이 버튼 왼쪽 선보다
   21px 오른쪽**이었다. 답은 액션을 버튼과 **같은 칸**에 두는 것(감싸개를 `contents`로 지우고
   `justify-end`)이었다 — 그러면 칸 폭이 버튼 폭이라 양쪽 선이 저절로 맞고 버튼 글자가 바뀌어도
   따라온다. 폭 계산으로 맞추면 그때만 맞는다. 정렬 문제는 세 번 지적받고서야 끝났다.

9-b. **아이콘은 여백까지가 버튼이라 배경 있는 버튼과 선이 안 맞는다.** 배경이 있는 버튼은 끝이
   곧 경계인데 아이콘은 여백이 경계다. `p-1.5`(6px) **더하기 lucide가 16px 박스 안에서 비워 두는
   좌우 2px** = 8px이라 상쇄는 `-mr-2`다. 판정은 svg의 rect가 아니라 **`getBBox()`로 잉크 상자**를
   재세요 — rect로 재면 2px 어긋난 채로도 "맞다"고 나옵니다(`-mr-1.5`로 끝냈다가 이 2px 때문에
   다시 지적받았습니다). 스크린샷 픽셀로도 확인했다.

9-b-2. **좁은 묶음을 넓은 버튼에 맞출 때는 한쪽 선이 아니라 가운데를 맞춘다.** 실선을 버튼
   왼쪽 선에, 아이콘을 오른쪽 선에 각각 붙였더니 그 사이 37px이 빈 채로 실선만 따로 떨어져
   보였다. 묶음이 버튼보다 좁으면 **어느 한쪽 선에 붙이는 순간 반대쪽에 구멍이 생긴다** —
   덩이째 가운데로. 그리고 가운데는 **박스가 아니라 자획**으로 잡으세요(§6-9-b). 정렬은 세 번
   지적받고서야 끝났고, 매번 "선 하나만 맞춘" 것이 원인이었다.

9-c-2. **`<button>`은 `display:flex`를 줘도 내용 폭으로 줄어든다**(폼 요소의 fit-content).
   피드 줄에서 `w-full`을 뺐더니 줄마다 폭이 달라져 시간 라벨이 제각각 섰고, `w-full`을 두면
   `-mx-2`가 폭은 못 늘리고 **왼쪽으로만 8px 밀어** 오른쪽이 16px 비었다(사용자가 두 번
   지적한 공백). 음수 마진으로 좌우를 넓히는 줄은 **`w-[calc(100%+16px)]`** 로 폭을 직접
   준다. div 줄은 width:auto라 저절로 넓어진다 — 같은 목록에 button 줄과 div 줄이 섞이면
   박스가 달라지니 같은 클래스를 준다(피드의 지워진 카드 줄).

9-c. **한 줄에 폭 고정인 것만 모아 두면 좁은 화면에서 서로 파고든다.** 프로젝트 헤더 값 줄이
   `메타(nowrap) + 진척 바(min-w) + 담당자 얼굴(shrink-0)`이라 아무것도 양보하지 않았고, 320px에서
   얼굴이 `새 업무` 버튼 **밑으로 들어갔다**(겹치지 않는 것처럼 보이지만 flex 항목의 최소 폭이
   글자 폭으로 굳어 칸을 넘긴 것이다). `main`의 가로 스크롤은 0이라 기존 넘침 검사에는 안 걸렸다.
   **줄마다 "양보할 것"을 하나 정하세요** — 여기서는 메타 글자(`truncate min-w-0`)다.

9-d. **모바일 입력 글자 크기를 CSS 한 줄로 덮지 마세요.** iOS Safari가 16px 미만 입력을
   탭할 때 화면을 자동 확대하는 것을 막으려고 `@media (max-width:767px) { input, textarea,
   select, [contenteditable] { font-size:16px !important } }`를 두고 있었다. 그 결과
   **11~13px로 설계된 칸이 12px 라벨보다 커지고**(담당자 '추가…', 선행 업무 select,
   하위 업무 입력, 댓글 입력), `text-2xl` 제목 입력은 도리어 **24 → 16px로 줄었다.**
   화면 전체의 글자 위계가 무너져 "폰트가 여기저기 안 맞는다"로 읽혔다(사용자 지적).
   자동 확대는 **viewport의 `maximum-scale=1`** 로 막는다(index.html) — iOS 10부터
   손가락 확대는 이 값과 무관하게 되므로 접근성은 그대로다. `tests/mobbits.mjs`가
   모바일에서 16px로 강제된 입력이 하나도 없는지 검사한다.

9-e. **스켈레톤은 `animate-pulse`로 두지 마세요.** 투명도를 1↔.5로 흔드는 것이라
   **흰 배경 위 옅은 회색에서는 눈에 아무 변화가 없다.** "로딩 중"이 아니라 "빈 상자가
   떡하니 있다"로 읽혔다(사용자 지적). 지금은 `.dc-skeleton`이 훑고 지나가는 빛
   (::after의 transform)으로 그린다 — 움직이는 것이 있어야 기다리는 중임을 안다.
   `prefers-reduced-motion`에서는 정적 음영으로 떨어진다.

9-f. **스켈레톤이 끝나지 않으면 원인은 스켈레톤이 아니다.** 첨부 썸네일이 영원히
   회색이었던 진짜 이유는 **80px 상자에 1.5MB 원본을 내려받고 있었던 것**이다
   (사진 열 장이면 LTE로 15MB). 이 프로젝트는 Storage 이미지 변환이 켜져 있어
   `createSignedUrl(..., { transform })`로 200px을 받는다. 변환은 서명 요청의
   **본문**에 실려 토큰에 묶이므로 묶음 발급(`createSignedUrls`)을 쓸 수 없다 —
   파일마다 한 번씩 서명하고 동시에 보낸다. "자리를 잡는" 스켈레톤은 실제 줄과
   **같은 높이**여야 하고 개수를 자르면 안 된다(36px×5줄로 80px×8줄을 대신하려
   했더니 자리를 잡으나 마나였다).

9-g. **서명 URL 캐시는 "주소를 오래 유지하는 것"이 전부다.** 브라우저 캐시는 주소가
   같을 때만 맞는다. 실측(2026-08-25): Storage 응답은 `cache-control: public,
   max-age=3600` + ETag이고 앞에 Cloudflare가 있다 — 주소만 유지되면 1시간 뒤에도
   304(본문 0바이트)다. 그래서 **썸네일만 7일로 서명**하고 6일까지 재사용하며,
   저장은 sessionStorage가 아니라 **localStorage**다(탭을 닫으면 주소가 바뀌어
   캐시가 다시 빗나간다). 새로 올리는 첨부는 `cacheControl: 30일`이다(경로에 uuid가
   박혀 있어 같은 주소가 다른 그림이 될 수 없다).

### dnd-kit

10. **가로 자동 스크롤이 드롭 타깃을 빗나가게 만든다** — 손가락이 화면 오른쪽 20%에 들어가면
    화면이 옆으로 밀려서, 놓으려던 상태 칩이 손가락 밑에서 빠져나간다(완료 칩에 놓았는데 진행
    중으로 저장). 해결: `autoScroll={{ threshold: { x: 0, y: 0.2 } }}` — 가로만 끄고 세로는 유지.
11. **`pointerWithin`이 비면 `rectIntersection`으로 떨어진다** — 이때 카드의 큰 사각형이 기준이
    되어 엉뚱한 컬럼이 잡힌다. 위 10번과 겹치면 원인 추적이 어렵다.
12. 터치와 마우스는 센서를 분리한다(`MouseSensor` distance 6 / `TouchSensor` delay 200).
    하나로 합치면 모바일에서 드래그가 아예 시작되지 않거나 스크롤과 싸운다.
12-a. **"놓은 카드 앞에 끼워 넣기"만 두면 아래로 끌 때 제자리로 돌아온다.** 나를 목록에서
    뺀 만큼 아래 카드들의 자리가 당겨지기 때문이다. 원래 자리가 놓은 자리보다 **위였으면
    뒤에, 아래였으면 앞에** 넣는다. 이건 테스트가 잡았다(사람 눈으로는 "안 움직인다"로만
    보인다) — 순서 바꾸기를 만들면 **아래로 끄는 경우를 반드시 검사에 넣으세요.**
12-b. **카드를 드롭 대상으로 만들면 컬럼 드롭 경로가 거의 안 쓰인다.** `pointerWithin`이
    포인터 아래 모든 droppable을 돌려주고 중심이 가까운 것이 이기므로, 카드 위에 놓는
    일이 대부분이다. 그래서 컬럼·상태 칩 갈래는 **빈 자리에 놓는 경우**를 위해 남는다 —
    지우지 마세요(그 검사가 `drag`·`dragdesk`에 있다).

### 캘린더

13. **띠를 절대 배치로 날짜 위에 얹지 말 것**(핸드오프 경고). 주 단위 행 안에서 ①배경 셀 7칸
    ②날짜 숫자 줄 ③띠 레인 순서의 세로 흐름이다.
14. **띠 레인이 클릭을 먹는다** — 레인 컨테이너가 칸의 아래 절반을 덮어서 날짜가 한 번에 안
    눌렸다. 레인은 `pointer-events-none`, 띠와 `+N건`만 `pointer-events-auto`.
15. **창이 낮으면 2줄이 안 들어간다** — 주 줄 높이를 `ResizeObserver`로 재서 띠 줄 수를
    1~`CAL_LANES`(2) 사이로 정한다. 넘치는 건 그대로 `+N건`.

### 상태 · 팝오버 · 모달

16. **같은 상태로 저장하면 아무 일도 안 한다** — 완료 목록에서 완료 버튼이 죽은 것처럼 보였다.
    끝난 건은 '되돌리기'로 바꿨다(`dashboardParts.jsx`).
17. **`ProfileMenu`/팝오버 위치** — `useAnchoredPos(triggerRef, open, w, estH, gap, measuredRef)`.
    `measuredRef`를 넘기면 실제 높이로 다시 잡는다. 추정 높이만 쓰면 위로 뜨는 팝오버가
    트리거에서 100px 떠 보인다. 여는 순간 `place()`를 먼저 호출해야 첫 프레임이 `{0,0}`에
    안 그려진다.
18. **업무 창 안의 state는 카드가 바뀌어도 살아 있다.** 모달은 카드를 바꿀 때 언마운트되지
    않으므로(`openTaskModal`이 state만 바꾼다) `TaskViewer`의 요약 state가 남아 **다음 카드에 앞
    카드의 요약이 보였다.** `key={formData.id}`로 카드마다 새로 마운트한다.
19. **"고정된 값 우선"과 "방금 만든 값 우선"을 `A || B`로 쓰면 둘 중 하나가 죽는다.**
    요약이 `pinned || summary`였다. 고정된 상태에서 '다시 만들기'를 누르면 AI는 돌고 `summary`에
    새 글이 들어오는데 화면은 옛 요약 그대로였고, 새로 만든 것을 고정할 방법도 없었다.
    지금은 `summary || (revealed ? pinned : '')`이고, 버튼 줄과 배지는 **지금 보고 있는 것이
    저장된 것인지**(`showingPinned`)로 갈린다.

### 데이터 로드 · 실시간

20. **목록 화면에서 `task.comments` / `task.activityLog`는 비어 있다.** 초기 로드가 읽는 것은
    팀·프로필·프로젝트·카드·리소스·첨부까지고, 댓글과 활동은 업무 창을 열 때 그 카드 것만
    읽는다(`cloudSync.loadCardDetail`). 카드 목록에 "댓글 3"을 붙이려면 개수를 따로 세는 경로가
    필요하다 — 전체 댓글을 다시 읽으면 예전으로 되돌아간다(그래서 0016).
21. **실시간은 표(payload.table)에 따라 갈라진다**(`cloudSync.subscribeWorkspace`).
    cards는 그 카드 1건만 다시 읽고, comments·files는 열려 있는 창일 때만 상세를 갱신하고,
    나머지는 전체 재조회다. **구독에 새 표를 추가하면 기본이 전체 재조회**이니 라우팅에 같이
    적어야 한다. comments의 DELETE payload에는 `card_id`가 없다(replica identity가 PK뿐) →
    cardId가 비면 "지금 열려 있는 카드"로 본다.
21-a. **모듈 캐시로 들고 있는 표는, 그 원본 테이블을 구독하지 않으면 영영 낡는다.**
    `cloudSync.profileIdToName`(프로필 id→이름)은 `loadCloudState`에서 한 번만 만들어지는데
    `profiles`가 Realtime 발행 목록에 없었다. 그래서 **누가 새로 가입하면, 그 전에 열어 둔
    화면은 그 사람을 영영 모른다**(새로고침 전까지). 활동 기록에 '알 수 없음'이 뜬 것이
    증상이지만 라벨만의 문제가 아니다 — 같은 표를 담당자·멘션이 같이 본다:
    `assigneeNames`가 모르는 id를 걸러내 옛 `cards.assignees` 컬럼으로 폴백하고(담당자가 딴
    사람으로 보인다), 선택기·멘션 목록에서 빠지고, 그 상태로 저장하면 `assigneeIdsOf`가 그
    사람의 id를 못 찾아 **방금 지정된 담당자가 조용히 지워진다.** 0018로 `profiles`를 발행에
    넣고 구독에 추가했다(전체 재조회로 받는다 — 가입·이름 수정뿐이라 드물다).
    **캐시해 둔 조회 결과를 만들 때마다 "이 표가 바뀌면 누가 알려주나"를 같이 정하세요.**
22. **스토어 히스토리는 최근 20개까지.** `LOAD_STATE`는 기록을 초기화하고(서버가 원본이라 그
    이전으로 되돌리면 유령 데이터가 살아난다) `SYNC_TASK`는 기록하지 않는다. `SYNC_TASK`는
    카드 1건을 **병합**한다 — `UPSERT_TASK`로 바꾸면 담아둔 댓글·활동·첨부가 날아간다.
23. **`store.canUndo()`를 렌더 중에 그냥 부르면 갱신되지 않는다.** `useCanUndo()`/`useCanRedo()`로
    구독한다(예전엔 카드를 옮겨도 실행 취소 버튼이 계속 비활성이었다).
24. **`cards.position`은 아무도 채우지 않는다**(전부 0). 정렬 키로 쓰면 같은 값이라 Postgres가
    순서를 보장하지 않아 카드가 뒤바뀐다. 컬럼 안 순서는 `dashboardParts.byDue`(마감일 순)가
    소유한다 — 마감 그룹 목록과 같은 함수다.

12-c. **컬럼의 빈 자리에 놓는 것은 "이 컬럼으로"가 아니라 "맨 밑으로"다.** 같은 상태면
    그냥 돌아가게 두었더니 **맨 밑으로 옮길 방법이 없었다**(사용자 지적). 상태 칩은
    상태를 바꾸는 자리라 같은 상태면 아무 일도 하지 않는 것이 맞지만, 컬럼의 빈
    자리는 다르다. 그리고 이 검사는 **카드가 화면을 채우지 않는 시드**로만 만들 수
    있다 — 공용 시드는 컬럼마다 서른 장이 넘어 빈 자리가 아예 없어서, 처음 두 판은
    되돌려도 통과했다(`dragdesk`가 카드 셋짜리 시드를 따로 쓰는 이유다).

### 서비스 계층

28-a. **수정 폼이 들고 있는 목록으로 스토어를 덮지 마세요.** 저장이 카드를 통째로
    교체(`UPSERT_TASK`)하는데, 편집 폼(formData)은 '수정'을 누른 순간의 스냅샷이라
    그때 아직 안 왔거나 그 뒤에 도착한 댓글·활동을 모른다. 클라우드는 이 둘을 창을
    열 때 따로 읽으므로(§6-20) 폼에는 빈 배열이 실려 있기 십상이고, 그대로 저장하면
    **저장하는 순간 댓글과 활동 기록이 화면에서 사라진다**(다시 들어가면
    loadCardDetail이 읽어 와서 "나갔다 오면 보인다"가 된다 — 사용자 지적).
    특히 **기록이 새로 안 생기는 수정**(팀만 바꾸거나 바뀐 것 없이 저장)이면 붙을
    것조차 없어 활동이 통째로 0이 된다. 지금은 저장이 `comments`·`activityLog`·
    `attachments`를 payload에서 빼고 `SYNC_TASK`로 병합한다 — 이 셋은 스토어의 것이
    원본이다. §6-22의 다른 얼굴이고, `modalclose`가 되돌림까지 확인한다.

28-b. **전체 재조회(`LOAD_STATE`)는 열려 있는 업무 창의 댓글·활동을 비운다.**
    초기 로드는 그 둘을 읽지 않으므로(§6-20) 재조회하면 모든 카드가 빈 배열로
    돌아간다. 창은 열려 있고 카드 id도 그대로라 상세 효과가 다시 돌지 않아
    **빈 채로 남는다**(닫았다 열면 보인다).
    저장 직후에 특히 잘 났다 — 내 저장이 실시간 cards 이벤트로 돌아오는데, 편집
    중이라 미뤄 둔 재조회가 **편집이 끝나는 순간**(=저장하는 순간) 실행된다.
    남이 프로젝트 이름을 바꾸거나 새로 가입해도 같은 일이 난다.
    지금은 `reloadCloud`가 재조회 뒤 **열린 창의 카드만 상세를 다시 읽는다.**
    그리고 편집 중 카드 변경은 전체 재조회가 아니라 그 카드만 다시 읽는다
    (`pendingCardsRef`) — 저장 한 번에 워크스페이스를 통째로 읽을 이유가 없다.
    **§6-28-a를 고치고도 증상이 그대로였다**(원인이 둘이었다). 게스트에는 실시간이
    없어 이 경로가 아예 안 도니, 검사는 `push.mjs`의 소스 단정으로 남겼다.

29-a. **`services/cloudSync.js`에서 스토어를 import하지 마세요.** `assignees`·`push`는
    `cloud.js`만 가짜로 바꿔치고 cloudSync를 **노드에서** 돌린다. 스토어를 물면 그
    경로가 통째로 `ERR_MODULE_NOT_FOUND`로 깨진다(드라이브 폴더 id를 스토어에 바로
    넣으려다 실제로 깨뜨렸다). 화면에 반영할 값은 **부르는 쪽이** 넣거나, 실시간
    재조회에 맡기세요.

### RLS · 저장

25. **RLS가 걸린 테이블에 "남의 행"을 넣을 때 `.select()`를 붙이면 전부 롤백된다.**
    `insert().select()`는 SQL의 `INSERT ... RETURNING`이라 넣은 행을 읽으려 하는데,
    `notifications`의 SELECT 정책은 본인 수신 행만 허용한다. 그래서 42501로 insert까지 되돌아갔고
    **멘션 알림이 한 번도 생성되지 않았다.** 호출부가 실패를 조용히 삼켜서(console.error만) 화면에는
    아무 표시가 없었다. 넣기만 할 때는 `.select()`를 붙이지 않는다.
26. **담당자를 표시명으로 붙이면 이름을 바꿀 때 카드가 남의 것이 된다.** `cards.assignees`는
    표시명 `text[]`인데 프로필 이름을 바꾸는 경로는 이 배열을 건드리지 않았고, '내 업무'는
    `assignees.includes(currentUser.name)`으로 거른다. 0013의 `card_assignees(profile_id)`가
    원본이고 **표시명은 읽을 때 `profiles`에서 파생한다** — 그래서 앱 안의 `task.assignees`는
    여전히 이름 배열이고 뷰·셀렉터·활동 기록·AI 컨텍스트는 그대로다. 조인 행이 없으면
    `cards.assignees` 컬럼으로 폴백한다. 담당자 선택기는 **등록된 멤버만** 고를 수 있다 —
    목록 밖 이름은 그 사람이 업무를 볼 수도 알림을 받을 수도 없어서 배정이 아니라 메모였다.
27. **조인 테이블은 "전부 지우고 전부 넣기"로 맞추면 안 된다.** 왕복이 두 번이라 멱등이 아니다.
    저장 두 개가 겹치면 문장이 D1 → D2 → I1 → I2 순으로 도착하고, D2가 빈손으로 지나가서 I2가
    I1의 행과 부딪힌다(23505). 지금은 **집합에 없는 것만 지우고 + on conflict do nothing**이라
    순서에 상관없다(`cloud.resetCardJoin`). 새 조인 테이블을 붙이면 같은 모양을 쓰세요. 이
    판단이 0015에서 하위 업무를 조인이 아니라 `cards.subtasks` 컬럼으로 둔 이유이기도 하다 —
    컬럼 통째 쓰기는 겹쳐도 마지막 것이 남을 뿐 깨지지 않는다.
28. **"새로 생긴 것"을 나중에 되계산하지 마세요.** 업무 저장이 활동 기록을
    `task.activityLog.slice(oldData.activityLog.length)`로 잘라냈는데, `oldData`(창을 열 때의
    스냅샷)와 `newData`(스토어를 따라가는 폼)가 **다른 스냅샷**이었다. 창을 열면 상세 로드가
    스토어에만 활동을 채우므로 `oldData.activityLog`는 빈 배열이고, 그래서 `slice(0)`이 되어
    서버에 이미 있는 기록까지 다시 넣었다 → `activity_pkey` 중복 → 카드는 저장됐는데 "저장에
    실패했어요"가 떴다. 지금은 `TaskService.updateWithLogs`가 바뀐 업무와 **이번에 생긴 기록**을
    같이 돌려줘서 그 계산 자체가 없다. `insertActivity`도 `on conflict do nothing`이다.
29. **한 겹 더 벗기면 조용히 `undefined`가 되고, 그 자리가 "아무도 안 걸리는 필터"가 된다.**
    `cloudSync.myUserId()`가 `const { data } = await cloud.getSession()`으로 읽었는데,
    `cloud.getSession()`은 supabase의 `{ data }`를 **이미 벗겨서** `{ session, user }`를 돌려준다.
    그래서 언제나 null이었고 **알림의 본인 제외가 한 번도 걸리지 않았다.** 멘션은 이름으로도 한 번
    거르므로 증상이 가려졌지만, 내 댓글에 내가 답글을 달면 나에게 알림이 왔다. "본인 제외" 같은
    방어는 통과가 기본값이라 깨져도 아무 소리가 안 난다. 검사를 남기세요(`tests/push.mjs`).
30. **웹 푸시의 상태 원본은 DB 행이 아니라 브라우저의 `PushSubscription`이다.** 기기에서 권한을
    껐거나 브라우저 데이터를 지우면 DB 행은 남아 있어도 알림이 오지 않는다. 화면의 켜짐/꺼짐은
    `pushManager.getSubscription()`으로 판정하고, 죽은 행은 발송에서 410/404를 받은 서버가 지운다.
    그리고 **iOS는 홈 화면에 추가(PWA)한 뒤에만** 동작한다 — 브라우저 탭에서는
    `requestPermission()` 창 자체가 뜨지 않으므로 묻기 전에 안내를 보여야 한다. iOS는 탭과 홈
    화면 앱을 **별개 저장소로 취급**한다(한쪽에서 켜도 다른 쪽은 꺼진 채로 보인다).
31. **화면에 말을 거는 문장은 화면 필터를 따라가면 안 된다.** 대시보드 인사말이 세그먼트
    (전체/내 팀/내 업무)를 따라가서, 남의 지연 한 건이 "OOO님, 밀린 업무부터 정리해봐요"로 떴다.
    인사말은 `utils.myScope`로 내 것만 센다. KPI·목록은 필터를 따라간다.

### 테스트 스크립트 작성 시

32. 페이지에 주입하는 문자열은 JS 템플릿 리터럴이라 `\d`가 `d`로 죽는다 → `[0-9]`.
33. Chrome은 `linear-gradient` 직렬화에서 `180deg`를 생략한다 → 가로 판정은 `to right`/`90deg`로.
34. `div.hidden.md:block`은 유효한 셀렉터가 아니다(`md\\:block`로 이스케이프).
35. 배지 껍데기와 안쪽 텍스트가 `textContent`가 같다 → `children.length === 0`으로 잎 노드만.
36. `getBoundingClientRect()`는 조상 `overflow`에 잘려 안 보이는 요소도 좌표를 돌려준다 →
    보이는지 판정할 때는 조상 clip을 같이 계산해야 한다(`tests/bottomgap.mjs`의 `visibleBottom`).
37. **`el.click()`은 `mousedown`을 내보내지 않는다.** 바깥 클릭으로 닫히는 팝오버는 실제 마우스
    이벤트(`Input.dispatchMouseEvent`)로 눌러야 재현된다.
38. **좌표를 미리 재두고 나중에 쓰면 엉뚱한 것을 누른다.** 참고 링크가 하나 붙으면
    `+ 참고 링크` 버튼이 오른쪽으로 밀리는데, 옛 좌표를 다시 눌러 방금 만든 외부 링크로 이동해
    테스트가 멈췄다. 누를 때마다 다시 찾으세요.
39. `Emulation.setTouchEmulationEnabled`의 `maxTouchPoints`는 1~16이다. 0을 넘기면 CDP가 던진다.
40. **셀렉터를 못 찾을 때 던지지 말고 FAIL로 남기세요.** 던지면 러너가 `CRASH`로만 찍어서
    어느 단정에서 어긋났는지 안 보인다.
41. 리로드 없이 폭만 바꾸면(`setDeviceMetricsOverride`) 방금 넣은 데이터를 그대로 두고 반응형을
    볼 수 있다. `load()`는 localStorage를 seed로 되돌린다.
42. **시드에 날짜를 박아 두면 달이 바뀌는 날 검사가 스스로 무너진다.** `batch10`의 캘린더 띠가
    `2026-07-12~25`였고 캘린더는 늘 **이번 달**을 펼치므로, 8월이 되자 띠가 화면에 없어서 팀 분할
    검사 네 개가 한꺼번에 FAIL이 됐다(앱은 멀쩡했다). 지금은 실행하는 달을 기준으로 만든다
    (`thisMonth(12)`·`nextMonth(5)`). 날짜가 필요하면 **오늘로부터 며칠**로 쓰세요. 이런 실패는
    코드를 안 건드려도 생기니, FAIL을 보면 **먼저 `git stash`로 main에서도 실패하는지** 보세요.

---

## 7. "다시 제안하지 말 것" (사용자가 판단해서 뺀 것)

| 항목 | 이유 |
|---|---|
| 주간 브리핑(AI) | 품질 검증을 먼저 하기로. 팀 단위 주 1회면 월 28회라 비용은 문제가 아닙니다 |
| 순(조직) 정보 | 나중에 명단을 줍니다. 대시보드는 팀 단위 기획을 유지하고, 순은 화면 축이 아니라 사람에 붙는 속성으로만 둡니다(순별 보드를 만들지 않습니다) |
| 카카오 알림톡·친구톡 | 비즈니스 채널 개설 + 사업자 정보 + **템플릿 사전 심사** + 발송 대행사 계약이 필요하고, 심사 문구에 갇혀서 §8의 문구 톤을 못 씁니다. 카카오 로그인의 '나에게 보내기'는 본인에게만 보낼 수 있어 팀 알림에 못 씁니다. 웹 푸시로 해결했습니다 |
| 반복 업무 | 지금은 아니라고 정했습니다 |
| 로티·외부 JSON | 빈 상태는 SVG 선 그리기(`dc-draw`)로 갑니다. lottie-web은 gzip 약 70KB인데 쓰이는 곳이 "할 일 없을 때만 보이는 화면"입니다 |
| 마감일 필수화 | 강제하면 아무 날짜나 넣어서 '지연' 숫자가 거짓이 됩니다. 대신 마감 미정 구간 + 2주 방치 표시로 해결했고, 안내 문구는 "마감일을 정하면 캘린더에서 볼 수 있어요" |
| 공유·삭제를 모바일 상단바로 | 아이콘이 일곱 개가 되어 번잡합니다. 자리는 §4.6대로 링크 줄 밖입니다 |
| 랭킹·점수·배지·스트릭 | 참여 유도의 흔한 수단인데 §8의 "누가 누구와 견주는 구조를 만들지 마세요"와 정면으로 부딪힙니다. 밀린 사람이 화면에서 지목됩니다. 스트릭은 특히 위험합니다 — 한 주 못 오면 벌 받는 느낌이 남습니다 |
| 최근 활동을 '내 업무만' 보기 | 이 칸의 값은 **남들이 움직이는 게 보이는 것**입니다. 내 것만 남기면 참여를 부르는 자리가 내 메아리 방이 됩니다. 길이 문제는 카드별 묶기 + 다섯 줄 상한으로 해결했습니다(§4.9) |
| 카드별 조회 추적 | "누가 무엇을 봤는지"가 남으면 같이 사역하는 사람들이 쓰는 화면에서 감시처럼 읽힙니다. 접속 시각 한 칸(`last_seen_at`)이 정직한 중간입니다(§4.8) |
| 미가입자 명단으로 생일 관리 | `profiles.id`가 `auth.users`를 참조해서 넣을 행이 없습니다. 이름으로 사람을 매다는 방식은 §6-26에서 이미 깨졌습니다. 그건 §7의 순 명단과 한 덩이로 판단할 일입니다(§4.8) |
| 커밋 날짜를 고쳐 잔디 채우기 | 어시스턴트가 거절했습니다. 없던 날에 작업이 있었던 것처럼 공개 레포 기록을 만드는 일이라, 방법 안내도 하지 않습니다 |
| 첨부를 **진짜로** 잠그기(비공개 유지) | 엑셀 비밀번호를 정할 때 두 갈래를 놓고 **화면 가림**을 골랐습니다. 진짜로 막으려면 그 파일이 공개가 아니어야 하는데, 엑셀 펼쳐보기는 뷰어가 공개로 닿는 주소를 받아야 동작합니다 — **진짜 잠금과 펼쳐보기는 동시에 가질 수 없습니다.** 되돌리려면 썸네일도 같이 포기해야 합니다(0023 주석) |
| 본문 이미지·프로필 사진을 드라이브로 | 옮기는 것은 **업무 첨부뿐**입니다. 이 둘은 올릴 때 이미 줄여 저장하고(본문 1600px·아바타 256px) 한 장에 수백KB라 안 쌓입니다. 무엇보다 본문 이미지는 **주소가 글 안에 박혀** 저장되어서, 주소 체계를 바꾸면 지난 글의 이미지가 구글 사정에 한꺼번에 끌려갑니다 |
| 드라이브 파일명을 `업무_파일명`으로 | 처음엔 이름에 접두사를 붙이려 했는데, 사용자가 **업무별 폴더**로 바꿨습니다. 이름을 건드리지 않아서 앱 화면의 파일명이 지저분해지지 않고, 폴더는 이름이 바뀌어도 id로 따라갑니다(0026) |
| 첨부에 편집 권한 주기 | 드라이브 파일은 `링크를 아는 사람은 **보기**`입니다. 편집으로 올리면 **링크를 아는 누구나 고칠 수 있습니다**(드라이브는 워크스페이스 멤버인지 모릅니다). 같이 채우는 문서가 필요하면 첨부가 아니라 **참고 링크**로 다는 쪽이 맞습니다 |

---

## 8. 관례

- 커밋 메시지는 한국어, 제목 한 줄 + 본문에 **왜**를 씁니다. `Co-Authored-By: Claude` 라인은
  넣지 않습니다.
- **문구 톤**: 담백하고 상태를 그대로 말합니다. 사용자가 고친 실제 예 —
  `여기는 다 정리됐어요` → `다 정리되었어요`,
  `지난 마감 없이 잘 굴러가고 있어요` → `지연된 업무가 없네요 :)`,
  `오늘 마감만 남았어요` → `오늘 마감되는 업무만 남았어요`,
  `마감 없음` → `마감 미정`, `+ 링크` → `+ 참고 링크`,
  `이날은 잡힌 일이 없어요` → `해당 날짜에는 업무가 없어요`,
  `단계를 입력하고 Enter` → `예: 포스터 시안 만들기`(방법보다 "여기에 무엇을 적는 칸인지"가 먼저).
  **"없어요"로 끝나는 짧은 부정 표현과 번역투를 특히 싫어합니다.**
  **누가 누구와 견주는 구조를 만들지 마세요.** `내가 맡은 업무에는 지연이 없네요`를
  `잘하고 있어요!`로 고쳤습니다 — "내 것에는 없다"는 곧 "남의 것에는 있다"로 읽히고,
  이 화면은 같이 사역하는 사람들이 봅니다.
  사람별 집계 섹션 이름은 **`청년별 남은 업무`** 입니다(`사역의 무게` → `팀원별` → `청년별`로
  두 번 고쳤습니다). `부하`·`과부하`·`병목` 같은 판정어는 쓰지 않습니다.
  `지난 7일 간 N건 끝냈어요`(`지난 7일에`가 아님).
  **AI가 만든 문장에는 엠 대시(—)·엔 대시(–)를 금지합니다**(`ai.js`의 `DASH_RULE`) — 요약 라벨
  구분자도 일반 하이픈입니다. 사람이 쓰는 문서·주석에서는 씁니다(이 문서가 그렇습니다).
  화면에 나가는 문구에는 지금 하나도 없습니다.
- **용어**: UI에서 "작업"이 아니라 **"업무"**. 업무 안의 단계는 **"하위 업무"**.
- **기능을 숨기지 않습니다.** 정리하려고 '⋯' 메뉴에 넣었더니 기존에 쓰던 공유를 못 찾았습니다.
  hover로만 나타나는 조작도 같은 이유로 피합니다 — 터치 기기에는 hover가 없어서 그 기능이 아예
  없는 것처럼 보입니다(요약 버튼·하위 업무 삭제·댓글 수정·알림 지우기에서 실제로 그랬습니다).
  **가로 스크롤도 숨기는 방법이 될 수 있습니다** — 같은 종류가 이어지는 줄(프로젝트 탭, 상태 칩,
  참고 링크)에서는 괜찮지만, 끝에 다른 성격의 버튼이 있으면 그건 숨긴 것입니다(§4.6).
- **버튼 배치·색**: 대화창·팝오버는 `취소 왼쪽 / 확정 오른쪽`(웹 관행이고, 삭제 같은 확정을
  손가락이 놓이는 자리에서 떨어뜨려 두는 안전 장치입니다). 상시 도구 줄은 반대로
  `확정 왼쪽 / 나가기 오른쪽`이고 두 모드에서 자리가 같습니다 — 업무 창 푸터(수정·저장 / 닫기),
  요약의 고치기·저장이 그렇습니다. 저장이 오른쪽이고 수정이 왼쪽이면 저장한 순간 손가락 밑의
  버튼이 다른 뜻이 됩니다.
  색은 구조색이 하나뿐이므로 행동에만 씁니다: 진한 accent = 확정(저장·새 업무),
  연한 accent = 편집 진입(수정), 무채색 = 아무 일도 안 함(닫기·취소).
  **Tailwind 기본 팔레트(`red-500` 등)를 쓰지 마세요** — 테마를 따라가지 않아 다크 모드에서
  그대로 튑니다. 토큰(`tag-red-fg` 등)만 씁니다.
- **삭제 확인은 `ConfirmPopover`로 통일**합니다(프로젝트·업무·댓글·첨부·하위 업무). 알림 지우기는
  예외입니다 — 잃는 것이 한 줄뿐이고 목록을 정리하는 일에 확인이 붙으면 누르는 횟수가 두 배가
  됩니다. 대신 실패하면 화면에 되돌려 놓습니다.
- 빈 화면에는 로티 대신 **SVG 선 그리기 마크**를 씁니다(외부 JSON·라이브러리 없음):
  `AllClearMark`(체크), `EmptyColumnMark`(카드 한 장). 빈 상태 내용은 남는 공간의 세로 가운데에.
- **여백·정렬을 매우 중시합니다.** 데스크톱과 모바일(375px) 완전 동작이 필수입니다.
  "핵심"이라는 단어를 쓰지 않습니다.
- **작업 방식**: 큰 목록을 받으면 항목마다 확인을 구하지 말고 배치로 진행하고 배치마다 `main`에
  푸시합니다. 마이그레이션은 dry-run을 보여준 뒤 바로 적용해도 됩니다(허락을 이미 받았습니다).
  **다만 시각 판단(레이아웃·문구 톤)은 물어보는 쪽이 낫습니다** — 목업을 만들어 보여주면
  훨씬 빠르게 정해집니다.
