# v2 설계 — 예배 · 순 · 말씀 · 동아리

2026-09-01 그릴링(요구사항 인터뷰)으로 사용자와 함께 확정한 설계입니다.
v2 회차 내내 **이 문서가 기준**입니다. 스펙 논쟁이 생기면 여기로 돌아옵니다.
구현 흐름은 HANDOFF §2(저장 자리 → 마이그레이션 → 코드 → 검사 → 문서)를 그대로 밟습니다.

## 0. 한 줄 요약

지금까지는 스태프 15명의 업무 도구였고, v2부터는 **청년 ~50명의 매일**(QT · 예배 ·
순 · 동아리)을 담는다. 기존 워크스페이스는 안쪽 구조 그대로 '업무' 영역으로 들어간다.

## 1. 확정된 결정 (그릴링 순서대로)

| # | 결정 | 내용 |
|---|---|---|
| 1 | 명단·계정 분리 | 청년 전체를 **명단(roster)** 으로 등록(계정 불필요). 가입한 사람은 자기 명단 항목에 연결. 출석은 명단 기준, QT·예배 노트·성경 읽기는 가입자만. 나중에 가입해도 지난 출석이 그대로 붙는다. §7의 '미가입자 명단' 보류를 이 회차에 푼다 |
| 2 | 조직 모델 | 사역 팀은 config 상수 그대로(워크스페이스 축 무변경). **순·동아리는 한 벌의 '모임' 모델**(type 구분). 순은 연도별 편성 버전을 남긴다(개편해도 지난 출석은 그 시절 순 기준) |
| 3 | 동아리 범위 | 명단·리더 관리 + 모임 일정·출석 + 가입 신청·모집. 모집·행사 준비는 기존 프로젝트/업무와 연결 |
| 4 | 주보 구성 | 말씀(제목·본문 구절·설교자) + 예배 담당자(처음 이름은 '임사자' — §5) + 찬양 목록 + 광고 목록. 본문 텍스트는 구절만 적으면 성경 데이터에서 자동으로 붙는다 |
| 5 | 주보 흐름 | '작성 중' 상태로 채우다가 **발행**하면 전체 공개 |
| 6 | 출석 체크 | 순장은 자기 순, 임원·교역자는 전체. 새신자는 '미등록 출석자 추가'로 그 자리에서 명단에 올린다. QR 자가 체크인은 v2 첫 회차에서 제외 |
| 7 | 예배 노트 | 가입자 누구나 예배마다 개인 노트. 기본 본인만, 원하면 '내 순에 공유' |
| 8 | 성경 데이터 | **개역한글판**(저작권 만료) 전체를 정적 데이터로 포함. 별도 청크로 분리해 첫 번들에 싣지 않는다. 주보 본문·QT·성경 읽기가 같은 데이터를 쓴다 |
| 9 | QT 일정 | ~~마스터가 본문표를 월 단위로 붙여넣기~~ → **바뀜(§5)**: 사용자가 성서유니온 매일성경 읽기표 이미지를 주면 어시스턴트가 옮겨 적어 `qt_schedule`에 시드한다(0038 — 2026·2027 730일). 해설·묵상 콘텐츠는 성서유니온 저작물이라 싣지 않는다(구절 일정만) |
| 10 | QT 잔디 | **개인 전용** 기록 달력. '이번 달 N번' 수준의 담백한 집계만. 'N일 연속' 스트릭·배지·남과 비교 없음. 순장도 순원의 잔디를 못 본다 |
| 11 | QT 나눔 | 묵상은 기본 비공개, '더다붓에 공유하기'(처음 이름은 '나누기' — §5)를 켜면 그 날의 나눔 피드에 올라가 공동체 전체가 본다 |
| 12 | 성경 읽기 | 리더(책·장 탐색, 본문 검색, 글자 크기) + 북마크 + 이어읽기. 통독 플랜·읽음 체크는 첫 회차에서 제외 |
| 13 | 권한 모델 | 직분은 명단 속성, 마스터/관리자는 앱 운영 권한으로 그대로. **새 관리 지점이 생기면 "누가 관리하나"를 사용자에게 먼저 묻고 그 사람에게만 준다** |
| 14 | 예배 종류 | 주일 4부 젊은이 예배(화면 표기 — §5)가 기본(매주 · 주보·출석 대상). 금요 열정·성탄절 같은 이벤트성 예배는 **필요할 때 한 건씩 따로** 만든다(정기 흐름은 주일 4부 하나) |
| 15 | 접근 범위 | 스태프/일반 구분 없음. **승인된 멤버는 전부 다 본다**(워크스페이스 포함). 등급은 지금처럼 마스터/관리자/멤버뿐 |
| 16 | IA | **A안 + 업무 모드 전환** — 목업 https://claude.ai/code/artifact/feb9f1a4-c011-41b0-aa12-47ffcf92caf9 참조. §3 |

### 관리 권한 표 (결정 13의 현재 답)

| 지점 | 누가 |
|---|---|
| 주보 작성·발행 | **회장 + 교역자 + 마스터** |
| 예배 출석 체크 | 순장(자기 순) + 임원·교역자(전체) + 마스터 |
| QT 본문 일정 입력 | ~~마스터만~~ → 화면 도구 없음. 어시스턴트가 시드한다(0038 · §5) |
| 명단 관리(추가·수정·직분·계정 연결) | 마스터 + 관리자 |
| 순 편성(만들기·순장 지정·연도 개편) | 마스터 + 교역자 + **리더순장** |
| 동아리 개설·리더 지정 | **마스터만** |
| 동아리 가입 신청 수락 | 그 동아리 리더 |

직분 구조: 권한에 쓰이는 것만 구조화한다 — **교역자**(명단 속성, 연도 무관) ·
**회장 / 리더순장**(연도별 — 임원진이 해마다 바뀐다). 순장·동아리장은 모임의
리더 지정에서 파생(별도 권한 테이블 없음). 총무·회계·팀장 같은 나머지 직함은
지금처럼 `role_note` 자유 텍스트(AI 호칭·표시용)로 남는다.
기존 가입자 15명의 직분은 그대로 승계하고, 미가입자 명단은 회차 1 전에 사용자가 준다.

### 지키는 원칙 (기존 §7·§8에서 살아 있는 것)

- **랭킹·점수·배지·스트릭 금지**, 누가 누구와 견주는 구조 금지. 예시 앱의 '리더보드'는
  들여오지 않는다. QT 잔디가 허용되는 건 "나만 보는 나의 기록"이라서다.
- 순장에게 순원의 QT 현황·노트(공유 안 한 것)를 보여주지 않는다 — '안 한 사람이
  지목되는' 구조가 되기 때문(§7 카드 조회 추적을 뺀 같은 이유).
- 안내·힌트 문구는 먼저 물어보고 붙인다. 문구 톤은 §8 그대로.
- §7의 "순은 화면 축이 아니라 속성" 항목은 **이 문서로 대체**된다(사용자가 v2에서
  직접 다시 꺼냈다). 단 "순별 업무 보드를 만들지 않는다"는 그대로다 — 순은 목양 축이지
  업무 축이 아니다.

## 2. 데이터 (저장 자리 초안 — 회차 1에서 확정)

HANDOFF §2-1 기준: 카드처럼 언제나 같이 읽고 쓰는 것은 jsonb 컬럼, 따로 조회·집계할
것만 조인 테이블.

```
people            명단. id · name · birthday? · profile_id(→profiles, null 허용) ·
                  is_pastor · note · removed_at
people_roles      연도별 직분. person_id · year · role('회장'|'리더순장')
groups            모임. id · type('sun'|'club') · name · year(순만) · leader_person_id
group_members     group_id · person_id (순은 편성, 동아리는 가입)
club_applications group_id · person_id · status(대기/수락/거절)
group_meetings    동아리·순모임 일정. group_id · date · title? · attendance jsonb
services          예배. id · kind('sunday'|자유 이름) · date · status(draft|published) ·
                  title · passage_ref · preacher · roles jsonb(담당자) ·
                  songs jsonb(찬양) · notices jsonb(광고) · attendance_note
attendance        service_id · person_id · checked_by · created_at  (있으면 출석)
service_notes     예배 노트. service_id · profile_id · body · shared_to_sun
qt_schedule       date(pk) · passage_ref · label?   (시드 마이그레이션으로 채운다 — 0038)
qt_entries        date · profile_id · body · shared · created_at
bible_state       profile_id(pk) · last_ref · bookmarks jsonb
성경 본문          DB 아님 — public/bible/*.json (개역한글, 책 단위 청크, lazy load)
```

- RLS: 읽기는 승인 멤버 전체(예외 — `qt_entries`·`service_notes`는 본인 + 공유 범위).
  쓰기는 위 권한 표대로, 화면만이 아니라 **DB에서도** 막는다(0029 전례).
- 출석의 사람 축은 profile이 아니라 **person**이다. 가입 시 profile↔person을 잇는다.

## 3. 화면 (IA — 목업으로 확정)

### 모바일 하단 바: 홈 · 예배 · 말씀 · 모임 · 업무

- **홈** — 교회 생활 요약: 오늘의 QT · 이번 주 예배 · 내 업무 N건 · 내 순 소식 · 생일
- **예배** — 주보 목록(종류·연도 필터) → 상세(말씀 / 임사자 / 찬양 / 광고) ·
  권한자에게 작성·발행 · 출석 체크 · 내 예배 노트
- **말씀** — QT(기본 탭: 오늘 본문 → 묵상 기록 → 나눔 피드 · 내 잔디) |
  성경 읽기(리더 · 북마크 · 이어읽기)
- **모임** — 내 순(명단 · 출석 현황 · 공유된 예배 노트) · 동아리(목록 · 가입 신청 · 모임)
- **업무** — 누르면 **하단 바가 지금의 네 칸(프로젝트 · 내 업무 · 대시보드 · 팀)으로
  바뀐다**(모드 전환 — 기존 손 습관 보존, 겹 수 안 늘림). 맨 왼쪽 '홈으로'로 복귀.
  안쪽 화면은 지금 그대로.

### 데스크톱 상단 1줄

`홈 · 예배 · 말씀 · 모임 | 업무 대시보드 · 내 업무 · 일정` + 검색·종·설정.
2줄째 프로젝트 탭 줄은 워크 문맥에서 지금처럼. 모드 전환 없음(폭이 넉넉하다).

### 만들 때 주의

- 새 전역 화면은 `GLOBAL_MENUS`에 등록(안 하면 프로젝트 id로 오해 — §3),
  안에서 스크롤하면 `needsFullHeight`도.
- (끝남) 회차 2의 임시 진입로(전역 메뉴)는 회차 3에서 하단 바 A안으로 재편됐다 — §5.

## 4. 회차 (확정)

| 회차 | 내용 | 방식 |
|---|---|---|
| 1 · 기반 | 명단·모임·직분·계정연결 스키마 + 마이그레이션 + 명단 시드(사용자 제공) + 명단 관리 화면(멤버 화면 확장) + 개역한글 데이터 파이프라인 | 순차 (전부의 전제) |
| 2 · 세 줄기 | 예배 줄기(주보·출석·노트) ∥ 말씀 줄기(성경 리더·QT) ∥ 모임 줄기(순·동아리) | **Opus 5 에이전트 3개 병렬**, 완결된 세로 조각 |
| 3 · IA 재편 | 하단 바 5칸 + 업무 모드 전환 + 홈 화면 + 데스크톱 상단 | 순차 |
| 4 · 통합 검증 | 전체 스위트 + 새 검사 + HANDOFF·README·§7 갱신 | Fable 5 (분석·검증 통합) |

- 역할: **설계·검증 = Fable 5, 구현 = Opus 5** (excel-fidelity-loop의 일반화 —
  Fable 분석 → Opus 구현 → Fable 재검 → 푸시).
- **푸시 전에 사용자 확인**(사용자 지시 2026-09-01): 회차 작업이 끝나면 바로 푸시하지
  않고 로컬 dev 서버를 띄워 사용자가 먼저 본다. 확인 후에 푸시(=Vercel 배포).
  클라우드 경로 확인 부탁(§2-6)은 그대로.
- 새 로직에는 검사를 하나 남기고 일부러 되돌려 실패를 확인(§2-5). 새 스위트 후보:
  성경 데이터 정합(장·절 수), QT 본문표 파싱, 출석 집계, 주보 렌더, 내비 스모크 확장.

## 5. 진행 기록 (2026-09-02 기준 — compact 후 여기부터 읽으면 된다)

### 어디까지 왔나

**회차 1~4까지 끝났고, 전부 로컬 커밋만 되어 있다(푸시 보류 — 사용자가 dev 서버로 확인 후
푸시하기로 함).** 남은 것은 회차 4의 2번(사용자 확인 → 푸시 → 클라우드 확인)뿐이다.

- 마이그레이션 **0035~0038 라이브 적용 완료**: 명단 53명(순 6·동아리 5·직분) ·
  예배/출석/노트 · QT/성경 상태 · **QT 읽기표 2026·2027 730일 시드**(사용자가 준
  이미지를 옮겨 적음 — 새해 표는 이미지를 받으면 같은 방식) · groups.position ·
  bible_state.highlights.
- 성경: 개역한글 정적 데이터 `public/bible/`(66권·31,103절, scripts/bible_check.mjs).
- 화면: 예배(주보 작성→발행·본문 선택 피커·담당자 자동완성·자동 저장·출석 체크) ·
  말씀(QT·묵상 MarkdownEditor·나눔·개인 잔디·성경 리더·북마크·**절 형광펜**+모아보기) ·
  모임(내 순·동아리 dnd 순서·가입 신청·모임·순 편성) · 명단 관리(멤버 화면 [계정|명단] 탭) ·
  **홈**(오늘의 QT·이번 주 예배·내 업무·내 순) · **모바일 하단 바 A안**(교회 바 ↔ 업무 바
  모드 전환, 첫 화면 홈) · 데스크톱 상단 [홈·예배·말씀·모임 | 업무 대시보드·내 업무·일정],
  교회 화면에서 프로젝트 탭 줄 접힘.
- 검증: 새 스위트 5종(worship 112 · word 111 · groups 132 · roster 66 · home 37) +
  갱신(navsmoke 39 · dashfix 45) 전부 초록, 빌드 통과. §2-5 일부러 깨뜨린 확인 다수.
- 탭 줄 관련 소소한 수정 5건은 **이미 main에 푸시·배포됨**(+ 프로젝트 오른쪽 끝,
  간격, 더보기는 고른 해만 등). 로컬 미푸시 커밋: 9b25b0a·814afd6·dbe8f80·bdcd32a·
  7d95f78·7397ccc·1acf4b2·46921bb + 회차 4 커밋 (+ 탭 수정들은 로컬·원격 양쪽에 있어 최종
  푸시 때 `git pull --rebase`로 접힌다).

### 그릴링 이후 사용자 피드백으로 **바뀐 결정** (§1 표보다 이것이 우선)

- 예배 종류 표기: **'주일 4부 젊은이 예배'**(화면 — AI 배경 지식은 아직 '청년 예배', 통일 여부 대기)
- QT 일정: 마스터 붙여넣기 도구 **삭제** — 어시스턴트가 읽기표 이미지를 받아 시드(0038)
- 세그먼트 명칭 '매일성경' → **'QT'** · '나누기' → **'더다붓에 공유하기'**
- 출석 체크는 **발행된 주보 + 예배일(KST) 이후**에만
- 임사자 → **담당자** · 저장 라벨 **'임시 저장되었어요'**(초록 칩, 발행본 편집은 '저장되었어요')
- 데스크톱 더보기는 **고른 해의 것만**(보관 포함 — 2026-08-24 '모든 해' 결정 대체)
- '전체 대시보드' → **'업무 대시보드'** · 첫 화면은 **홈** · 성경 읽기에 **형광펜**(사용자별) 추가
- 사람 목록은 **리더 먼저 + 가나다순** · 빈 상태는 **마크와 함께 정가운데**
- §6 함정 후보(회차 4에서 HANDOFF에 등재): tw `duration-150`은 transition-duration까지
  정하고 transition-property 초깃값이 all이라 **팝오버 위치(top)가 전이되어 미끄러진다** —
  팝오버에는 transition-none 또는 place() 먼저.

### 사용자 결정 대기

① AI 배경 지식의 예배 명칭 통일('젊은이') ② 형광펜 색 추가·QT 본문 형광펜
③ 내 순 출석: 최근 1건 요약 유지 vs 주차별 이력 ④ 나눔 빈 자리 마크·홈 생일 카드
⑤ 찬양 구성(생각 중) ⑥ 새 문구 톤 전반(리뷰 중 지적으로 갈음)

### 회차 4 (2026-09-02)

1. ~~문서 정합~~ **끝** — HANDOFF(처음이라면 5번 · §1.1 클라우드 확인 목록 · §1.2 · §3 파일 지도 ·
   §5 마이그레이션 0035~0038 · §6-17-b 팝오버 전이 함정 · §7 순·미가입자 명단 항목 대체 명시) ·
   README(기능·구조·마이그레이션) · tests/README.md(새 스위트 6종) · tests/run.mjs ORDER(bibleref는
   NODE_ONLY) · 이 문서 §0~3의 낡은 문구.
2. **남음**: 사용자 dev 확인(`npx vite --port 5173`) → `git pull --rebase` → 푸시(=배포) → 클라우드
   경로 확인(HANDOFF §1.1의 2026-09-02 회차분). `npm run verify` 전체는 회차 4 끝에 한 번 돌렸고 **32종 전부
   초록** — 회차 3의 '첫 화면=홈' 때문에 `/`로 들어가던 업무 스위트 4종(handoff·batch10·batch11·wide)을
   `/?p=dashboard`로, calfit은 고정 날짜 시드를 오늘로 고쳤다. 그 과정에서 실제 버그 1건: 대시보드 필터가
   `?f=`만 적혀 새로고침하면 홈으로 열렸다 → `p=dashboard`를 같이 적는다(App.jsx).
3. ~~통합 잔손~~ **끝** — 주보 구절 → 성경 읽기(App.jsx `openBible` → WordView `initialRef`, 말씀
   화면을 떠나면 비운다 · worship 스위트에 검사 1건) · groupsParts와 roster.jsx의 버튼·입력칸 클래스
   한 벌(roster 글자 11 → 11.5px) · 게스트 저장 헬퍼는 `people.guestStore` 한 벌. **키 3벌은 합치지
   않았다** — 시드의 `me`(자격) 모양이 서비스마다 달라 한 키에 두면 서로 덮어쓴다. 게스트에서 한
   명단을 셋이 같이 봐야 할 때 합친다.

## 6. 명단 초기 데이터

- 기존 가입자 15명: 지금 직분(role_note·팀) 그대로 승계.
- 미가입 청년(~34명): **회차 1 전에 사용자가 제공**(이름·순·직분). 제공 즉시 시드
  마이그레이션에 넣는다.
- 순 편성 현황(다른 서비스 기준): 순 6개 — 꼬순(김윤주) · 콩순(천진영) · 고구마순(김승찬) ·
  하이순(배현민) · 선착순(임재훈) · TT순(노준석), 총 ~49명. 정확한 명단은 사용자 제공분이 기준.
