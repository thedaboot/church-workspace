# tests

> **도는 동안 레포 파일을 고치지 마라.** dev 서버가 vite여서 **아무 파일이 바뀌면**
> 열려 있는 페이지를 새로고침한다(`HANDOFF.md`도 그렇다) — 좌표를 재던 스위트가
> 발밑을 잃고 `CRASH`나 엉뚱한 FAIL로 나온다. 깨졌으면 **손대지 않고 그 스위트만
> 혼자 다시** 돌려 보는 것이 첫 수순이다.

브라우저를 실제로 띄워 화면을 재는 검증 스위트. 단위 테스트 프레임워크는 쓰지 않는다 —
각 파일이 헤드리스 Chrome을 CDP로 붙잡고, 화면을 만들고, 좌표·계산값·localStorage를 재서
`PASS` / `FAIL` 줄을 찍고 종료 코드로 결과를 알린다.

```bash
npm run verify -- calfit drag  # **골라서 — 고친 것과 관련된 것만. 이게 기본이다**
npm run verify                 # 전부(약 7분). **사용자가 부를 때만** — 푸시 직전도 아니다
npm run verify -- --jobs 3     # 동시 실행 (스크립트마다 CDP 포트가 달라 충돌 없음)
SHOTS=1 npm run verify         # 스크린샷도 저장
CHROME=/path/to/chrome npm run verify   # 크롬 경로가 다를 때
```

- 러너가 `vite --mode guest`를 띄운다. `.env.guest`가 없으면 만든다(빈 Supabase 키 = 로그인
  없이 localStorage 모드). 이 파일은 커밋하지 않는다.
- 각 스위트는 `node tests/<name>.mjs <베이스 URL>` 로 혼자서도 돌아간다. 게스트 서버를 직접
  띄우려면 `npx vite --mode guest --port 4598 --strictPort`. 아래 표에 **(노드)** 라고 적힌
  것은 브라우저도 서버도 필요 없다(`run.mjs`의 `NODE_ONLY`).
- **스위트는 33종**이다(`tests/run.mjs`의 `ORDER`가 목록이고 디스크와 같아야 한다 —
  `run.mjs`·`zip.mjs`·`seed.js`는 스위트가 아니라 러너와 공용 도구다). 새 스위트를 만들면 `ORDER`에
  넣고 이 표에도 한 줄 남긴다.
- **여러 러너를 동시에 띄우지 마라.** 한 러너 안에서는 스크립트마다 CDP 포트가 갈라져 있지만,
  러너를 둘 띄우면 그 보호가 없어 서로의 크롬에 붙어 `CRASH`·타임아웃이 난다(`docs/PITFALLS.md` §6-42-a).

## 스위트

| 파일 | 보는 것 |
|---|---|
| `logcheck` (노드) | `utils.js`·서비스 계층의 **순수 로직 전부** — 활동 기록 · 하위 업무 진척 · 정렬·순서 규칙 · 지금 보고 있는 사람 · 다녀간 시각·심장박동 · 캐시 범위(15초 재조회 생략·날짜 열쇠 정리) · v2 실시간 라우팅(홈 접두 넷) · 카드 실시간 모으기·활동 앞에 얹기 · 홈·모임 주보 가볍게 읽기 · 호칭(형제/자매·0064) · 홈이 세우는 주보와 그 머리 글자 · 성경 최근 검색어(0065) · 노트 초안 열쇠 · 첨부 발췌 글 판정(`textQuality`)·HTML · 무거운 부품은 열 때만 · 가이드 굽기 열쇠 · 회의록 날짜 반년 경계(로컬) · 첨부 이름 NFC · 호버 공장·`rowDelay` 배선 · pdf.js 6.3 옛 브라우저 폴리필 · 마이그레이션 모양(9차 셋·0071 칸 가드·0073·0074) · 성경 색인(편집 표기 36절 빼기 · 단위 벡터) · 문서 임베딩(조각·해시·글 모양·증분 계획) · AI 사람 줄(`aiPeople` 직함 고르기·글에 나온 가입자) · 주보 편집 줄 열쇠(`stableRowKeys`) · 담당 업무 도막 value 왕복 · `mergeTaskEdit` · 서식 안 멘션 · 뜻 검색 결과를 줄로(`vecSearch` — 와/과 · 조각 발췌 · 업무 묶기 · 성경 대체 줄 · 게스트 네트워크 0) · 탭 줄 앞 칸(`tabRank`)·업무 채널 재접속 판정(`realtimeStatus`) · 상시(0075)와 업무 셈 한 벌(`taskCounts` — 지연·구간·2주 방치·진척·고른 해 · 상시로 바꾸면 날짜를 지운다) · **소스의 모든 Enter 핸들러에 한글 조합 가드**가 있는지. 새 순수 함수는 기본적으로 여기에 붙는다 |
| `mdcheck` (노드) | 마크다운 ↔ 에디터 문서 모델 왕복(`services/markdown.js`) — 서브셋 밖 문법이 조용히 사라지지 않는지 · 빈 줄·들여쓰기 보존 · 줄 구조 왕복 안정(블록 안 줄바꿈·중첩 목록·번호) · 문법처럼 생긴 줄의 `\` 막기 |
| `bibleref` (노드) | 구절 참조 파서(`services/bibleRef.js`)와 커밋된 `public/bible/` 데이터의 정합(절 수·양끝·클램프·`formatRef` 왕복) |
| `sunguide` (노드) | 순모임 가이드의 순수 로직(`services/sunGuide.js`) — 프롬프트에 실리는 것 · 코드펜스 JSON 파싱 · `fitGuide`가 상한을 문장 경계에서 자르는지 · 큐시트 요지(`cueDigest` — 주제·전례색 + 설교·결단 칸 · 인용문·담당자 버림 · 1200자)와 그 배선 |
| `sheet` (노드) | 엑셀 파서의 **글자 뽑기**(`services/xlsx.js`) — zip·XML 훑기 · 셀 주소 · 공유 문자열 · 서식 · 500줄에서 멈추기. 표를 그리는 것은 구글이므로 서식 충실도는 안 본다 |
| `office` (노드) | 워드·PPT 파서(`docx.js`·`pptx.js`)와 공용 zip·XML 기계(`ooxml.js`) — 상대 경로 · 문서 순서 · 표 병합 · 슬라이드 순서·자리 표시자 |
| `drivesync` (노드) | 드라이브 ↔ 앱 **싱크 불변식**(소스 단정) — 업로드 한 벌·멱등·되돌리기 · 상한 세 곳이 같은 값인지 · 프록시와 Apps Script 문서(`docs/APPS_SCRIPT.md`)가 같은 액션을 아는지 · 미리보기 사본과 큐시트 편집 주소 · 승인 확인이 한 벌(`requireApprovedUser`)이고 합친 계정을 따라가는지(서비스 키라 `is_approved()`를 못 쓴다) · `grantEditors` 자격·`uploadFromUrl` 주소·스크립트로 넘기는 칸 목록 · Apps Script v12(`underRoot` · 첨부 편집은 올린 사람+관리자) · 보안 헤더와 CSP 테마 스크립트 해시(LF로 잰다) · 첨부 발췌도 미리보기와 같은 PDF 워커를 쓰는지 · 내려받기가 attachment 주소로 가는지 · 한글 PDF cmaps가 실제로 나가는지 · 틀 갈래 넷에 시간 제한이 있는지 |
| `assignees` (노드) | 담당자 ↔ 프로필 id 변환. `cloud.js`를 가짜로 바꿔치고 `cloudSync`만 노드에서 돌린다 — 게스트 모드로는 볼 수 없는 경로다. 합친 계정의 이름·사진과 **한 이름 → id 하나**(알림 두 벌 방지)도 여기 |
| `push` (노드) | 알림 문구(앱·푸시 공용) · 크론 둘의 분기와 KST 날짜 · api 공용 머리·상한·크론 비밀 비교 · 딥링크와 그 출처(서버·서비스 워커) · 이전에 없던 대상만 · 본인 제외 · 체크 제약과 INSERT 정책의 짝 · 댓글 반응 · `/api/ai`의 `{ embed }` 갈래(인증이 먼저 · 질문 taskType · 정규화 · 500자) · 8시 크론 뒤 문서 임베딩(알림이 먼저 · 실패 격리 · `?job=embed` 비밀) |
| `aictx` (노드) | AI 프롬프트에 실리는 것 전부 — **등장하지 않는 사람이 안 들어가는지**가 가장 중요하다. 죽은 멘션 되돌리기 · 관련 업무 링크 · 발췌 상한 · 발췌는 문서 먼저, 사진 캡션 뒤 · 사람 줄(`순:` 칸 · 부를 때 · 직함 고르기) · 글에 이름으로 나온 가입자 · 다듬기 예시의 실명·날짜 · 챙길 것 |
| `errhunt` | 화면 × 데이터 상태 × 화면폭 64조합에서 ErrorBoundary·콘솔 오류 |
| `handoff` | 핸드오프 규격 — 토큰·모션·화면 구조 · 업무 상세/수정 · 팀 보드 칩 줄 · 에디터 서식 바(모든 폭에서 sticky-top 하나 · 붙으면 상자가 아니라 줄 · 폭 여섯 × 자리 셋 매트릭스) · 번호 목록이 이어지는 숫자로 그려지는지 · 업무 링크가 '첨부 파일' 구역 안인지 · **업무 창에 링크가 없는지**(2026-09-11에 되돌림) · 업무 보기↔편집기 줄 위치 · 하위 업무 추가 칸 x(B2) · **D9 소스 훑기**(주 버튼 비활성에 bg-line 없음 · 10px 미만 글자는 얼굴 머리글자·종이뿐 · 12px 미만 글에 faint 없음 · 뒤판 50/80 · 버튼 두 단 값) |
| `worship` | 예배 줄기 — 주보 목록·상세·탭 · 작성→발행 · 본문 피커 · 담당자·찬양·광고 편집 · 출석 체크(발행 + 예배일 이후 · 순장은 자기 순) · 손님 · 예배 노트와 종이 · 송폼·큐시트 · 편집 중 공유 토글 · 쓰고 곧바로 나가기 · 인도자 칸 목록은 **문서에서**(포털) · 주보 목록 첫 읽기 실패 자리·토스트 없음·다시 시도·딥링크(D2 — 게스트 저장 자리를 망가뜨려 만든다 · PITFALLS 42-h). **약한 줄 하나**: '취소하면 고치던 글을 버린다'는 글을 치지 않고 통과한다(`docs/PITFALLS.md` §6-42-c) |
| `word` | 말씀 화면 — QT(본문·날짜 이동·묵상 편집기·공유 토글·나눔(사람 칩 + 종이 하나)·잔디) · 성경 읽기(리더·검색·북마크·절 형광펜과 범위 선택·이어읽기) · 노트 종이(서식 6종·진짜 목록·사진·옛 '본문' 도막이 안 서는지·브라우저 초안) · 잠긴 도막 아래 백스페이스 · 평문 붙여넣기 · 초안 즉시 남기기 · **읽기 = 편집 줄 위치**(1440·375 차 0) · 최근 검색어 판이 키보드 아래에서 칸을 덮지 않음 · 폰 Enter 뒤 blur · X 누르는 자리 · 포커스 테두리 자리 · AI 실패 판정과 벡터 대체 머리줄(S-a) |
| `groups` | 모임 줄기 — 내 순(구성원·출석·공유된 노트) · 동아리(순서·가입 신청·리더 도구·모임 출석·QR) · 순 편성과 자격 분기 · 순장 지정 4갈래 · 순모임 가이드 패널 · 첫 읽기 실패 자리(D2 · 375) |
| `roster` | 청년 명단 탭 — 직분 6종·연도 세그먼트·검색·생일 피커·계정 연결 후보 세 갈래·환송/되돌리기 · 도구 줄 줄나눔 · 명단 첫 읽기 실패 자리(왼쪽 정렬 · D2) |
| `home` | 홈 — 인사말·히어로·쇼케이스 · 카드 넷의 차례·초점/메타 한 줄·자리 스켈레톤·캐시 · 카드마다의 이동 |
| `navsmoke` `onebar` `mobbits` | v2 내비 — 데스크톱 상단 한 줄과 모바일 하단 바(교회 바 ↔ 업무 바 모드 전환) · 둘 중 하나만 마운트되는지 · 탭이 줄 폭만큼 보이는지 · 더보기 연도 폴더 · 모바일 상단바 · 탭 줄 앞 칸(게스트는 activityLog로 · 세로선 · 앞 칸 끌기 막힘)과 폰 하단 '프로젝트'가 여는 곳(navsmoke). 검색: navsmoke — 상단 결과 판 폭 320·화면 안(800·1440) · ↓↑Enter로 열기 · 판 최대 360px · mobbits — 가로 폰 마지막 줄(elementFromPoint) · 키보드 흉내 마지막 줄(PITFALLS 42-g) · Enter로 키보드 내림 · 검색어 바꾸면 맨 위 · 둘 다 '검색 결과가 없어요'가 판의 가운데인지와 **게스트에 '관련된 업무 내용' 구역·/api/ai 요청이 없는지**(G-a) |
| `bottomgap` | 모바일 각 탭에서 마지막 내용이 하단 탭바에 가리지 않는지 |
| `modalclose` | 업무 상세 모달 바깥 클릭·드래그·닫기 · 댓글·활동 사이드바 접기(언마운트하지 않음) · 답글 UX(멘션 목록은 문서에서) · ✕·딤도 고친 것을 묻는지 · beforeunload · 남의 체크를 내 저장이 안 되돌리는지 · 폰 Enter 줄바꿈 · 조합 중 Enter · D9(댓글 등록 작은 단 · 업무 창 뒤판 50%·150ms · 댓글 시각 10px muted) |
| `batch10` `batch11` `dashfix` `wide` | 대시보드 — KPI 배치(320~1920px) · 최근 활동 피드 · 프로젝트 연결 지도 · 모바일 세 탭 · '다녀감'과 '활동'이 같은 시각을 보는지 · '지난 7일 간 N건'이 `completed_at` 기준인지 |
| `share` `onboard` | 공유 링크(프로젝트·업무) · 첫 로그인 팀 설정 · 시작하기 확정 단·비활성 opacity .4·뒤판 50%(D9) |
| `three` | 프로젝트 이름 수정 · 참고 링크(팝오버·서비스 표시·저장 · 구글 문서는 앱 안 창 · 비밀번호가 걸린 링크) · 헤더 배치가 좁은 폭에서 화면 안인지 |
| `themefit` | 라이트/다크 × 모바일(390)/데스크톱(1440) 네 조합 × 대시보드·보드·업무 창과 v2·전역 화면(내 업무·일정·홈·예배·말씀·모임·멤버) — 글자가 배경에 묻히지 않는지(대비) · Tailwind 기본 팔레트를 쓰지 않는지 · 가로로 넘치지 않는지 · 10px 미만 글자(얼굴 머리글자 빼고)·12px 미만 faint가 없는지(D9) |
| `calfit` | 캘린더가 창 높이에 맞는지 · 띠가 잘리지 않는지 · 모바일에서 격자와 목록이 다른 스크롤 통인지 · 날짜 칸 +N이 10px muted로 칸 안 · 숫자·얼굴·점과 같은 가운데 줄 · 틈 4px(375·1440 × 두 테마 · D9) |
| `drag` `dragdesk` | 터치/마우스 드래그로 상태 이동·상태 칩 드롭 · 같은 상태 안에서 순서 바꾸기(빈 자리에 놓으면 맨 밑) · 모바일 프로젝트 탭 순서(길게 눌러 끌기) · 상시 줄(칸 위 한 줄 · 카드를 놓으면 상시 · 칩을 칸에 놓으면 그 상태) |

스위트를 한 러너로 잇달아 돌릴 때 `groups`가 CRASH(`.club-meet-date button` null)·`roster` 12 FAIL이 한 번 났는데, 둘 다 혼자 다시
돌리면 전부 통과했다(2026-09-24). 앞 스위트의 여운으로 보인다 — 코드 원인이 아니다.

## 셀렉터가 낡으면

앱 마크업이 바뀌면 셀렉터가 못 찾고 스크립트가 **예외로 죽는다**. 이때 종료 코드는 1이지만
`FAIL` 줄이 없어서 러너가 `CRASH`로 표시한다 — "테스트가 통과했다"와 구분되니, CRASH가
보이면 그 스위트의 셀렉터를 지금 화면 기준으로 다시 맞춰야 한다. 셀렉터를 못 찾을 때
**던지지 말고 FAIL로 남기는** 것이 새 검사를 쓸 때의 규칙이다(`docs/PITFALLS.md` §6-40).
