# tests

> **도는 동안 레포 파일을 고치지 마라.** dev 서버가 vite여서 **아무 파일이 바뀌면**
> 열려 있는 페이지를 새로고침한다(`HANDOFF.md`도 그렇다) — 좌표를 재던 스위트가
> 발밑을 잃고 `CRASH`나 엉뚱한 FAIL로 나온다. 깨졌으면 **손대지 않고 그 스위트만
> 혼자 다시** 돌려 보는 것이 첫 수순이다.

브라우저를 실제로 띄워 화면을 재는 검증 스위트. 단위 테스트 프레임워크는 쓰지 않는다 —
각 파일이 헤드리스 Chrome을 CDP로 붙잡고, 화면을 만들고, 좌표·계산값·localStorage를 재서
`PASS` / `FAIL` 줄을 찍고 종료 코드로 결과를 알린다.

```bash
npm run verify -- calfit drag  # **골라서 — 고친 것과 관련된 것만. 이게 기본이다**
npm run verify                 # 전부(약 7분). 푸시 직전 한 번이면 된다
npm run verify -- --jobs 3     # 동시 실행 (스크립트마다 CDP 포트가 달라 충돌 없음)
SHOTS=1 npm run verify         # 스크린샷도 저장
CHROME=/path/to/chrome npm run verify   # 크롬 경로가 다를 때
```

- 러너가 `vite --mode guest`를 띄운다. `.env.guest`가 없으면 만든다(빈 Supabase 키 = 로그인
  없이 localStorage 모드). 이 파일은 커밋하지 않는다.
- 각 스위트는 `node tests/<name>.mjs <베이스 URL>` 로 혼자서도 돌아간다.

## 스위트

| 파일 | 보는 것 |
|---|---|
| `logcheck` `mdcheck` | 활동 기록 · 하위 업무 진척 · 인사말이 세는 범위(`utils.myScope`) · 프로필 사진 주소 https 승격(`utils.httpsImage`) · 대시보드 사람 칸(다녀간·생일·합류·방문순·시간 단위 사다리) · 달력 생일 · 선후관계 열 배치(depLayers) · 선행 업무 활동 기록 · 프로젝트 탭 순서(position→created_at) · **끌어다 놓기 순서 규칙**(`utils.reorderIds` — 뒤로 끌면 놓은 것 뒤) · **지금 보고 있는 사람**(`utils.viewersOf` — 본인 제외·3명 상한·**사람마다 at 최신 meta 하나만**(자리를 옮기면 옛 자리에서 즉시 빠짐)·presence가 at을 싣는지·본문 링크 pushState 배선) · **presence 연결 수명**(healRefs가 join·leave 둘 다에 걸렸는지 등) · **다녀감 심장박동**(`utils.dueForHeartbeat` — 5분 간격·경계 포함) · **1분 틱**(`useMinuteTick` — 분 단위 숫자 하나) · 업무 줄의 팀 표시(`utils.teamsLabel`) · 프로젝트 연도(`utils.projectYear`·`projectsOfYear`) · 달력에 얇힐 수 있는 업무(`utils.datedTasks`) · 엑셀 미리보기 주소(`utils.sheetPreviewUrl`) · 달력 열 폭(`utils.snapCols`) · **목록 최신순 정렬**(`utils.byNewest` 만든 순↓ = 선행 업무 후보 · `utils.byRecent` 손댄 순↓ = 마감 목록의 '끝낸 업무' · 호출부 배선까지) · **끝낸 시각**(`TaskService.completedAtFor` — 완료로 들어올 때만 찍고 완료→완료는 그대로 · 0034 트리거와 같은 규칙인지까지) · **그래프 부드러움**(`forceStep` 상수와 `useForceGraph`의 미리 돌리기 — 초기 폭발과 방향 반전을 실제로 재서 상한을 둔다) · **라벨 떼어놓기**(`utils.spreadLabels` — 두 방향 훑기 · 자리가 모자라면 균등 분배) · **지도가 한 번만 배치되나**(폭을 재기 전에는 노드를 만들지 않는지 — 두 번 배치되면 "뚜둑"이다) · **끌기 손맛**(잡은 지점 유지 · 끌기용 넓은 범위 `forceBounds(…, drag)`) · **빈 그래프 뷰**(표식 + 가운데 정렬) · **지도 연도 고르기**(고른 해만 받는지 · 팀 집계도 그 해만 · 같은 YearPicker 부품 · 몇 개까지 라벨이 안 닿는지) · **연도 바꿀 때 스크롤 보정**(지도가 화면에서 안 움직이는지 · 창이 아니라 main을 스크롤하는지) · **순차 등장은 첫 마운트만**(`useEnterStagger` — 나중에 붙는 줄에 순번 지연을 주면 그 줄만 뒤늦게 나타난다) · 마크다운 라운드트립 + **본문 체크리스트**(`- [ ]` 파싱·직렬화·`utils.toggleTodoLine`) (브라우저 없이 순수 로직) |
| `sheet` | **엑셀·csv 파서**(`services/xlsx.js`) — zip 풀기 · 공유 문자열(rPh 덧말 걷어내기) · 숫자 형식 · 굵기/기울임/취소선 · 글자색 · 칠 · **테두리** · **조건부 서식**(cellIs·색눈금·데이터막대·**수식 규칙**·**아이콘 집합**) · `services/formula.js`(상대 참조 이동·범위·40여 함수·못 읽는 수식은 칠하지 않기) · 병합(끝 셀 테두리를 앵커로 합성 · rs/cs 클램프) · 숨긴 열(자리째 제거) · 열 너비(시트 기본값 포함) · **부분 서식**(한 셀 안 구간별 색·굵기·크기) · **한국어 내장 날짜**(m월 d일·요일) · **숫자 서식 구간**(₩ 접두·회계식 0="-"·[Red] 음수) · 글자 크기(sz) · 행 높이 · 값 영역 안 테두리-only 칸 살리기 · **시트 위 그림**(앵커 셀·표 밖 클램프) · 자동 필터 자리 · csv 따옴표. **자기 닫는 태그가 다음 태그를 삼키지 않는지**를 특히 본다(그 버그가 값을 한 칸 왼쪽으로 밀고 공유 문자열을 인덱스 숫자로 보이게 했다). 브라우저도 서버도 필요 없다 |
| `office` | **워드·파워포인트 파서**(`services/docx.js`·`services/pptx.js`)와 공용 zip·XML 기계(`services/ooxml.js`) — `../` 상대 경로 풀기(못 풀면 슬라이드가 통째로 빈다) · 중첩을 세는 문서 순서 훑기(표 안 문단이 본문에 또 나오지 않게) · 문서 제목(Title/제목) · 스타일에만 걸린 목록 · 표 병합/칠/열 너비 · `w:val="0"`이 굵기를 끄는지 · 탭/줄바꿈/쪽나눔 · 자리 표시자 좌표를 레이아웃에서 물려받는지 · 제목에 점을 안 찍는지 · 슬라이드 순서가 `sldIdLst`를 따르는지. 브라우저도 서버도 필요 없다 |
| `drivesync` | **드라이브 ↔ 앱 싱크 불변식**(소스 단정) — 함수 시간 제한을 우리가 명시했는지 · 프록시가 스스로 끊고 한국어 이유를 주는지 · **업로드가 멱등 목록에 없는지**(있으면 중복 파일) · 재시도에 `retry` 표시가 붙는지 · 행 실패 시 올린 파일을 되돌리는지 · 폴더 확보가 업로드보다 앞인지 · 프록시가 아는 액션과 `docs/DRIVE.md` 스크립트가 아는 액션이 같은지. 브라우저도 서버도 필요 없다 |
| `push` | 알림 문구(앱·푸시 공용) · 마감 임박 배치의 KST 날짜 · 딥링크 · `insert`에 `.select()`가 붙지 않았는지 · 이전에 없던 담당자만 · 본인 제외 · 0017의 체크 제약 ↔ INSERT 정책 짝 · **댓글 반응**(0032 — 토글 판정·종류별 요약·반응 알림의 본인 제외·표가 없어도 loadCardDetail이 안 죽는지·RLS·실시간 라우팅·발행·칩 라벨(좋아요/최고/확인)·아이콘 정사각 가운데·얼굴 인라인/+N) · 크론 설정 · `sw.js`. 브라우저 없이 돈다 |
| `assignees` | 담당자 ↔ 프로필 id 변환 (읽기: 조인·폴백·빈 값 / 쓰기: 신규·수정·미등록). `cloud.js`를 가짜로 바꿔치고 `cloudSync`만 노드에서 돌린다 — 게스트 모드로는 볼 수 없는 경로다 |
| `aictx` | **AI 프롬프트에 실리는 것 전부**(78단정) — 오늘 날짜와 마감까지 남은/지난 날 · 같은 프로젝트 12건 · **다른 프로젝트의 진행 중 업무와 같은 팀의 완료 아카이브** · 선후관계 · **하위 업무를 끝난 것/남은 것으로 가르는지** · 첨부 이름과 **발췌**(파일 3개·3000자 상한) · **그 업무에 등장하는 사람만** 싣는지(팀·역할·멘션 표기) · AI가 쓴 죽은 멘션을 되돌리는지 · **관련 업무 링크**(`[[업무:제목]]` 지시가 실리는지 · `resolveTaskLinks`가 정확 일치로만 링크를 만들고 자기 자신·불일치·**파일 확장자로 끝나는 제목**은 글자로 두는지 · 첨부 이름 표시 금지 지시) · 배경 지식(예배 순서·성찬·지출은 메뉴얼 업무를 가리키는지·순과 조·워십팀·§8 문구 톤) · 요약 캐시 · 다듬기가 안내 문구를 그대로 돌려주는지. 브라우저도 서버도 필요 없다 |
| `errhunt` | 화면 × 데이터 상태 × 화면폭 64조합에서 ErrorBoundary·콘솔 오류 |
| `handoff` | 핸드오프 규격(토큰·모션·4개 화면 구조·업무 상세/수정) · **에디터 서식 바**(머리줄 바로 아래에 붙는지 — 데스크톱·모바일 · 완전 불투명 · 블러·그림자 없음) · **제목 Enter**(끝에서 빈 문단을 안 만드는지 · 가운데서 본문으로 떨어지는지 · keepMarks) |
| `bibleref` | **구절 참조 파서**(`services/bibleRef.js` — '이사야 32:9-20' → 책·장·절 범위 · 약칭 · 장 전체 · 장을 건너는 범위 · '아가'가 '아모스'를 밟지 않는지 · 요한일서/요한복음 · 모르는 책은 null) + 커밋된 `public/bible/` 데이터로 절 수·양끝·클램프·`formatRef` 왕복. 브라우저도 서버도 필요 없다 |
| `sunguide` | **순모임 가이드 순수 로직**(`services/sunGuide.js`) — 프롬프트에 제목·구절·본문·JSON 지시가 실리는지 · 코드펜스 JSON 파싱 · 잘못된 모양은 null · `fitGuide`가 상한을 **문장 경계에서** 자르고 배열을 3으로 맞추는지 · `**굵게**` 마커 파서. 브라우저도 서버도 필요 없다 |
| `worship` | **예배 줄기** — 주보 목록·상세(말씀/담당자/찬양/광고) · 작성→발행 · 본문 선택 피커 · 담당자 자동완성 · 저절로 저장 라벨('임시 저장되었어요' / 발행본은 '저장되었어요') · 출석 체크(**발행된 주보 + 예배일 이후만** · 순장은 자기 순) · 미등록 출석자 추가 · 내 예배 노트 · **주보 구절 → 성경 읽기** · **한 줄 생성기**(기본값 채움·모바일 두 줄) · 책 칸 ▾가 실제 mousedown에 열리는지 · **빈 상태가 남는 공간을 채우는지**(스크롤 박스 바닥 기준) · 유튜브 URL 파서(`youtubeListId`·`youtubeVideoId`·`mergeSongs`) · 노트 저장 버튼 · 375px. 게스트 저장 자리 `church_worship_v1`에 가짜 주보·명단을 심는다 — 자격(회장·순장·일반)은 시드의 `me`가 말하고, 클라우드에서는 RLS(0035·0036)가 같은 경계를 긋는다 |
| `word` | **말씀 화면** — QT(오늘 본문 · 날짜 이동 · 묵상 MarkdownEditor · '더다붓에 공유하기' · 나눔 피드 · 나만 보는 잔디) · 성경 읽기(**목차 · 북마크 · 형광펜 세그먼트** · 리더 · 검색 · 이어읽기 · **절 형광펜**이 인라인 mark에만 칠해지는지 · 팝오버가 절마다 새로 앉는지) · 날짜를 넘길 때 묵상 칸이 위로 튀지 않는지 · 공유 토글이 dirty를 안 건드리는지 · 나눔 지우기 = 공유 해제 · QT 날짜 데이트피커 · 잔디 13px. 앞부분은 서버 없는 날짜 셈, 뒷부분이 브라우저. 게스트는 `word_qt_schedule`·`word_qt_entries`에 가짜 일정·묵상을 심는다 |
| `groups` | **모임 줄기** — 내 순(구성원 · 예배 출석 · 공유된 노트) · 동아리(목록 · 끌어서 순서 · 가입 신청 · 리더 도구 · 모임 출석) · 순 편성(자격 분기 — **관리자에겐 보이고 교역자에겐 없는지** · 순원 추가/이동/빼기 · 새 순 · 리더 먼저+가나다) · 동아리 이름·설명 인라인 수정(`canEditClub` 네 경계) · 모임 한 줄 생성기 · **순모임 가이드 패널**이 순장에게만 보이고 내 순 카드와 좌우 끝이 같은지 · **순장 지정 4갈래**(다른 순의 순장 → 불가 · 이 순의 순원 → 바로 · 다른 순의 순원 → 먼저 순원 추가 안내 · 미배정 → 순장+구성원) · `sun_exempt` 제외 · 새 동아리 카드형·새 순 한 줄 생성기 · 원인이 붙은 실패 문구. 게스트 저장 자리 `church_groups_v1` — 예배 표는 `church_worship_v1`에도 같이 심는다(내 순의 출석이 그쪽을 읽는다) |
| `roster` | **명단 관리**(멤버 화면의 '명단' 탭 — 마스터+관리자) — 생일 형식 · 검색 · 계정 연결 후보 · 직분 배지(순수 로직) + 추가 · 수정 · 팀 · 직분 · 계정 연결/해제 · 환송/되돌리기(브라우저). 게스트 `church_roster_v1` |
| `home` | **홈**(첫 화면) — 인사말 · 카드 넷(오늘의 QT · 이번 주 예배 · 내 업무 · 내 순) · **히어로**(캐릭터 컷이 그려지는지 · 시각별 컷 `heroCut` 경계 · 인사말과 카드의 왼쪽 선 정렬) · 카드마다의 이동 · 스켈레톤 · 빈 자리 · 375px · 다크. 자기 저장 자리가 없어 말씀·예배·모임·업무 **네 곳에 심고** 한 화면에 모이는지를 본다 |
| `navsmoke` `onebar` `mobbits` | **v2 내비**(데스크톱 상단 [홈·예배·말씀·모임 \| 업무 대시보드·내 업무·일정] · 모바일 하단 바 — 홈에서는 교회 바(예배·말씀·모임·업무), '업무'를 누르면 업무 바 5칸(홈·프로젝트·내 업무·대시보드·팀)), 내비 구조, 데스크톱/모바일 내비가 하나만 마운트되는지, 모바일 상단바, **탭이 줄 폭만큼 보이는지**(1440px=8개 전부·800px=일부+더보기·무넘침), **더보기 연도 폴더**, **연도 고르기**(탭 줄 맨 앞 `2026 ▾` · 연도는 만든 날짜가 아니라 `projects.year`를 따른다 — 0025), **보관**(보관하면 그 탭에서 나가는지 · 열어 둔 보관 프로젝트가 탭과 더보기에 동시에 보이지 않는지) |
| `bottomgap` | 모바일 각 탭에서 마지막 내용이 하단 탭바에 가리지 않는지 |
| `modalclose` | 업무 상세 모달 바깥 클릭·드래그·닫기, **댓글·활동 사이드바 접기**(폭 0 · 언마운트하지 않음), 저장해도 댓글·활동이 남는지, **답글 UX**(답글 버튼이 hover 없이 보이는지·답글 수·'○○님에게 답글' 줄·textarea·`@` 멤버 제안·접어도 초안이 남는지·등록) |
| `batch10` `batch11` `dashfix` `wide` | 대시보드·모바일 조작·문구·KPI 배치(320~1920px) · **최근 활동 피드·프로젝트 연결 지도** · **모바일 세 탭(업무·청년·연결)·연도별 프로젝트 진행·`+ 프로젝트` 정렬**(dashfix) |
| `share` `onboard` | 공유 링크(프로젝트·업무), 첫 로그인 팀 설정 |
| `three` | 프로젝트 이름 수정, **참고 링크**(포털 팝오버 안을 눌러도 닫히지 않는지 · 서비스 표시 · 저장), **헤더 배치**(375·414·768에서 삭제가 화면 안이고 링크 스크롤 칸 밖인지 · 모바일에서 실선·공유·삭제 묶음이 '새 업무' 버튼 **가운데**에 서는지(자획 기준) · 실선이 아이콘에 붙어 있는지 · 묶음이 버튼 칸을 안 벗어나는지 · 링크 줄과 아이콘의 **가운데선**이 같은지 · '+ 참고 링크'가 미는 칸 밖인지 · 헤더 아래 실선 밖으로 나가는 것이 없는지 · '새 업무'가 값 줄과 같은 줄인지 · 진척 바 · 담당자 얼굴이 바 뒤인지 · 320px에서 값 줄이 넘치지 않는지), **그래프 보기**(노드·선·열 배치·수정 폼의 선행 업무 칸), 모바일 가로 스크롤 잠금, 컬럼 구분 |
| `themefit` | **라이트/다크 × 모바일(390)/데스크톱(1440)** 네 조합 × 대시보드·보드·업무 창. 글자가 배경에 묻히지 않는지(대비 2.0 미만), Tailwind 기본 팔레트를 쓰지 않는지(§8), 가로로 넘치지 않는지, 본문 체크박스를 직접 그리는지 |
| `calfit` | 캘린더가 창 높이에 맞는지, 띠가 잘리지 않는지, 날짜 한 번에 선택, **모바일에서 격자와 목록이 다른 스크롤 통인지**(목록을 밀 때 달력이 밀려 올라가지 않아야 한다) · 목록이 좌우로 밀리지 않는지 |
| `drag` `dragdesk` | 터치/마우스 드래그로 상태 이동, 상태 칩 드롭, **같은 상태 안에서 순서 바꾸기**(0024 — 데스크톱·모바일 양쪽 · position 저장 · 컬럼 안 세로 스크롤 유지 · **컬럼 빈 자리에 놓으면 맨 밑으로**. 마지막 것은 카드 셋짜리 시드를 따로 쓴다 — 공용 시드는 컬럼이 꽉 차 빈 자리가 없다) · **모바일 프로젝트 탭 순서**(길게 눌러 끌기 · 뒤로 끌면 놓은 탭 뒤 · 짧게 누르면 이동만 · 가로 스크롤 유지) |

## 셀렉터가 낡으면

앱 마크업이 바뀌면 셀렉터가 못 찾고 스크립트가 **예외로 죽는다**. 이때 종료 코드는 1이지만
`FAIL` 줄이 없어서 러너가 `CRASH`로 표시한다 — "테스트가 통과했다"와 구분되니, CRASH가
보이면 그 스위트의 셀렉터를 지금 화면 기준으로 다시 맞춰야 한다.
