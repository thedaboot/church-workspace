# tests

> **도는 동안 레포 파일을 고치지 마라.** dev 서버가 vite여서 **아무 파일이 바뀌면**
> 열려 있는 페이지를 새로고침한다(`HANDOFF.md`도 그렇다) — 좌표를 재던 스위트가
> 발밑을 잃고 `CRASH`나 엉뚱한 FAIL로 나온다. 깨졌으면 **손대지 않고 그 스위트만
> 혼자 다시** 돌려 보는 것이 첫 수순이다.

브라우저를 실제로 띄워 화면을 재는 검증 스위트. 단위 테스트 프레임워크는 쓰지 않는다 —
각 파일이 헤드리스 Chrome을 CDP로 붙잡고, 화면을 만들고, 좌표·계산값·localStorage를 재서
`PASS` / `FAIL` 줄을 찍고 종료 코드로 결과를 알린다.

```bash
npm run verify -- calfit drag  # **골라서 — 고친 것과 관련된 것만. 이게 기본이다**
npm run verify                 # 전부(약 7분). **사용자가 부를 때만** — 푸시 직전도 아니다
npm run verify -- --jobs 3     # 동시 실행 (스크립트마다 CDP 포트가 달라 충돌 없음)
SHOTS=1 npm run verify         # 스크린샷도 저장
CHROME=/path/to/chrome npm run verify   # 크롬 경로가 다를 때
```

- 러너가 `vite --mode guest`를 띄운다. `.env.guest`가 없으면 만든다(빈 Supabase 키 = 로그인
  없이 localStorage 모드). 이 파일은 커밋하지 않는다.
- 각 스위트는 `node tests/<name>.mjs <베이스 URL>` 로 혼자서도 돌아간다.
- **스위트는 33종**이다(`tests/run.mjs`의 `ORDER`가 목록이고 디스크와 같아야 한다 — `run.mjs`·`zip.mjs`는
  스위트가 아니라 러너와 공용 도구다). 새 스위트를 만들면 `ORDER`에 넣고 이 표에도 한 줄 남긴다.
- **여러 러너를 동시에 띄우지 마라.** 한 러너 안에서는 스크립트마다 CDP 포트가 갈라져 있지만,
  러너를 둘 띄우면 그 보호가 없어 서로의 크롬에 붙어 `CRASH`·타임아웃이 난다(HANDOFF §6-42-a).

## 스위트

| 파일 | 보는 것 |
|---|---|
| `logcheck` `mdcheck` | 활동 기록 · 하위 업무 진척 · 인사말이 세는 범위(`utils.myScope`) · 프로필 사진 주소 https 승격(`utils.httpsImage`) · 대시보드 사람 칸(다녀간·생일·합류·방문순·시간 단위 사다리) · 달력 생일 · 선후관계 열 배치(depLayers) · 선행 업무 활동 기록 · 프로젝트 탭 순서(position→created_at) · **끌어다 놓기 순서 규칙**(`utils.reorderIds` — 뒤로 끌면 놓은 것 뒤) · **지금 보고 있는 사람**(`utils.viewersOf` — 본인 제외·3명 상한·**사람마다 at 최신 meta 하나만**(자리를 옮기면 옛 자리에서 즉시 빠짐)·presence가 at을 싣는지·본문 링크 pushState 배선) · **presence 연결 수명**(healRefs가 join·leave 둘 다에 걸렸는지 등) · **다녀감 심장박동**(`utils.dueForHeartbeat` — 5분 간격·경계 포함) · **1분 틱**(`useMinuteTick` — 분 단위 숫자 하나) · 업무 줄의 팀 표시(`utils.teamsLabel`) · 프로젝트 연도(`utils.projectYear`·`projectsOfYear`) · 달력에 얇힐 수 있는 업무(`utils.datedTasks`) · 엑셀 미리보기 주소(`utils.sheetPreviewUrl`) · 달력 열 폭(`utils.snapCols`) · **목록 최신순 정렬**(`utils.byNewest` 만든 순↓ = 선행 업무 후보 · `utils.byCompleted` 끝낸 순↓ = 마감 목록의 '끝낸 업무' · 호출부 배선까지) · **끝낸 시각**(`TaskService.completedAtFor` — 완료로 들어올 때만 찍고 완료→완료는 그대로 · 0034 트리거와 같은 규칙인지까지) · **그래프 부드러움**(`forceStep` 상수와 `useForceGraph`의 미리 돌리기 — 초기 폭발과 방향 반전을 실제로 재서 상한을 둔다) · **라벨 떼어놓기**(`utils.spreadLabels` — 두 방향 훑기 · 자리가 모자라면 균등 분배) · **지도가 한 번만 배치되나**(폭을 재기 전에는 노드를 만들지 않는지 — 두 번 배치되면 "뚜둑"이다) · **끌기 손맛**(잡은 지점 유지 · 끌기용 넓은 범위 `forceBounds(…, drag)`) · **빈 그래프 뷰**(표식 + 가운데 정렬) · **지도 연도 고르기**(고른 해만 받는지 · 팀 집계도 그 해만 · 같은 YearPicker 부품 · 몇 개까지 라벨이 안 닿는지) · **연도 바꿀 때 스크롤 보정**(지도가 화면에서 안 움직이는지 · 창이 아니라 main을 스크롤하는지) · **순차 등장은 첫 마운트만**(`useEnterStagger` — 나중에 붙는 줄에 순번 지연을 주면 그 줄만 뒤늦게 나타난다) · **화면 데이터 캐시 계약**(17 — scope를 바꿀 때 옛 키 정리 · 한도에 걸리면 이 scope만 비우고 한 번 재시도 · `dropCache` 접두) · **묵상 본문 동기화 · 성경 상태**(14 — `word.shouldAdoptBody`가 쓰던 글을 안 덮는지 · `word_bible_state:<uid>` 사용자별 자리) · **v2 실시간 라우팅**(31 — 표→캐시 접두 · 300ms 묶기 · 구독자가 없으면 `dropCache`만 · 편집 중 게이트가 신호를 기억했다 한 번만 흘리는지) · **자리 이동 판정**(7 — `utils.nextWhereMeta`가 자리가 달라졌을 때만 찍고 `seq`로 동점을 가르는지) · **쓰기는 곧 지금**(12 — `WRITE_STAMP_MS` 1분 스로틀 · `mergeActivitySeen`이 활동과 다녀감 중 큰 값을 고르고 바뀐 것이 없으면 같은 배열을 돌려주는지) · **다녀간 시각을 찍는 자리**(4 — `cloudSync.markSeen` 하나인지 · 서버 `now()` rpc · 떠날 때 keepalive) · **공유 카드 메타**(5 — `api/share.js`가 조회 `error`를 읽는지 · 상태 라벨) · **SYNC_TASK는 되돌리기 기록에 안 쌓인다**(6 — 없는 액션 이름은 조용히 무동작이라 **실제 스토어를 돌려서** 본다) · **팀 보드 사람 칩**(12 — `utils.teamChips`가 팀 **소속** 기준인지, 남은 건수↓→가나다, 게스트 폴백, views.jsx 배선) · **순차 등장 배선**(5 — membersView·roster까지) · 마크다운 라운드트립 + **본문 체크리스트**(`- [ ]` 파싱·직렬화·`utils.toggleTodoLine`) (브라우저 없이 순수 로직) · **업무 이번 주(주일~토)**(`utils.weekEndOf` 16 — 경계·연말·두 화면이 같은 함수) · **멘션 꼬리**(`utils.splitMention` 8 — RichText가 같은 규칙) · **기호 보조 글꼴**(symbols.css 범위에 ◡·결합 점 · SUIT 바로 뒤 · 한글은 밖) · **한 벌로 모은 규칙 셋**(15 — `layout.CHURCH_MENUS`·`utils.GOOGLE_EDITOR`·`viewPw.js`) · **성경 상태 그릇**(`useStateBox` — 표식 한 자리·adopt 두 자리) · **liveV2 표 11**(0056 · 맨 'groups' 접두 없음) |
| `sheet` | **엑셀 파서의 글자 뽑기**(`services/xlsx.js` · 12가지) — zip 풀기 · 여는 태그 훑기(`attrs`·`blocks`) · 셀 주소 · 보이는 시트 · 공유 문자열(rPh 덧말 걷어내기) · 숫자·날짜 서식 · inlineStr/참거짓/오류값 · 숨긴 행 · 500줄에서 멈추기 · **이 파서를 쓰는 곳이 첨부 내용 검색 하나인지**(소스 단정). **2026-08-30부터 색·테두리·조건부 서식·그림·틀 고정을 보던 단정은 없습니다** — 표를 그리는 것은 구글이고 우리 렌더러(`SheetView`)와 `formula.js`를 그날 지웠습니다(HANDOFF §6-29-c). 브라우저도 서버도 필요 없다 |
| `office` | **워드·파워포인트 파서**(`services/docx.js`·`services/pptx.js`)와 공용 zip·XML 기계(`services/ooxml.js`) — `../` 상대 경로 풀기(못 풀면 슬라이드가 통째로 빈다) · 중첩을 세는 문서 순서 훑기(표 안 문단이 본문에 또 나오지 않게) · 문서 제목(Title/제목) · 스타일에만 걸린 목록 · 표 병합/칠/열 너비 · `w:val="0"`이 굵기를 끄는지 · 탭/줄바꿈/쪽나눔 · 자리 표시자 좌표를 레이아웃에서 물려받는지 · 제목에 점을 안 찍는지 · 슬라이드 순서가 `sldIdLst`를 따르는지. 브라우저도 서버도 필요 없다 |
| `drivesync` | **드라이브 ↔ 앱 싱크 불변식**(소스 단정) — 주보 파일 갈래(0054 CHECK·백필·업로드 호출 한 군데·기본 송폼·게스트 kind·화면 필터) · 함수 시간 제한을 우리가 명시했는지 · 프록시가 스스로 끊고 한국어 이유를 주는지 · **업로드가 멱등 목록에 없는지**(있으면 중복 파일) · 재시도에 `retry` 표시가 붙는지 · 행 실패 시 올린 파일을 되돌리는지 · 폴더 확보가 업로드보다 앞인지 · 프록시가 아는 액션과 `docs/DRIVE.md` 스크립트가 아는 액션이 같은지 · **주보 송폼**(0047 · 4묶음 — 업무 첨부와 **같은 업로드 한 벌**을 지나는지 · 드라이브 자리가 `예배/<날짜>` 한 벌이고 폴더가 파일보다 먼저인지 · 초기 로드가 송폼까지 끌어오지 않는지 · `files` RLS 넷이 전부 주보 갈래로 갈라져 있는지) · HTML 첨부 iframe의 sandbox 허용이 `allow-scripts` **하나뿐**인지. 브라우저도 서버도 필요 없다 · **오피스 구글 사본**(8 — `previewKind` `'gdoc'`은 `preview_file_id`가 있을 때만 · `previewCopyUrl` 종류별 주소 · 클라이언트가 `convert: true`를 워드·PPT에 안 보냄 · v8 문서에 `GOOGLE_DOCS`·`GOOGLE_SLIDES`·`convertTo` · 백필이 version≥8 게이트 · 편집 권한/`/edit` 금지) · **v9 업로드 왕복 둘**(`createInFolder` · `Drive.Files.create(meta, blob` · createFile/stampKey/setSharing 없음 · reader 공유 · upload·uploadFromUrl 둘 다) · 슬라이드 사본 주소에 `rm=minimal` 없음(문서에는 있음) |
| `push` | 알림 문구(앱·푸시 공용 — v2 종류 여섯과 `notifArea`) · 마감 임박 배치의 KST 날짜 · 딥링크 · **예배 당일 배치**(크론 둘 `0 22`·`30 2` · `?job=worship` 분기 · 서버가 브라우저 모듈을 끌고 오지 않는지 · `link` 전달) · `insert`에 `.select()`가 붙지 않았는지 · 이전에 없던 담당자만 · 본인 제외 · 0017의 체크 제약 ↔ INSERT 정책 짝 · **댓글 반응**(0032 — 토글 판정·종류별 요약·반응 알림의 본인 제외·표가 없어도 loadCardDetail이 안 죽는지·RLS·실시간 라우팅·발행·칩 라벨(좋아요/최고/확인)·아이콘 정사각 가운데·얼굴 인라인/+N) · 크론 설정 · `sw.js` · **가입 요청 문구**(2 — `approval`이 '○○○님이 가입을 요청했어요'이고 시스템 갈래가 아니다. 이 열쇠가 없던 동안 fallback으로 '나를 멘션했어요'로 떴다) · **마감 임박 배치의 N+1**(4 — 구독·안 읽은 수 조회가 발송 루프 **밖에서 한 번**인지, 루프 안에 왕복이 남아 있지 않은지). 브라우저 없이 돈다 |
| `assignees` | 담당자 ↔ 프로필 id 변환 (읽기: 조인·폴백·빈 값 / 쓰기: 신규·수정·미등록). `cloud.js`를 가짜로 바꿔치고 `cloudSync`만 노드에서 돌린다 — 게스트 모드로는 볼 수 없는 경로다 |
| `aictx` | **AI 프롬프트에 실리는 것 전부**(78단정) — 오늘 날짜와 마감까지 남은/지난 날 · 같은 프로젝트 12건 · **다른 프로젝트의 진행 중 업무와 같은 팀의 완료 아카이브** · 선후관계 · **하위 업무를 끝난 것/남은 것으로 가르는지** · 첨부 이름과 **발췌**(파일 3개·3000자 상한) · **그 업무에 등장하는 사람만** 싣는지(팀·역할·멘션 표기) · AI가 쓴 죽은 멘션을 되돌리는지 · **관련 업무 링크**(`[[업무:제목]]` 지시가 실리는지 · `resolveTaskLinks`가 정확 일치로만 링크를 만들고 자기 자신·불일치·**파일 확장자로 끝나는 제목**은 글자로 두는지 · 첨부 이름 표시 금지 지시) · 배경 지식(예배 순서·성찬·지출은 메뉴얼 업무를 가리키는지·순과 조·워십팀·§8 문구 톤) · 요약 캐시 · 다듬기가 안내 문구를 그대로 돌려주는지. 브라우저도 서버도 필요 없다 · **`callGemini` 상한**(3 — AbortController · `CALL_TIMEOUT_MS` · AbortError는 `failed`) |
| `errhunt` | 화면 × 데이터 상태 × 화면폭 64조합에서 ErrorBoundary·콘솔 오류 |
| `handoff` | 핸드오프 규격(토큰·모션·4개 화면 구조·업무 상세/수정) · **팀 보드 칩 줄**(제목 아래 · 왼쪽 정렬 · nowrap+가로 스크롤 · 끝 여백 12px) · **에디터 서식 바**(머리줄 바로 아래에 붙는지 — 데스크톱·모바일 · 완전 불투명 · 블러·그림자 없음) · **제목 Enter**(끝에서 빈 문단을 안 만드는지 · 가운데서 본문으로 떨어지는지 · keepMarks) |
| `bibleref` | **구절 참조 파서**(`services/bibleRef.js` — '이사야 32:9-20' → 책·장·절 범위 · 약칭 · 장 전체 · 장을 건너는 범위 · '아가'가 '아모스'를 밟지 않는지 · 요한일서/요한복음 · 모르는 책은 null) + 커밋된 `public/bible/` 데이터로 절 수·양끝·클램프·`formatRef` 왕복. 브라우저도 서버도 필요 없다 |
| `sunguide` | **순모임 가이드 순수 로직**(`services/sunGuide.js`) — 프롬프트에 제목·구절·본문·JSON 지시가 실리는지 · 코드펜스 JSON 파싱 · 잘못된 모양은 null · `fitGuide`가 상한을 **문장 경계에서** 자르고 배열을 3으로 맞추는지 · `summaryRef`(배경 요약 구절 범위, 선택 필드 ≤30자) · `**굵게**` 마커 파서. 브라우저도 서버도 필요 없다 · **2026-09-08 계약**(79 — 줄글 요약 선택 · 질문 3~4 · `questionNote` 상한 · 프롬프트가 질문 넷·첫 질문 일상·'EX.'/'Q.'/번호 금지 · `guideServices` 최근순 8 + 고정본 · 게스트 고정 깃발 · `saveGuide`가 고정을 풀지 않음) · `passage.title`은 프롬프트에 없고 `generateGuide`가 주보 제목으로 채움 · `guidedServiceIds`(기본값 `{}` 행 제외) · `guideServiceDate` |
| `worship` | **예배 줄기** — 주보 목록·상세(말씀/담당자/찬양/광고) · 작성→발행 · 본문 선택 피커 · 담당자 자동완성 · 저절로 저장 라벨('임시 저장되었어요' / 발행본은 '저장되었어요') · 출석 체크(**발행된 주보 + 예배일 이후만** · 순장은 자기 순) · 미등록 출석자 추가 · 내 예배 노트 · **주보 구절 → 성경 읽기** · **한 줄 생성기**(기본값 채움·모바일 두 줄) · 책 칸 ▾가 실제 mousedown에 열리는지 · **빈 상태가 남는 공간을 채우는지**(스크롤 박스 바닥 기준) · 유튜브 URL 파서(`youtubeListId`·`youtubeVideoId`·`mergeSongs`) · 노트 저장 버튼 · 캐릭터 컷 0개(홈만) · 주보 자격(관리자·회장·미디어팀) · 카드 초점+메타 2줄 · 편집 탭 반응형(375~1440 × 4탭) · 찬양 썸네일·링크 · 가져오기 로딩 · 모바일 하단 고정 편집 줄(포털) · 노트 = MarkdownEditor + 공용 ShareToggle(말씀과 같은 부품인지 소스 검사) · 캐시(목록 → 상세 → 목록에 스켈레톤 없음) · **송폼**(11 — 수정 화면에만 올리기·삭제가 서는지 · 붙은 것이 없을 때 안내 줄을 붙이지 않는지 · 고른 파일이 `service_id` 축으로 저장되는지 · 자격이 없으면 읽고 열기만 · 375px에서 줄이 안 넘치고 하단 저장 줄에 안 가리는지) · **순장**(2 — 0050 · 순장이 올린 새신자가 자기 순 명단에 같이 들어가 그 묶음에 출석으로 서는지) · **출석 메모**(1) · **묶음 머리**(교역자 '전도사님' · 올해 부장 '부장님'이 순 위에, 순 미지정에서 빠짐) · **손님**(`attendance_guests` — 명단(people)에 안 올라가고 이름 칩 + × · 상단 '손님 N') · **노트 읽기 모드**(저장 뒤 RichText + 수정 · 편집은 저장/취소 · 도구 줄 grid가 375에서 토글만 한 줄) · 찬양 썸네일 스켈레톤 · 유튜브 가져오기·찬양 줄의 고아 도막 없음 · **큐시트**(구글 문서 링크만 담김 · 비밀번호 칸 없음 · **파일**(0054 `kind='cuesheet'`) — 수정 화면에만 올리기, `files` 행에 service_id+kind, 보기 카드에 링크+파일 줄, 송폼 줄에 안 섞임, 375 고아 줄 없음 · 편집 줄이 탭바와 틈 없이 맞닿음 · 링크·제목 칸 같은 폭) · 목록 카드 '출석 N명' · 딥링크 `?s=` · 375px. 게스트 저장 자리 `church_worship_v1`에 가짜 주보·명단을 심는다 — 자격(회장·순장·일반)은 시드의 `me`가 말하고, 클라우드에서는 RLS(0035·0036)가 같은 경계를 긋는다 · **생성기 세그먼트**([주일예배|다른 예배] 전체 라벨 · 라벨 붙은 칸 34px · 375 세 줄, 취소 오른쫽 끝) · **출석 메모 읽기/수정**(저장은 바뀐 것이 있을 때만 · 빈 메모 저장 허용 · 취소 복원) · **`cleanTitle`**(수학 볼드·전각 → 평문, 서버 import 소스 검사) · '작성 중인 주보 N건' · '예: 다붓이' · **목록 스켈레톤이 칩 줄까지**(`worship-loading-chips`) · 본문 대기 `PassageSkeleton` · 출석 메모 폭 = 옆 구역 · 375 찬양 링크 칸 왼쪽 = 제목 칸 · 광고 펼치기 재측정 · **재생목록 ×**(주소만 비움·곡 유지·칸이 주보 주소로 채워짐) · `PraiseHead` 가운데 정렬(링크 중심 = 인도자 글자 중심 ±1px, 375·1440) · **주보 종이**(7 — 발행본이 '주보' 탭(종이 두 쪽)으로 열림 · 머리 띠 날짜/예배 이름 · 1쪽에 본문 **전문** 12절 + 요약 줄 없음 · 2쪽 라벨 셋과 '찬양 인도' · 캐릭터 컷 0 · 본문 도착 뒤 PDF 버튼 열림) · 탭은 **주보 + 네 개**(걷지 않는다 — 유튜브 링크·성경 읽기 잇기가 탭에 있다) · 노트 읽기 모드는 종이(`worship-note-read`) |
| `word` | **말씀 화면** — QT(오늘 본문 · 날짜 이동 · 묵상 MarkdownEditor · '더다붓에 공유하기' · 나눔 피드 · 나만 보는 잔디) · 성경 읽기(**목차 · 북마크 · 형광펜 세그먼트** · 리더 · 검색 · 이어읽기 · **절 형광펜**이 인라인 mark에만 칠해지는지 · 팝오버가 절마다 새로 앉는지) · 날짜를 넘길 때 묵상 칸이 위로 튀지 않는지 · 공유 토글이 dirty를 안 건드리는지 · **조작 가능한 공유 토글이 화면에 한 벌**(내 묵상 칸 — 나눔 줄에는 토글·칩·눈 가리기가 없다) · QT 날짜 데이트피커 · 잔디(날짜 숫자 · 20px 칸) · **장 넘기기**(데스크톱 sticky 화살표 · 모바일 스와이프, 세로 스와이프는 무시) · 어느 폭에서도 열이 트랙을 다 쓰는지(`data-col`) · 형광펜 도구 줄이 눌린 절 바로 아래 인라인인지 · 형광펜 4색·앵커 범위 선택(4→1, 1→4, 늘리기·줄이기, 해제) · 컷 0개 · 공유 칩 '나만 볼게요/더다붓에 공유할게요' · 비공개 묵상이 내 나눔 줄에 잠금 표시로 남는지(조작은 편집기에서) · 날짜별 캐시 · **묵상 읽기 모드**(저장된 날은 RichText + 수정 · 편집은 저장/취소, 취소는 줄 오른쪽 끝 · 편집기는 hidden으로만 · 모드 전환에 높이 불변) · **잔디 이전·다음 달**(‹ › · 이번 달에서 다음 비활성 · 다른 달 '오늘' · `word.shiftMonth` · 6주 높이 고정) · 도구 줄 grid(375에서 토글 묶음이 열 폭). 앞부분은 서버 없는 날짜 셈, 뒷부분이 브라우저. 게스트는 `word_qt_schedule`·`word_qt_entries`에 가짜 일정·묵상을 심는다 · **AI 검색**(15 — 프롬프트에 책 이름·DASH_RULE · JSON 파싱 · 참조 해석이 우리 본문 · 지어낸 참조 제거 · 캐시 1회 · 게스트는 도막 없음 · '해당 단어는 찾지 못했어요' · 자리표 '어떤 본문을 찾으시나요?') · **풀·캐시**(`forEachPool` 동시 6 정경 순 · `caches.open('bible-v1')` · 미리 받기) · **강조 3초** · **북마크 판** · **노트 템플릿**(8 — 5/4도막 · `isTemplateOnly` · 저장 잠김) · 묵상 삭제 문구 두 줄 · **칸별 펼침**(북마크↔형광펜 안 넘어감) · **검색어 여러 번 표시**(등장 수 = mark 수) · 세그먼트 `aria-pressed`(QT/성경 읽기 · Aa) · **검색 머리줄**(`searchHeads` — `N건`/상한 `50건`/0건+AI면 낱말 줄 없음/`…에 대해 AI가 찾은 구절 N건`) · **자리표 회전**(placeholder 첫 줄 고정 · 겹친 span · 게스트는 둘째 줄 없음 · layout 한 벌) · 템플릿 새 이름 + 옛 이름 호환 · **근거 문장 없음**(파싱은 참조 문자열 배열 · 옛 `{ref, why}` 모양이 와도 참조만 건짐 · 화면 줄에 `why` 키 없음) · **공유 캐시**(0057 — 남이 물어본 말이면 AI를 안 부름 · 없는 말은 한 번 나가고 캐시에 남음 · 열쇠는 `normalizeQuery`라 공백·대소문자 무관) · 노트 읽기 모드는 종이(`data-note-read`) · 도막 제목에 **잎 표시 없음**(사용자가 뺐다) |
| `groups` | **모임 줄기** — 내 순(구성원 · 예배 출석 · 공유된 노트) · 동아리(목록 · 끌어서 순서 · 가입 신청 · 리더 도구 · 모임 출석) · 순 편성(자격 분기 — **관리자에겐 보이고 교역자에겐 없는지** · 순원 추가/이동/빼기 · 새 순 · 리더 먼저+가나다) · 동아리 이름·설명 인라인 수정(`canEditClub` 네 경계) · 모임 한 줄 생성기 · **순모임 가이드 패널**이 순장에게만 보이고 내 순 카드와 좌우 끝이 같은지 · **순장 지정 4갈래**(다른 순의 순장 → 불가 · 이 순의 순원 → 바로 · 다른 순의 순원 → 먼저 순원 추가 안내 · 미배정 → 순장+구성원) · `sun_exempt` 제외 · 새 동아리 카드형·새 순 한 줄 생성기 · **같은 이름 막기**(0041 — 같은 해 순·동아리 전체) · 마스터가 명단 연결 없이도 가이드 자리를 보는지 · 컷 0개(마크 복귀) · 캐시 우선(모임 → 홈 → 모임 스켈레톤 없음) · 노트+가이드 한 덩이 스켈레톤 · 내 비공개 노트 잠금 표시 + **공용 ShareToggle 두 쪽**(라벨 고정 · 이 화면엔 편집기가 없어 조작이 줄에 남는다) · 표시명 기준 · 생성기 모션 · 원인이 붙은 실패 문구(줄바꿈) · **공유된 노트만**(내 비공개 노트는 목록·개수에 없음) · **동아리 신청 QR**(`canEditClub`만 버튼 · 카드 · 밝은 종이 · 공유/저장/복사/닫기) · **딥링크 `?p=groups&g=&apply=1`**(상세 열림 + 신청 대기 + 토스트 네 갈래) · `api/share.js` type `c`(OG · `error` 읽기). 토스트는 `[data-toast]`로 잡는다. 게스트 저장 자리 `church_groups_v1` — 예배 표는 `church_worship_v1`에도 같이 심는다(내 순의 출석이 그쪽을 읽는다) · **출석 기준**(`attendanceSunday` — 미래 발행 무시 · 오늘 0행이면 지난 주일 · 행 있으면 오늘) · **출석 태그·스켈레톤** · **QR 신청 fresh 게이트**(stale 한 벌로 판정 안 함 · 신청 뒤 목록 갱신) · **피커 추적**(앵커를 밀면 2프레임 안에 따라옴 · 폭 일치) · **모임 만들기 라벨형** · **상세 진입 `dc-nav-fwd`/back** · **순모임 가이드**(종이 구조 · 주보 피커 최근순·기본=고정/최근 · 순장 만들기 · 고정 잠금·배지·해제 · 이미지 1080px PNG에 로고 · 375/1440 · 라이트/다크 종이 밝음) · 연도 고르개가 로딩 중에도 남음(목록만 스켈레톤) · 순 카드 머리줄 1440 한 줄/375 두 줄(인원 수는 이름 줄) · 비우는 캐시는 `GROUP_KEYS` 셋 · 동아리·직분 조회 한 틱에 한 번(`groups.share`) · **터치 피커 인라인**(8 — `.person-pick` 자손 · 칸 아래 4px 같은 폭 · 아래 카드 밀림 · ↑↓ Enter · 순 옮기기 흐름 · 트리거가 줄에 남음 · 1024 굵은 포인터 · 데스크톱은 포털) · **가이드 고르기 먼저**(8 — 줄 최근순·강조·'가이드 있음'·`<날짜> 주보로 만들기`·그 주보 id로 저장·종이일 때만 머리줄 피커) |
| `roster` | **청년 명단**(멤버 화면의 [가입자 | 청년 명단] 탭 — 마스터+관리자. 직분 6종·연도 세그먼트·캐시·생일 yearless 피커·계정 연결 후보 세 갈래·'다녀감' 스토어 겹쳐 쓰기) — 생일 형식 · 검색 · 계정 연결 후보 · 직분 배지(순수 로직) + 추가 · 수정 · 팀 · 직분 · 계정 연결/해제 · 환송/되돌리기 · **도구 줄 줄나눔**(1440 한 줄 / 600·375 두 줄 — 버튼 오른쪽 끝, 연도 둘째 줄 왼쪽) · 칩 줄 끝 12px 여백 · `.dc-row` 등장(브라우저). 게스트 `church_roster_v1` · 가입자 줄(`MemberRow`)은 화면 함수 밖(1분 틱마다 리마운트·깜빡임 금지) |
| `home` | **홈**(첫 화면) — 인사말 · 카드 넷(오늘의 QT · 이번 주 예배 · 내 업무 · 내 순) · **히어로**(컷 무리가 원본 이하로 그려지는지 · 시각별 컷·인사말이 같은 경계(`heroSlot`)로 바뀌는지 · 날짜 칩이 인사말 오른쫽 같은 줄) · **쇼케이스 4블록**(컷·제목·두 줄 설명, 호버 애니, 반응형 1/2/4열) · 카드 넷 초점+메타·행 높이·화살표 제목 줄·두 자리 연도 · 인사말 4구간 · 카드 캐시(재진입 스켈레톤 없음) · 카드마다의 이동 · **자리 스켈레톤**(`orderedSlots` — 늦은 갈래가 있어도 카드 순서 고정 · 같은 칸에서 내용으로) · `+N건 더` 오른쪽 끝 버튼 · '공유된 노트 N' · 카드 hover · 빈 자리 · 375px · 다크 · **긴 메타 한 줄**(카드 좌우 여백이 같은지 · 줄이 카드 안에서 잘리는지 · 가장 긴 마감 날짜가 한 줄인지 — HANDOFF §6-9-r·9-s·9-u). 자기 저장 자리가 없어 말씀·예배·모임·업무 **네 곳에 심고** 한 화면에 모이는지를 본다 · '돌아오는 주 예배' · '+N건' · **이번 주일/지난 주일 N명 참석**(오늘 주보에 출석 행이 있으면 '이번 주일') · 6c는 새로고침 전 `caches.delete('bible-*')` · 첫 진입 스켈레톤 어긋남 0 · 홈/예배 날짜 한 벌(`formatServiceDate`) |
| `navsmoke` `onebar` `mobbits` | **v2 내비**(데스크톱 상단 [홈·예배·말씀·모임 \| 업무 대시보드·내 업무·일정] · 모바일 하단 바 — 홈에서는 교회 바(예배·말씀·모임·업무), '업무'를 누르면 업무 바 5칸(홈·프로젝트·내 업무·대시보드·팀)), 내비 구조, 데스크톱/모바일 내비가 하나만 마운트되는지, 모바일 상단바, **탭이 줄 폭만큼 보이는지**(1440px=8개 전부·800px=일부+더보기·무넘침), **더보기 연도 폴더**, **연도 고르기**(탭 줄 맨 앞 `2026 ▾` · 연도는 만든 날짜가 아니라 `projects.year`를 따른다 — 0025), **보관**(보관하면 그 탭에서 나가는지 · 열어 둔 보관 프로젝트가 탭과 더보기에 동시에 보이지 않는지) · **화면 전환**(교회 축 사이 `dc-nav-fwd/back` 방향 · 업무 축엔 없음 · 탭을 옮기면 `main.scrollTop===0` · reduced-motion) |
| `bottomgap` | 모바일 각 탭에서 마지막 내용이 하단 탭바에 가리지 않는지 |
| `modalclose` | 업무 상세 모달 바깥 클릭·드래그·닫기, **댓글·활동 사이드바 접기**(폭 0 · 언마운트하지 않음), 저장해도 댓글·활동이 남는지, **답글 UX**(답글 버튼이 hover 없이 보이는지·답글 수·'○○님에게 답글' 줄·textarea·`@` 멤버 제안·접어도 초안이 남는지·등록) |
| `batch10` `batch11` `dashfix` `wide` | 대시보드·모바일 조작·문구·KPI 배치(320~1920px) · **최근 활동 피드·프로젝트 연결 지도** · **모바일 세 탭(업무·청년·연결)·연도별 프로젝트 진행·`+ 프로젝트` 정렬**(dashfix) · **'다녀감'과 '활동'이 같은 시각을 본다**(dashfix 3 — 활동이 더 최근이면 그 시각이 다녀감으로 뜨고, 활동이 없는 사람은 다녀간 시각 그대로이며, 피드의 그 줄과 목록의 그 줄이 같은 값을 말하는지) · **'지난 7일 간 N건'은 `completed_at` 기준**(dashfix 1 — 첨부만 만진 옛 완료 업무를 다시 세지 않는다) |
| `share` `onboard` | 공유 링크(프로젝트·업무), 첫 로그인 팀 설정 |
| `three` | 프로젝트 이름 수정, **참고 링크**(포털 팝오버 안을 눌러도 닫히지 않는지 · 서비스 표시 · 저장 · **구글 문서 링크는 앱 안 창** — iframe src `/edit`·`rm=minimal`·`#gid` 보존·`sandbox` 없음 · 비밀번호가 걸린 링크는 `PwPrompt`가 먼저 · `docEmbedKind`·`makeViewPw`↔`verifyViewPw` 왕복은 앞부분에 노드로), **헤더 배치**(375·414·768에서 삭제가 화면 안이고 링크 스크롤 칸 밖인지 · 모바일에서 실선·공유·삭제 묶음이 '새 업무' 버튼 **가운데**에 서는지(자획 기준) · 실선이 아이콘에 붙어 있는지 · 묶음이 버튼 칸을 안 벗어나는지 · 링크 줄과 아이콘의 **가운데선**이 같은지 · '+ 참고 링크'가 미는 칸 밖인지 · 헤더 아래 실선 밖으로 나가는 것이 없는지 · '새 업무'가 값 줄과 같은 줄인지 · 진척 바 · 담당자 얼굴이 바 뒤인지 · 320px에서 값 줄이 넘치지 않는지), **그래프 보기**(노드·선·열 배치·수정 폼의 선행 업무 칸), 모바일 가로 스크롤 잠금, 컬럼 구분 · **구글 iframe 준비 화면 다섯 자리**(4 — 모달 sheet·gdoc·drive·office + 첨부 `InlineSheet`, `FRAME_SETTLE`·페이드) |
| `themefit` | **라이트/다크 × 모바일(390)/데스크톱(1440)** 네 조합 × 대시보드·보드·업무 창. 글자가 배경에 묻히지 않는지(대비 2.0 미만), Tailwind 기본 팔레트를 쓰지 않는지(§8), 가로로 넘치지 않는지, 본문 체크박스를 직접 그리는지 |
| `calfit` | 캘린더가 창 높이에 맞는지, 띠가 잘리지 않는지, 날짜 한 번에 선택, **모바일에서 격자와 목록이 다른 스크롤 통인지**(목록을 밀 때 달력이 밀려 올라가지 않아야 한다) · 목록이 좌우로 밀리지 않는지 |
| `drag` `dragdesk` | 터치/마우스 드래그로 상태 이동, 상태 칩 드롭, **같은 상태 안에서 순서 바꾸기**(0024 — 데스크톱·모바일 양쪽 · position 저장 · 컬럼 안 세로 스크롤 유지 · **컬럼 빈 자리에 놓으면 맨 밑으로**. 마지막 것은 카드 셋짜리 시드를 따로 쓴다 — 공용 시드는 컬럼이 꽉 차 빈 자리가 없다) · **모바일 프로젝트 탭 순서**(길게 눌러 끌기 · 뒤로 끌면 놓은 탭 뒤 · 짧게 누르면 이동만 · 가로 스크롤 유지) |

## 셀렉터가 낡으면

앱 마크업이 바뀌면 셀렉터가 못 찾고 스크립트가 **예외로 죽는다**. 이때 종료 코드는 1이지만
`FAIL` 줄이 없어서 러너가 `CRASH`로 표시한다 — "테스트가 통과했다"와 구분되니, CRASH가
보이면 그 스위트의 셀렉터를 지금 화면 기준으로 다시 맞춰야 한다.
