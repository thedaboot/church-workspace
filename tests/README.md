# tests

> **도는 동안 레포 파일을 고치지 마라.** dev 서버가 vite여서 **아무 파일이 바뀌면**
> 열려 있는 페이지를 새로고침한다(`HANDOFF.md`도 그렇다) — 좌표를 재던 스위트가
> 발밑을 잃고 `CRASH`나 엉뚱한 FAIL로 나온다. 깨졌으면 **손대지 않고 그 스위트만
> 혼자 다시** 돌려 보는 것이 첫 수순이다.

브라우저를 실제로 띄워 화면을 재는 검증 스위트. 단위 테스트 프레임워크는 쓰지 않는다 —
각 파일이 헤드리스 Chrome을 CDP로 붙잡고, 화면을 만들고, 좌표·계산값·localStorage를 재서
`PASS` / `FAIL` 줄을 찍고 종료 코드로 결과를 알린다.

```bash
npm run verify -- calfit drag  # **골라서 — 고친 것과 관련된 것만. 이게 기본이다**
npm run verify                 # 전부(약 7분). **사용자가 부를 때만** — 푸시 직전도 아니다
npm run verify -- --jobs 3     # 동시 실행 (스크립트마다 CDP 포트가 달라 충돌 없음)
SHOTS=1 npm run verify         # 스크린샷도 저장
CHROME=/path/to/chrome npm run verify   # 크롬 경로가 다를 때
```

- 러너가 `vite --mode guest`를 띄운다. `.env.guest`가 없으면 만든다(빈 Supabase 키 = 로그인
  없이 localStorage 모드). 이 파일은 커밋하지 않는다.
- 각 스위트는 `node tests/<name>.mjs <베이스 URL>` 로 혼자서도 돌아간다. 게스트 서버를 직접
  띄우려면 `npx vite --mode guest --port 4598 --strictPort`. 아래 표에 **(노드)** 라고 적힌
  것은 브라우저도 서버도 필요 없다(`run.mjs`의 `NODE_ONLY`).
- **스위트는 33종**이다(`tests/run.mjs`의 `ORDER`가 목록이고 디스크와 같아야 한다 —
  `run.mjs`·`zip.mjs`는 스위트가 아니라 러너와 공용 도구다). 새 스위트를 만들면 `ORDER`에
  넣고 이 표에도 한 줄 남긴다.
- **여러 러너를 동시에 띄우지 마라.** 한 러너 안에서는 스크립트마다 CDP 포트가 갈라져 있지만,
  러너를 둘 띄우면 그 보호가 없어 서로의 크롬에 붙어 `CRASH`·타임아웃이 난다(HANDOFF §6-42-a).

## 스위트

| 파일 | 보는 것 |
|---|---|
| `logcheck` (노드) | `utils.js`·서비스 계층의 **순수 로직 전부** — 활동 기록 · 하위 업무 진척 · 정렬·순서 규칙 · 지금 보고 있는 사람 · 다녀간 시각·심장박동 · 캐시 범위 · v2 실시간 배선 · 호칭 · 그래프 물리 상수. 새 순수 함수는 기본적으로 여기에 붙는다 |
| `mdcheck` (노드) | 마크다운 ↔ 에디터 문서 모델 왕복(`services/markdown.js`) — 서브셋 밖 문법이 조용히 사라지지 않는지 |
| `bibleref` (노드) | 구절 참조 파서(`services/bibleRef.js`)와 커밋된 `public/bible/` 데이터의 정합(절 수·양끝·클램프·`formatRef` 왕복) |
| `sunguide` (노드) | 순모임 가이드의 순수 로직(`services/sunGuide.js`) — 프롬프트에 실리는 것 · 코드펜스 JSON 파싱 · `fitGuide`가 상한을 문장 경계에서 자르는지 |
| `sheet` (노드) | 엑셀 파서의 **글자 뽑기**(`services/xlsx.js`) — zip·XML 훑기 · 셀 주소 · 공유 문자열 · 서식 · 500줄에서 멈추기. 표를 그리는 것은 구글이므로 서식 충실도는 안 본다 |
| `office` (노드) | 워드·PPT 파서(`docx.js`·`pptx.js`)와 공용 zip·XML 기계(`ooxml.js`) — 상대 경로 · 문서 순서 · 표 병합 · 슬라이드 순서·자리 표시자 |
| `drivesync` (노드) | 드라이브 ↔ 앱 **싱크 불변식**(소스 단정) — 업로드 한 벌·멱등·되돌리기 · 상한 세 곳이 같은 값인지 · 프록시와 Apps Script 문서(`docs/APPS_SCRIPT.md`)가 같은 액션을 아는지 · 미리보기 사본과 큐시트 편집 주소 |
| `assignees` (노드) | 담당자 ↔ 프로필 id 변환. `cloud.js`를 가짜로 바꿔치고 `cloudSync`만 노드에서 돌린다 — 게스트 모드로는 볼 수 없는 경로다 |
| `push` (노드) | 알림 문구(앱·푸시 공용) · 크론 둘의 분기와 KST 날짜 · 딥링크 · 이전에 없던 대상만 · 본인 제외 · 체크 제약과 INSERT 정책의 짝 · 댓글 반응 |
| `aictx` | AI 프롬프트에 실리는 것 전부 — **등장하지 않는 사람이 안 들어가는지**가 핵심이다. 죽은 멘션 되돌리기 · 관련 업무 링크 · 발췌 상한 |
| `errhunt` | 화면 × 데이터 상태 × 화면폭 64조합에서 ErrorBoundary·콘솔 오류 |
| `handoff` | 핸드오프 규격 — 토큰·모션·화면 구조 · 업무 상세/수정 · 팀 보드 칩 줄 · 에디터 서식 바 자리 · 업무 링크가 '첨부 파일' 구역 안인지 |
| `worship` | 예배 줄기 — 주보 목록·상세·탭 · 작성→발행 · 본문 피커 · 담당자·찬양·광고 편집 · 출석 체크(발행 + 예배일 이후 · 순장은 자기 순) · 손님 · 예배 노트와 종이 · 송폼·큐시트 |
| `word` | 말씀 화면 — QT(본문·날짜 이동·묵상 편집기·공유 토글·나눔 피드·잔디) · 성경 읽기(리더·검색·북마크·절 형광펜과 범위 선택·이어읽기) · 노트 종이 |
| `groups` | 모임 줄기 — 내 순(구성원·출석·공유된 노트) · 동아리(순서·가입 신청·리더 도구·모임 출석·QR) · 순 편성과 자격 분기 · 순장 지정 4갈래 · 순모임 가이드 패널 |
| `roster` | 청년 명단 탭 — 직분 6종·연도 세그먼트·검색·생일 피커·계정 연결 후보 세 갈래·환송/되돌리기 · 도구 줄 줄나눔 |
| `home` | 홈 — 인사말·히어로·쇼케이스 · 카드 넷의 차례·초점/메타 한 줄·자리 스켈레톤·캐시 · 카드마다의 이동 |
| `navsmoke` `onebar` `mobbits` | v2 내비 — 데스크톱 상단 한 줄과 모바일 하단 바(교회 바 ↔ 업무 바 모드 전환) · 둘 중 하나만 마운트되는지 · 탭이 줄 폭만큼 보이는지 · 더보기 연도 폴더 · 모바일 상단바 |
| `bottomgap` | 모바일 각 탭에서 마지막 내용이 하단 탭바에 가리지 않는지 |
| `modalclose` | 업무 상세 모달 바깥 클릭·드래그·닫기 · 댓글·활동 사이드바 접기(언마운트하지 않음) · 답글 UX |
| `batch10` `batch11` `dashfix` `wide` | 대시보드 — KPI 배치(320~1920px) · 최근 활동 피드 · 프로젝트 연결 지도 · 모바일 세 탭 · '다녀감'과 '활동'이 같은 시각을 보는지 · '지난 7일 간 N건'이 `completed_at` 기준인지 |
| `share` `onboard` | 공유 링크(프로젝트·업무) · 첫 로그인 팀 설정 |
| `three` | 프로젝트 이름 수정 · 참고 링크(팝오버·서비스 표시·저장 · 구글 문서는 앱 안 창 · 비밀번호가 걸린 링크) · 헤더 배치가 좁은 폭에서 화면 안인지 |
| `themefit` | 라이트/다크 × 모바일(390)/데스크톱(1440) 네 조합 — 글자가 배경에 묻히지 않는지(대비) · Tailwind 기본 팔레트를 쓰지 않는지 · 가로로 넘치지 않는지 |
| `calfit` | 캘린더가 창 높이에 맞는지 · 띠가 잘리지 않는지 · 모바일에서 격자와 목록이 다른 스크롤 통인지 |
| `drag` `dragdesk` | 터치/마우스 드래그로 상태 이동·상태 칩 드롭 · 같은 상태 안에서 순서 바꾸기(빈 자리에 놓으면 맨 밑) · 모바일 프로젝트 탭 순서(길게 눌러 끌기) |

## 셀렉터가 낡으면

앱 마크업이 바뀌면 셀렉터가 못 찾고 스크립트가 **예외로 죽는다**. 이때 종료 코드는 1이지만
`FAIL` 줄이 없어서 러너가 `CRASH`로 표시한다 — "테스트가 통과했다"와 구분되니, CRASH가
보이면 그 스위트의 셀렉터를 지금 화면 기준으로 다시 맞춰야 한다. 셀렉터를 못 찾을 때
**던지지 말고 FAIL로 남기는** 것이 새 검사를 쓸 때의 규칙이다(HANDOFF §6-40).
