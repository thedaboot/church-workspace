node_modules
dist
dist-guest
.env
.env.guest
.DS_Store
tests/*.png
# 검증 스크립트를 임시로 돌릴 때 나오는 스크린샷. 출력 경로를 실수로 레포 루트('.')로
# 넘기면 여기에 쌓인다 — 실제로 8장 1.9MB가 커밋됐다. 루트 이미지는 통째로 무시하고,
# 진짜 자산은 public/ · src/assets/ 안에만 둔다(그 둘은 이 규칙에 걸리지 않는다).
/*.png
/*.jpg
/*.jpeg
/*.webp
.claude/
.agents/
skills-lock.json
.vercel
